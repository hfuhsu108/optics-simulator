# Design Brief — 光的成像（針孔・面鏡・透鏡・眼睛）

> 讀者：Claude Design。**你沒有看過委託方的對話**，本檔自包含。
> 委託方：台灣國中理化教師，這是「國中理化互動模擬」系列的第四支。
> 交付對象是教室：Windows 筆電、學生 iPad、**教室觸控大屏**（不是投影機）。

---

## 0. 一句話需求

一支涵蓋國中光學「成像」全部主題的互動模擬，**四個底部分頁**：針孔成像｜面鏡成像｜透鏡成像｜眼睛。
畫面的主體是**光路圖**——學生拖動物體與紙屏，看光線怎麼走、像長在哪裡、是實像還是虛像。

---

## 1. 專案脈絡

### 這個模擬要教什麼（理解教學意圖才設計得對）

國中光學的核心困難不在公式，在**「像」到底是什麼**。學生普遍以為像是貼在鏡面上的一張貼紙、
以為實像一定要有屏才存在、以為遮住透鏡一半像就會少一半。這支模擬的每一個功能都對著一個具體的誤解。

四個分頁的教學鏈：

1. **針孔成像** — 光沿直線前進。沒有焦距、沒有透鏡，最單純的成像。
2. **面鏡成像** — 反射。平面鏡（恆虛像、等距）、凹面鏡（可實可虛）、凸面鏡（恆虛像縮小）。
3. **透鏡成像** — 折射。凸透鏡（五個區間各不相同）、凹透鏡（恆虛像縮小）。
4. **眼睛** — 前一頁的應用。正常眼、近視、遠視，以及用透鏡矯正。

### 這支模擬與前三支最大的不同

**畫面上不出現任何數值。** 委託方明確指定：物距、像距、放大率全部不標，改用背景格線讓學生自己數格判讀。
因此**格線不是裝飾網底，是這支模擬唯一的量測工具**——它的視覺份量要重新拿捏（見難題 2）。

---

## 2. 既有設計規範（必須遵守）

**本節只告訴你「哪一節管什麼」，規格本身一律去讀原檔，不在這裡重抄。**
這是這個系列踩過的教訓：brief 抄一份規格表，規範改版後 brief 不會有人回頭更新，
下一份設計稿就會照著過期的值畫。以下引用的節號請直接翻閱附帶的檔案。

| 你要決定的東西 | 去讀哪一節 |
|---|---|
| 版面骨架、欄數與比例、iPad 直向的摺疊 | `design/DESIGN_SPEC.md` **第 1 節** |
| 介面色彩、對比度門檻、合格色定案表 | 同檔 **第 2 節** |
| 畫布繪圖配色（光線、器材、圖表序列色） | 同檔 **第 3 節** |
| 共用元件（按鈕、膠囊、分段切換器、滑桿、核取、下拉、數據卡） | 同檔 **第 4 節** |
| 字體與字級級距 | 同檔 **第 5 節**、`DEV_SPEC.md` 第 3 節 |
| 圓角、間距、陰影的使用範圍 | 同檔 **第 6 節** |
| 觸控命中區、響應式斷點 | `DEV_SPEC.md` **鐵律 6**、5.3 |
| 圖示 | `DEV_SPEC.md` **鐵律 10**（Font Awesome 6，禁止裸符號與 emoji） |

### 這支模擬特別要注意的三條（都是規範裡剛加的，前三支模擬沒有先例）

1. **兩欄，不是三欄**（`DESIGN_SPEC` 第 1 節「兩欄配置」）
   `grid-template-columns: 1fr 3fr` — 左欄設定、右邊整片是畫布，**沒有右欄**。
   1366×768 下左欄 331 / 畫布 995。理由：這支模擬的讀數本來就長在光路圖上（物距是光軸上的一段），
   搬進右欄卡片反而違反「即時讀數貼在它描述的圖形旁」。
   **`design/layout.html` 畫的是三欄版本，不要照它畫成三欄。**

2. **格線分主次**（`DESIGN_SPEC` 第 2 節）
   格線在這支模擬是**量測工具**而不是裝飾網底（學生要靠數格判讀），所以每 5 格有一條加深的主格線。
   **兩個色值請去第 2 節的合格色定案表取**——主格線的深淺**尚未經教室大屏實測、很可能會調**，
   這裡刻意不抄值，就是為了不讓這份 brief 過時。

3. **沒有底部抽屜**（`DESIGN_SPEC` 第 1 節「抽屜的存廢」）
   這支模擬沒有隨時間推進的動畫，沒有播放／暫停／時間軸。
   重置等動作鈕掛在**左欄下方的動作卡**（與左欄同寬、緊貼其下、白卡 r10、不加陰影）。
   `layout.html` 畫了抽屜，同樣不要照畫。

---

## 3. 一併提供給你的檔案

- `_shared/DEV_SPEC.md` — 工程規範（鐵律、字體、觸控、響應式）
- `_shared/design/DESIGN_SPEC.md` — 視覺規範（上表引用的就是它）
- `_shared/design/layout.html` — 版面範例，瀏覽器直接開。**注意它示範的是三欄＋抽屜，本模擬兩者都不同**
- `_shared/design/components-spec.html` — 元件與配色視覺總覽，瀏覽器直接開

---

## 4. 本模擬的資訊架構與內容清單

### 4.1 畫布的座標系統（四頁共用，這是版面設計的地基）

- **縱向固定 16 格**，格邊長 ＝ 繪圖區高 ÷ 16（1366×768 下約 38.6px）；橫向格數隨寬度浮動（約 25 格）。
  格子必須是**正方形**——學生要用它同時量水平的距離與垂直的高度。
- **光軸**是一條貫穿畫面的水平線，固定在第 8 格（正中）。鏡／透鏡／針孔固定在橫向中央。
- 光軸上用小三角形加字母標 **F** 與 **2F** 的位置（**只有字母，不寫數值**）。焦距改變時它們跟著移動。

### 4.2 四頁共用的畫布元素

| 元素 | 要求 |
|---|---|
| 實際光線 | 實線，帶方向箭頭 |
| 反向延長線 | 虛線且降透明度 |
| 實像 | **實心**箭頭 |
| 虛像 | **半透明空心**箭頭 |
| 三條特徵光線 | 各自一個核取方塊，預設全開 |
| 完整光束 | 核取方塊；從物體頂端發出十幾條光線，全部聚到同一點 |
| 光屏（紙屏） | 核取方塊控制顯示、**可左右拖**。對焦時屏上的像清晰，離焦時模糊。**虛像情境下永遠對不到焦** |
| 觀察者眼睛 | 核取方塊控制顯示。位置固定，畫出進入眼睛的實線與腦補的虛線延長 |
| 物體 | **可左右拖**。外觀三選一（膠囊）：**箭頭／蠟燭／F 形** |
| 快捷鈕 | 五顆，一鍵跳到 `u>2f`、`u=2f`、`f<u<2f`、`u=f`、`u<f`（針孔頁不適用） |

### 4.3 分頁 1「針孔成像」

畫布：左為物體、中為開孔的擋板、右為可拖的紙屏。沒有焦距，也沒有特徵光線。

**核心畫面（委託方親自指定的畫法）**：從物體上的**一個發光點**射出**一組離散光線**（十幾道），
擋板把大部分擋掉——**被擋的光線畫到板面就停**，穿過孔的繼續走到屏上，各自落在不同位置。
孔小 → 只有一道通過 → 屏上一個點 → 清晰而暗；孔大 → 多道通過 → 屏上散成一段 → 模糊而亮。
**「亮」與「糊」必須看起來是同一件事的兩個後果**，不是兩條要各自背的規則。

孔徑用**上下兩片可拖的擋板**控制（拖近拖遠＝縮放孔徑），左欄另給五檔膠囊當快捷。

左欄：孔徑大小（膠囊 5 檔）｜孔的形狀（膠囊：圓／方／三角）｜孔的數量（膠囊：1／2／3）｜
單點光束示範（核取）｜完整光束（核取）｜物體外觀｜光屏（核取，預設開）

### 4.4 分頁 2「面鏡成像」

左欄頂端用**分段切換器**切三種鏡：**平面鏡｜凹面鏡｜凸面鏡**。
（用分段切換器而非膠囊，是因為切換後左欄會整組改變——平面鏡沒有焦距，焦距膠囊與五顆快捷鈕整組消失。
判準見 `DESIGN_SPEC` 第 4 節。）

左欄：鏡種切換器｜焦距三檔（平面鏡時隱藏）｜五顆快捷鈕（平面鏡時隱藏）｜三條特徵光線核取｜
完整光束｜光屏｜觀察者眼睛｜物體外觀

**這一頁要打掉的誤解**：像貼在鏡面上。做法是鏡後畫出等距虛像與反向延長虛線，
並在鏡面**兩側各標出格數**，拖物體時兩邊格數一直相等。

### 4.5 分頁 3「透鏡成像」

左欄頂端分段切換器：**凸透鏡｜凹透鏡**。焦距三檔，**透鏡的形狀要跟著變厚變薄**（越凸焦距越短）。

左欄：透鏡種類切換器｜焦距三檔｜五顆快捷鈕｜遮板（可拖）｜三條特徵光線核取｜
完整光束｜光屏｜觀察者眼睛｜物體外觀

**這一頁要打掉的誤解**：遮住透鏡一半，像會少一半。做法是一塊**可從透鏡上方往下拉的遮板**——
被擋的光線不畫，剩下的光束仍聚到同一點，屏上的像**逐漸變暗但形狀完全不變**。

### 4.6 分頁 4「眼睛」

畫布：眼球剖面（角膜、水晶體、視網膜）＋外面的景物，光線聚焦在視網膜的**前／上／後**。

左欄頂端分段切換器：**正常眼｜近視眼｜遠視眼**。

成因**只做一種**：水晶體太厚（近視）／太薄（遠視）。**不做眼軸過長。**
整頁只有一個水晶體焦距在動，三層疊加：眼睛狀態決定基準厚度 → 調節作用（看遠／看近）再加一層 →
矯正鏡（凹／凸）＋度數滑桿是第三層。

左欄：眼睛狀態切換器｜調節作用（看遠／看近）｜戴上眼鏡 toggle｜矯正度數滑桿｜視覺模擬（核取）

**視覺模擬**：畫布角落一個小視窗，畫**遠處的樹＋近處的書本**，配合看遠／看近切換，
用同一套模糊呈現「這個人看到的畫面」。

**這一頁要打掉的誤解**：近視眼鏡是凸透鏡。矯正鏡的形狀要畫清楚（近視凹、遠視凸），
拿開／戴上能即時對照像落在視網膜前還是後。

---

## 5. 需要你解決的難題

### 難題 1｜四頁的器材完全不同，但要看起來是同一個系統

擋板＋紙屏、三種鏡、兩種透鏡、眼球剖面——四頁的主角長相毫無共通點，
但共用同一條光軸、同一套格線、同一組光線語彙。請給出一套**器材繪製語言**
（描邊粗細、填色、與光軸的對齊方式），讓四頁一眼認得出是同一支模擬，又能一眼分辨在第幾頁。

### 難題 2｜格線是量測工具，卻不能蓋過光線（最重要）

同一張圖上會同時出現：主格線、次格線、光軸、三條特徵光線（實線）、反向延長線（虛線）、
完整光束（十幾條）、物體、像、光屏、擋板／遮板、觀察者眼睛、F 與 2F 記號。**十二種圖層。**

請給出**明確的圖層優先順序與視覺層級**（誰壓在誰上面、誰用多粗、誰可以被壓淡）。
特別是：完整光束打開時十幾條線會鋪滿畫面，此時格線還讀得出來嗎？三條特徵光線還分得出來嗎？

### 難題 3｜光屏是側視的一條線，但屏上的像是二維的

側視光路圖裡，紙屏是垂直於光軸的一條線段。可是委託方要求學生能看到屏上的影像
「從模糊到清晰」的過程，還要能看出**孔的形狀不影響像的形狀**（圓孔糊成圓斑、方孔糊成方斑）。
一條線段上放不下二維影像。

請解決：屏要怎麼畫（帶透視的面？另開一個「屏的正面」小視窗？），
才能同時看得到「屏在光路上的位置」與「屏上現在長什麼樣」。這兩件事都不能犧牲。

### 難題 4｜實像與虛像的視覺差異要多大

實心 vs 半透明空心。但半透明到什麼程度？教室大屏遠看要一眼分辨，
又不能讓虛像看起來像「還沒載入完」或「壞掉的圖層」。請給出兩者的具體規格與並排對照圖。

### 難題 5｜三條特徵光線可以各自關掉，關掉時是消失還是變灰？

老師會關掉兩條只講一條。如果直接消失，畫面會突然變空、學生失去參照；
如果變灰留著，又可能被誤認為「另一種光線」。請決定並說明理由。

### 難題 6｜物體有三種外觀，但它們是三種不同的東西

箭頭是**作圖語言**（課本上的畫法）、蠟燭是**實驗情境**（實驗室桌上的東西）、
F 形是**判讀工具**（唯一能看出左右相反的圖案）。三者要在同一個位置互換，
高度基準與「頂端」的定義必須一致（三條特徵光線都是從物體頂端射出的）。請設計這三個造型。

### 難題 7｜眼球剖面怎麼畫成扁平教學插圖

規範第 3 節的儀器風格是「扁平填色＋2px 深一階描邊＋6px 圓角，不做擬真漸層陰影」，
但眼球是有機曲線。角膜、水晶體、視網膜、視神經要畫到什麼程度？
水晶體還必須能**明顯地變厚變薄**（那是這一頁的主要變因）。

### 難題 8｜左欄四頁的控制項完全不同，切換分頁時怎麼不迷路

針孔頁沒有焦距與特徵光線，眼睛頁沒有物體外觀與快捷鈕。
四頁的左欄長度與項目差很多。請給出左欄的**分區結構**（哪些區塊是四頁固定的、哪些是該頁專屬的），
讓學生切分頁時知道「同一個東西還在同一個位置」。

---

## 6. 硬約束（不可違反，違反等於不能用）

1. **不載入任何網路字型**（Google Fonts、Noto Sans TC、自載 woff2 全部不行）。只用 `DESIGN_SPEC` 第 5 節的系統字型堆疊。
2. **Font Awesome 6 是唯一允許的外部資源**。圖示只能從 FA6 挑，且要註明用哪個圖示名；禁止 emoji 與裸符號。
3. **不要輸出 React / Vue / Tailwind 程式碼**。成品是單一 HTML 檔內嵌原生 CSS/JS。可以用 Tailwind 的**數值語彙**描述間距，但不要交付需要 build step 的東西。
4. **色票只用 `DESIGN_SPEC` 第 2、3 節那一組**，不要新增品牌色或漸層。
5. **兩欄版面，沒有右欄；沒有底部抽屜。** 這兩點與 `layout.html` 的示範不同，以本檔第 2 節為準。
6. **畫面上不出現任何數值。** 物距、像距、放大率一律不標。F 與 2F 只有字母。
7. **觸控目標 ≥ 44×44px**；響應式斷點 **900px**（不是 768px）。`<900px` 時左欄摺成浮出面板，**只有「設定」一顆 chip**（沒有右欄，所以沒有第二顆）。
8. **字重只用 400 / 700**；字級最小 12px。
9. **不做裝飾性 UI**：無插畫背景、無擬真陰影、無玻璃擬態。資訊密度優先。
10. **每個視覺輔助都要有開關**（`DESIGN_SPEC` 第 4 節「教學輔助疊層」），不得寫死常駐。

---

## 7. 交付格式

**要**：

- 三種尺寸的完整版面稿：**1366×768（Windows 桌機）**、**1024×768（iPad 橫向）**、**768×1024（iPad 直向）**
- **四個分頁各自的版面稿**（不要只畫一頁，四頁的左欄與畫布主角差異極大）
- **光路圖的視覺語言元件稿**（難題 2）：實線光線、虛線延長線、實像、虛像、光軸、主格線、次格線、
  F 與 2F 記號，以及**它們疊在一起時的樣子**（含「完整光束打開」的最擁擠情形）
- **紙屏的方案稿**（難題 3）：屏在光路上的畫法，加上對焦／離焦／虛像三種狀態下屏上的樣子
- **物體三種造型稿**（難題 6）：箭頭、蠟燭、F 形，含尺寸與頂端基準線的對齊
- **眼球剖面稿**（難題 7）：含水晶體最厚與最薄兩種狀態
- 元件狀態稿：分段切換器（三檔與兩檔）、焦距膠囊三檔、五顆快捷鈕、核取方塊群、
  矯正度數滑桿、左欄下方的動作卡、底部導覽四分頁
- **色票與間距標註**（用 `DESIGN_SPEC` 第 2、3 節的色碼，標出你用的間距數值）
- 第 5 節八個難題的**文字說明**（你的方案與理由）

**不要**：

- React / Vue / Tailwind 程式碼，或任何需要 build step 的產出
- 新字型、新色票、裝飾性插畫
- 只在 hover 才出現的資訊
- 擬真的光學實驗照片風格（本系列是扁平教學插圖風）
- 三欄版面或底部抽屜（見硬約束 5）
