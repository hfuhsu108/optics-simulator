# 光的成像（optics-sim）— 專案進度

> **本檔是跨 session 的唯一權威進度來源。** 不要靠 memory 或對話記憶推斷進度。
> **來源標籤**：**A**＝使用者原話（附引文）｜**B**＝AskUserQuestion 選定（抄選項原文）｜**C**＝模型判斷（使用者未表態）。
> **只有 A、B 算定案**；C 類是我按規範與教學經驗抓的預設值，使用者隨時可以推翻。
>
> 建立於 2026-09-04（規劃 session）。

---

## 1. 專案基本資訊

| 項目 | 內容 | 來源 |
|---|---|---|
| repo 名 | `optics-sim` | **A**「optics-sim」 |
| 標題 | 光的成像｜國中理化模擬 | C |
| 形式 | **一支模擬、一個 repo、一個 `index.html`** | **A**「這幾個模擬是在同一個網站」 |
| 底部分頁 | 針孔成像｜面鏡成像｜**視深**｜透鏡成像｜眼睛 | **A**「透過底下導覽列就是針孔成像、面鏡成像、透鏡成像、眼睛這四個主題」＋ 2026-09-13 **A**「多一個頁籤，內容是折射，放在面鏡成像和透鏡成像之間」＋ 2026-09-14 **A**「頁籤標題改成視深」（見 §13） |
| 本機預覽 | `.claude/launch.json` 的 `optics`，port **4177**，serve root 限 `./optics-sim` | C |
| 部署 | GitHub Pages，`main` / root（尚未建 remote） | — |

> **架構修訂鏈**：規劃過程中曾一度定為「4 支獨立模擬，照課本四節」（**B** 選項原文「4 支（照課本四節）」），
> 隨即由使用者更正為「同一個網站、四個底部分頁」。**4 支那個版本作廢**，留在這裡是避免下一個 session 又把它拆開。

---

## 2. 已定案

> ⚠ **2026-09-08 此節有 10 條定案已被使用者當面推翻，讀本節前先讀 §6**。被推翻的是：
> ①「觀察者眼睛」整個功能（§2.1 表、2.4、2.5、2.7 多處提到）②「視覺模擬／看到的樣子」小窗
> （§2.3 頁 4、§2.7 眼睛頁、§5 階段 5）③「圖例卡要有開關」（§2.6 修訂表第 9b 條）
> ④「箭頭與 F 由模型繪製、半寬 ≤0.4 格」（§2.6）⑤ 針孔屏上的像「沿孔形**輪廓**取樣」（§2.3 頁 1）
> ⑥ 眼睛頁「只有兩條特徵光線、不畫通過鏡心」⑦ 眼睛頁「未矯正一律畫成最凸／最薄」
> ⑧ `EYE_ACC` 的四個端點（原本由「像落在視網膜前／後 1.5 格」反解）⑨ 矯正度數「預設最弱」
> ⑩ 眼睛頁「看遠／看近兩個代表位置」（改成可拖範圍三等分）。
> 另有一條**新增**：面鏡成的像要左右相反（§6 第 9 項）。未列在上面的定案一律仍然有效。

### 2.1 版面

| 項目 | 定案 | 來源 |
|---|---|---|
| 欄數 | **兩欄**，`grid-template-columns: 1fr 3fr`。**無右欄** | **A**「我覺得合理的配置是不要右欄，把空間給畫布」 |
| 抽屜 | **不設底部抽屜**。動作鈕掛左欄下方動作卡 | **A**「一律不做動畫，底下抽屜可以拿掉」 |
| 1366×768 實際欄寬 | 左欄 331 / 畫布 995（三欄 2:1 時畫布只有 657，寬 51%） | C |
| `<900px` | 左欄摺成浮出面板，**只有「設定」一顆 chip**（規範原本假設「設定/數據」兩顆） | C，待回流 X3 |

「設定 : 其餘 ＝ 1:3」這條既有比例規則**不變**，只是把數據併回畫布欄。

### 2.2 畫布通則（四頁共用）

| 項目 | 定案 | 來源 |
|---|---|---|
| 數值標示 | **一律不標任何數值**，靠背景格線讓學生自己判讀 | **A**「一律不標，背景可以給淡淡的格線讓學生判讀」 |
| 格線 | 主次兩級；`F`、`2F` 用小三角形＋字母標在光軸上，**不寫數值** | **B** 選項原文「主次格線＋標 F、2F（推薦）」 |
| 格線幾何 | **縱向固定 16 格**，格邊長 ＝ 繪圖區高 ÷ 16（1366×768 下約 38.6px），橫向格數隨寬度浮動（約 25 格）。光軸固定在第 8 格，鏡／透鏡／針孔固定在橫向中央 | C |
| 主格線 | 每 **5 格**一條加深 | C |
| 焦距 | **可調 3 檔**，`f = 2 / 3 / 4 格`（對應 `2f = 4 / 6 / 8 格`，物體最遠可拖到 12 格 ≈ 3f）。焦距一變，`F`／`2F` 記號與鏡／透鏡的彎曲程度跟著變 | 檔數 **B**「可調，3 檔（推薦）」；格數 C |
| 物距操作 | **畫布上直接拖物體（連續）** ＋ 左欄五顆快捷鈕（`u>2f`、`u=2f`、`f<u<2f`、`u=f`、`u<f`）。針孔頁無焦距，不適用 | **B** 選項原文「畫布拖曳＋快捷鈕（推薦）」 |
| 可拖物件 | **物體、光屏**（＋透鏡頁的遮板、針孔頁的擋板）。**觀察者眼睛不可拖**，固定在觀察側光軸稍上方 | **B**（複選：物體、光屏）＋ **B**「可拖的遮板（推薦）」 |
| 特徵光線 | **三條各自一個核取方塊**，預設全開 | **B** 選項原文「三條＋完整光束開關」 |
| 完整光束 | 另一個核取方塊：從物體頂端發出十幾條光線，全部聚到同一點 | 同上 |
| 實際光線 | **實線**＋方向箭頭 | **B**（必做項） |
| 反向延長線 | **虛線**且降透明度 | 同上 |
| 實像 | **實心箭頭**，由實線交會而成 | 同上 |
| 虛像 | **半透明空心箭頭**，由虛線交會而成 | 同上 |
| 光屏（紙屏） | 核取方塊控制顯示、**可左右拖**；離焦模糊、對焦清楚。**四頁都有**——虛像情境下屏上恆為一團散開的光，學生自己拖一輪驗證「虛像接不到」 | **A**「紙屏的部分我希望可以勾選是否顯示，拖動紙屏時能即時顯示畫面，沒有對焦時模糊，有放到正確焦距時清楚」＋ **B**「四頁都有（推薦）」 |
| 觀察者眼睛 | 核取方塊控制顯示，畫出進入眼睛的實線與腦補的虛線延長 | **B**（第一輪複選「觀察者眼睛與視線」） |
| 物體外觀 | **箭頭／蠟燭／F 三種都做，膠囊切換** | **A**「箭頭、蠟燭、F都做可以切換」 |
| 出界處理 | `u` 略大於 `f` 時像距趨近無限大。像跑出畫布 → 邊緣畫方向箭頭＋「像在更遠處」提示，格數讀數仍正確（`DESIGN_SPEC` 第 7 節要求）。像高超出畫布同理 | C |

**光屏的模糊模型（四頁共用同一套）**：彌散斑直徑 `d = A × |屏距 − 像距| ÷ 像距`，`A` ＝ 透鏡口徑或針孔孔徑。
`d` 為 0 時全清晰。虛像情境沒有實際會聚點，屏上恆為散開的光。

### 2.3 四個分頁

#### 頁 1｜針孔成像

沒有焦距、沒有特徵光線，模型是相似三角形。畫布：左為物體、中為開孔的擋板、右為可拖的紙屏。

**核心呈現（使用者親自指定畫法，勿改回連續光束）**

> **A** 使用者原話：「單一點本身會發出多道光線，孔洞小時只有一道光線能通過孔，所以像清晰；
> 孔洞大時有多道光線能通過孔(以發光點為頂點，從孔上緣到孔下緣的多道光線都能通過)，
> 因為這些光線會到達紙屏的不同位置，所以模糊」「一樣是用類似透鏡成像拖擋板的解釋方式」

落實方式：

1. 從發光點發出**一組離散光線**（十幾道），擋板把大部分擋掉——**被擋的畫到板面就停**（淡灰色），穿過孔的繼續走到屏。
2. 這些光線**在擋板平面上等間隔**（不是等角度），使「孔徑幾個間隔寬 → 幾道通過」成為可數的對應。〔C〕
3. 孔徑五檔對應通過 **1 / 3 / 5 / 7 / 9 道**；最小檔真的只有一道通過 → 屏上一個點 → 清晰而暗。〔C〕
4. **「亮」與「糊」是同一個幾何的兩個後果**：通過幾道光線，同時決定屏上佔幾個位置（糊）與有多少光到達（亮）。
5. 孔徑操作：**上下兩片擋板可直接拖**（與透鏡頁遮板同一套視覺語言），左欄另給五檔膠囊當快捷。〔C〕
6. 示範點預設**只有一個**（物體頂端），核取可再加底端那一點；「完整光束」在本頁＝加更多示範點。〔C〕

〔**修訂鏈**：草案初版寫的是「畫出該點→孔上緣與該點→孔下緣兩條**邊界**，在屏上圍出一段線段」。
使用者否決——那個畫法只呈現結果（一段糊掉的梯形），沒有呈現原因（好幾道光都擠過去了），學生數不出東西。
**舊版作廢。**〕

**左欄**：孔徑大小（膠囊 5 檔）｜孔的形狀（膠囊：圓／方／三角）｜孔的數量（膠囊：1／2／3）｜單點光束示範（核取）｜完整光束（核取）｜物體外觀｜光屏（核取，**預設開**——關掉就沒有像可看）

**迷思概念**（**B** 複選）：
- **孔的形狀不影響像的形狀** — 孔形要在**屏上的像**才看得出來（側視光路圖只表現得出孔的高度）。做法：屏上的像沿孔形輪廓取樣、半透明疊繪，圓孔糊成圓斑、方孔糊成方斑，但整幅像的形狀不變；孔要大到彌散斑大於像本身才看得出斑形。〔C〕
- **多個孔 → 多個像** — 每個孔各自放行一組光線，屏上自然出現多個完整的像。

#### 頁 2｜面鏡成像

**`.seg` 子類**：平面鏡｜凹面鏡｜凸面鏡。切換後左欄整組換（平面鏡沒有焦距，焦距膠囊與五顆快捷鈕整組消失）——符合 `DESIGN_SPEC` 第 4 節 `.seg` 的連動判準。

**左欄**：鏡種 `.seg`｜焦距三檔（平面鏡時隱藏）｜五顆快捷鈕（平面鏡時隱藏）｜三條特徵光線核取｜完整光束｜光屏｜觀察者眼睛｜物體外觀

**迷思概念**（**B** 選項原文「像就貼在鏡面上」）：鏡後畫出等距虛像＋兩條反向延長虛線，並在鏡面**兩側各標出格數**，拖物體時兩邊格數一直相等。

#### 頁 3｜透鏡成像

**`.seg` 子類**：凸透鏡｜凹透鏡

**左欄**：透鏡種類 `.seg`｜焦距三檔（透鏡形狀跟著變厚變薄）｜五顆快捷鈕｜遮板（**可拖**，從透鏡上方往下拉）｜三條特徵光線核取｜完整光束｜光屏｜觀察者眼睛｜物體外觀

**迷思概念**（**B** 選項原文「遮住透鏡一半，像會少一半」）：拖遮板時被擋的光線不畫，剩下的光束仍聚到同一點，屏上的像**逐漸變暗但形狀完全不變**。連帶效果：某條特徵光線被遮掉時像仍在，學生會發現三條線只是作圖工具。

#### 頁 4｜眼睛

**`.seg` 子類**：正常眼｜近視眼｜遠視眼

**只做一種成因**：水晶體太厚／太薄。**不做眼軸過長。**
〔**A** 使用者原話「近/遠視成因先只做水晶體太厚/薄」。此項取代了規劃過程中「兩種成因可切換對照」的中間版本，
左欄原訂的「近視成因 `.seg`」**取消**。〕

模型因此簡化成**一個水晶體焦距**在動，三層疊加〔C〕：

1. 眼睛狀態 `.seg` 決定水晶體的**基準厚度** — 近視太厚（焦距太短，像落在視網膜前）、遠視太薄（像落在視網膜後）
2. **調節作用**（看遠／看近）在基準上再加一層變化 — 正常眼兩種都能調到剛好，近視眼看遠時薄不下來（**B**）
3. **矯正鏡**（凹／凸）＋**度數可調滑桿** — 度數不足仍模糊、過頭往另一邊模糊，學生要自己調到剛好（**B**）

**左欄**：眼睛狀態 `.seg`｜調節作用（看遠／看近）｜戴上眼鏡 toggle｜矯正度數滑桿｜視覺模擬（核取）

**視覺模擬**：畫布角落一個小視窗，畫**遠處的樹＋近處的書本**，配合看遠／看近切換，用同一套模糊模型呈現清晰程度。
〔**A**「遠處的樹+近處的書」〕

**迷思概念**（**B** 選項原文「近視眼鏡是凸透鏡」）：矯正鏡形狀畫清楚（近視凹、遠視凸），拿開／戴上可即時對照像落在視網膜前還是後。

### 2.4 明確不做（使用者在選項中未選，記在此避免日後被當成遺漏）

實像要有屏才存在｜物體靠近像也靠近｜凸透鏡一定放大（生活情境標示）｜離鏡越遠像越小｜照全身鏡只要一半高｜影子本影半影｜五區間成像性質對照表｜任何數值標示｜光線逐條作圖動畫｜底部抽屜｜眼軸過長成因

### 2.5 第二輪裁決（2026-09-06，接 Claude Design 設計稿之後）

> 來源：使用者 2026-09-06 以 **17 點編號回覆**審核報告。除另標 C 者外全部是 **A 使用者原話**。
> **本節取代 2.2／2.3 中被點名的條目**；設計稿（`光的成像 設計稿.dc.html`）與這裡衝突時，以本節為準。
> 設計稿本身有多處新舊版本未對齊（詳見本節末「設計稿失效區塊」），下一個 session 不要整份照抄。

#### 已定案

| # | 裁決 | 取代了什麼 | 來源 |
|---|---|---|---|
| 2 | **實像／虛像不用顏色區分**。物體＝黑色實線框、實像＝灰色實線框、虛像＝灰色虛線框 | **作廢** F2 的 1e 與 C2 節的綠 `#2F9E63`／紫 `#8B5CF6` 外框 | **A**「顏色在光學是重要的觀察項目本身即有意義」 |
| 4 | **虛線只有三個用途**：①虛像外框＝灰虛線 ②光路向後延伸＝光的顏色的虛線 ③**法線＝黑虛線** | 新增規則。**法線是全新元素**，原稿與 2.2／2.3 皆無 | **A** |
| 5 | 物體高度**一律 2 格** | **作廢** C1 節與難題 6 的「頂端在主軸上方 2.5 格」、F 形「1.12×2.5 格」 | **A**「物體高度一律2格」 |
| 6 | 小窗放**下方留白帶** | **作廢** 難題 3 的「右上角常駐」 | **A** |
| 7 | 眼睛頁物體**一律蠟燭**；眼球改用**使用者提供的 svg** 重畫 | **作廢** 設計稿 A4 的藍箭頭說明與自繪眼球 | **A**「眼睛頁的物體我一律改成蠟燭」 |
| 7b | **眼球配色豁免硬約束 4、9** | 例外，僅限眼球剖面 | **A**「眼球為了像課本的圖，顏色違反沒關係」 |
| 8 | 光路收**上 2/3**、**下 1/3** 給小窗與必要說明 | 確認設計稿的主軸第 5 格（**作廢** 2.2「主軸固定在第 8 格」） | **A** |
| 9＋17 | 孔徑**3 檔**、**膠囊**、**用選的不拖動** | **作廢** 2.3 頁1 的「五檔」與設計稿的 `.seg` | **A**「孔徑3檔，直接選不要拖動」「孔徑用膠囊」 |
| 10 | 統一名稱**紙屏**（不再叫光屏）；**四頁都有**，眼睛頁該處叫**視網膜**、顯示蠟燭的模糊 | **作廢** 設計稿「眼睛頁無屏」 | **A**「不要叫光屏，統一名稱紙屏」 |
| 11 | 焦距 **2／3／4 格，四種尺寸一致**；直向改用**格線縮放**填滿 | **作廢** 設計稿直向的「1.5／2／3 格」 | **A**「焦距2/3/4格，直向焦距不變」 |
| 12 | **「主軸」取代「光軸」**；一律叫**「鏡心」** | 全域用詞 | **A** |
| 13 | 特徵光線定名：**①平行主軸 ②通過鏡心 ③通過焦點** | **作廢** 「射向鏡心」「通過 F」等混用 | **A** |
| 14 | 三條特徵光線**同色同粗**（`#D2700F` 100%、3px） | 確認 D 表第 8 層 | **A**「接受同色同粗」 |
| 15 | 左欄在 1366×768 就要捲動的問題，**由模型調整** | — | **A**「你幫我調整」 |
| 16 | **圖例卡放下方** | **作廢** D3 的「左下角」（四頁版面稿實測其實畫在左上 `left:14px;top:12px`） | **A** |

#### 未定案，等使用者回覆

| # | 事項 | 狀態 |
|---|---|---|
| 1 | **可拖動的視覺暗示** | 使用者否決 Design 四案（「都不大滿意」）。模型提三案待選：**甲＝光具座滑座（推薦）**／乙＝雙向箭頭圖示／丙＝夾持凹槽 |
| 3 | **模糊畫法** | 使用者否決 Design 四案。改用**真高斯模糊**（`filter: blur()`），只套在紙屏小窗（150px、拖曳時 rAF 節流）。**待 iPad 真機實測**才能定案；備案為預算幾檔模糊度的離屏快取。使用者不需提供 svg —— 造型已是單一 path |
| C1 | 物體填色是否也去掉藍色 `#2A6FDB`（貫徹「顏色留給光學」）？蠟燭的紅燭身黃焰屬實物顏色 | 待答 |
| C2 | 虛像是灰**虛**線（第 4 點）還是灰**實**線（第 2 點字面）？模型判斷第 2 點為筆誤 | 待答 |
| C3 | 像的填色濃淡（實像 `.5`／虛像 `.28`）是否保留為第三個線索 | 待答 |
| C4 | 蠟燭的「像」保留紅黃、還是轉灰階 | 待答 |
| C5 | **法線畫在哪一頁**（面鏡頁？四頁都要？） | 待答 |
| C6 | **鏡心的物理定義**：模型解讀為「透鏡＝光心、面鏡＝鏡面中心點」，等於不採用凹面鏡「通過球心原路返回」那條 | 待答 |
| 8b | **課本的圖** | 使用者稱要提供，2026-09-06 該則訊息**未附圖** |
| 7c | **眼球 svg** | 已向使用者提出六點規格（分層具 id、水晶體最薄／最厚兩個同錨點 path、視網膜獨立可取樣、朝左、總高建議 6 格、顏色照給） |
| 11b | 直向格線縮放的**連帶後果**：維持 `f=2/3/4` 則橫向需約 25 格，格邊長 ≈29.6px，**縱向由 16 格變成約 27 格**，物體 2 格高在直向只有 59px（桌機 80px）。替代方案：直向允許橫向捲動、格邊長不變 | 待確認 |

#### 本輪未回覆、仍在檯面上的審核項目

乙3 平行四邊形要不要畫厚度側面（稿頭「不畫」⟷ C3b「右側加 0.22 格側面」）｜乙4 像的外框線寬（C2 `0.1 格`≈4px ⟷ D 表 `2.8px`）｜乙8 完整光束透明度基準（C4 圖例 40% ⟷ D1／D2 28%）｜乙9 難題 8 稱「固定六個區塊」但 A1 實際 7 塊、A4 另有「矯正」｜丙3 圖例卡**沒有開關**（違反硬約束 10，使用者只指定了位置）｜丙4 A3 透鏡頁「屏面正視窗」缺開關｜丙5 膠囊內形狀圖示 `font-size:11px` 低於 12px 下限｜丁2 面鏡「鏡面兩側標格數」（2.3 頁2 的 **B 類定案**）與硬約束 6 衝突，模型提的折衷是改畫**等長標記線段**｜丁3 B1（iPad 橫向）左欄缺「完整光束」｜己4 「完整光束」預設值 A1–A3 關、A4 開｜設計稿檔案位置（現在 `optics-sim/` 根目錄，會進版控；`.gitignore` 排除的是 `design_handoff/`）。

#### 設計稿的失效區塊（下個 session 不要照抄）

- **C1 整節**（三種造型尺寸）仍是 2.5 格基準，SVG 實測 `viewBox` 光軸 y=4／頂端 y=1.5。稿頭自稱「已依 2 格重算」的範圍只有「四頁 ＋ D2 ＋ B 版」，**C 段沒更新**
- **C2 節**的綠／紫外框已被第 2 點作廢
- **難題 3** 的「右上角小窗」、**D3** 的「左下角圖例卡」均為舊值
- **A4 眼球**（行 685–769）：7 個表外色 ＋ `radialGradient` ＋ `feGaussianBlur`，與同稿 E2／難題 7 的合規描述互斥。**此段整個作廢**，改用使用者提供的 svg
- 虛像透明度稿內有三個值：C2 `.28`／難題 4 `45%`／附錄 `virtualAlpha 0.18`
- **F1／F2／F3 三組選擇題**：F2、F3 已由第 2、3 點取代；F1 待選

#### 用詞統一的枚舉範圍（2026-09-06 執行）

| 用詞 | 命中 | 處置 |
|---|---|---|
| 光軸 → 主軸 | 設計稿 19、本檔 4、`DESIGN_BRIEF` 8 | 本檔與實作全改；brief 是一次性文件不回頭改；設計稿不改 |
| 光屏 → 紙屏 | 設計稿 7、本檔 5、`DESIGN_BRIEF` 6 | 同上 |
| 射向鏡心／通過鏡心 → 通過鏡心 | 設計稿 4 | 統一 |
| 通過 F → 通過焦點 | 設計稿 3 | 統一（畫面記號仍是 `F`／`2F`） |

**`_shared/` 不動**〔C〕：共用規範中「光軸」只出現在 `DESIGN_SPEC.md:29`，是解釋兩欄配置判準的通用物理舉例（「光學的物距、像距是光軸上的一段」），不是本模擬的 UI 文字；其餘皆為「座標軸／時間軸／縱軸」。要連規範舉例一起改，需另走一次共用規範修訂。

### 2.6 第三輪裁決（2026-09-06，課本圖到位後）

> 來源：使用者 2026-09-06 第二則回覆（9 點）＋**課本透鏡成像圖一張**。全部 **A 使用者原話**，另標者除外。

#### 課本圖的量測（模型數格，**待使用者核對**）

| 項目 | 課本 | 原定案 | 處置 |
|---|---|---|---|
| 橫向格數 | **約 28 格** | 1366×768 下 24.9 格 | **待裁決（甲）**：改以「橫向固定 28 格」為基準，格邊長 `995÷28≈35.5px`，**縱向隨之變成 18 格**（原定「縱向固定 16 格」失效） |
| 縱向格數 | 約 8 格 | 16 格 | 同上 |
| 焦距 f | **5 格**（2f＝10 格） | 2／3／4 格 | **待裁決（乙）**：是否改成 3／4／5 格，讓某一檔與課本圖完全重合 |
| 物體高 | 2 格 | 2 格 | 一致 ✓ |
| 透鏡高 | 約 7 格 | 未定 | 採用 7 格〔C〕 |
| 主軸縱向位置 | 自頂端第 3.66 格（約 46%） | 上 2/3 | 相容 |
| **主軸線型** | **黑色虛線** | 實線 `#7A8496` | **待裁決（丙）**：照課本用虛線會成為第 4 種虛線用途，與 2.5 第 4 點的「虛線只有三個用途」衝突。**模型建議維持實線** |
| **F／2F 記號** | **黑色實心圓點**；只有 F 標字母，**2F 只有點不標字** | 小三角形＋字母 | **待裁決（丙）** |

#### 已定案

| # | 裁決 | 取代了什麼 | 來源 |
|---|---|---|---|
| C1 | **物體填色保留原色**（箭頭／F 形 `#2A6FDB`、蠟燭紅黃）＋黑色實線框 | 確認 2.5 第 2 點只改「框」不改「填色」 | **A**「c1物體色保留」 |
| C2 | 虛像＝**灰虛線**框 | 確認 2.5 第 2 點「實現」為筆誤 | **A**「c2虛像是灰虛線」 |
| C3 | **實像與虛像的濃淡一致**（單一 fill-opacity） | **作廢** 實像 `.5`／虛像 `.28` 的差異；區分全部交給線型 | **A**「實像和虛像的濃淡一致」 |
| C4 | 蠟燭的像＝**保留紅黃＋灰框** | — | **A**「保留紅黃加灰框」 |
| C5 | **法線只在面鏡頁**，且**只加在重要的光線上** | 縮小 2.5 第 4 點(3) 的適用範圍 | **A**「法線是面鏡頁要加，而且只加在重要的光線上」 |
| C6 | 特徵光線**照使用者順序**，且**保留圈圈數字 ①②③**（配合課本） | 左欄核取項也用 ①②③ 前綴 | **A**「要留著圈圈數字因為配合課本」 |
| 8 | **物距用詞統一**：`2f外`／`2f上`／`f~2f間`／`f上`／`f內`。做成**離散 5 段拖桿**；學生手動拖蠟燭時拖桿連動 | **作廢** 2.2 的「五顆快捷鈕」與設計稿的「遠於 2F／在 2F／F 與 2F 之間／在 F／近於 F」 | **A** |
| 9a | 平行四邊形**不畫厚度側面** | **作廢** C3b 的「右側加 0.22 格側面」（乙3 結案） | **A** |
| 9b | **圖例卡要有開關** | 補上硬約束 10 的缺口（丙3 結案） | **A** |
| 9c | **透鏡頁與面鏡頁的紙屏做開關**，打開後才出現紙屏**與小窗** | 小窗跟著紙屏開關連動（丙4 結案） | **A** |
| 9d | 「平行四邊形」「物體 2 格高」是**使用者本人在 Claude Design 對話中說的** | 進度落差結案：這兩項改標 **A**，不是模型判斷 | **A**「都是我在claude design說的」 |

**第 8 點的 UI 行為**〔C〕：五個檔位中 `2f上`、`f上` 是**單點**，其餘三個是**區間**。因此連動不對稱——拖蠟燭時拖桿顯示「落在哪一段」；拖拖桿時蠟燭**跳到該段代表位置**（區間取中點、單點取精確值），學生原本的自由位置會丟失。`f上` 時像在無限遠，走既有出界處理（邊緣箭頭＋「像在更遠處」）。

#### 待使用者回覆

| # | 問題 |
|---|---|
| 甲 | 格線基準改「橫向 28 格／縱向 18 格」？（原「縱向固定 16 格」失效） |
| 乙 | 焦距三檔是否改 **3／4／5 格**，讓某一檔與課本圖重合 |
| 丙 | 主軸用**黑虛線（照課本）**還是**實線（守虛線紀律）**；F／2F 是否改成**實心圓點、2F 不標字** |
| N1 | 法線的「**重要的光線**」是哪一條——射向鏡心那條？還是平面鏡頁全部反射光線 |
| N2 | 實／虛像統一後的濃淡值（模型建議 `.45`） |
| N3 | **針孔頁**紙屏是否仍**預設開**（原理由「關掉就沒有像可看」）；**眼睛頁**的視網膜屬眼球構造、應常駐不做開關 —— 三頁三種處理，確認 |

#### 前兩輪仍未回覆的項目（結轉）

乙4 像的外框線寬（C2 `0.1 格`≈4px ⟷ D 表 `2.8px`）｜乙8 完整光束透明度基準（40% ⟷ 28%）｜乙9 難題 8 稱「六個區塊」但實際 7 塊｜丙5 膠囊內圖示 `11px` 低於 12px 下限｜丁2 面鏡「鏡面兩側標格數」（**B 類定案**）與硬約束 6 衝突，折衷是改畫等長標記線段｜丁3 B1 左欄缺「完整光束」｜己4 「完整光束」預設值四頁不一致｜第 1 點**可拖動視覺暗示**三案未選（甲光具座滑座／乙雙向箭頭／丙夾持凹槽）｜設計稿檔案位置（會進版控）｜**眼球 svg 尚未提供**。

### 2.7 第四輪裁決與座標系統驗算（2026-09-06）

> 來源：使用者第三則回覆（5 點）＋ 一次 fresh-context agent 獨立驗算（主對話已逐項親核）。

#### 課本圖的正確數字（**使用者更正，取代 2.6 的模型量測**）

| 項目 | 正確值 | 模型原本數錯成 |
|---|---|---|
| 橫向格數 | **22 格** | 28 格 |
| 縱向格數 | **6 格** | 8 格 |
| 焦距 | **3 格**（＝三檔中的「中」檔） | 5 格 |
| 透鏡高 | **6 格** | 7 格 |

〔**A** 使用者原話「你算錯了，寬22格、高6格，焦距3格，透鏡高6格」。**2.6 節那張量測表全部作廢**。〕

#### 已定案

| # | 裁決 | 來源 |
|---|---|---|
| 焦距 | 三檔 **4／3／2 格** 維持不變；課本那張圖 ＝ 中檔（3 格） | **A** |
| 主軸線型 | **實線**（不照課本的虛線），守住「虛線只有三個用途」 | **A**「丙建議b」 |
| F／2F 記號 | 改用**實心圓點**標在主軸上，且 **F 與 2F 都標字母**（課本只標 F，此處與課本不同） | **A**「用圓點標、F、2F都標」 |
| 法線 | 面鏡頁的**三條特徵光線都畫**法線（①平行主軸 ②通過鏡心 ③通過焦點） | **A**「畫在平行主軸、通過鏡心、通過焦點」 |
| 針孔頁紙屏 | **預設開、不加開關**。豁免理由〔C〕：針孔頁的紙屏是**主要器材**而非教學輔助疊層（關掉就沒有像可看），不受硬約束 10 管轄 | **A**「針孔預設開且不加開關」 |

**待使用者回覆**：面鏡在鏡心處的**法線與主軸重合**（鏡面頂點的法線就是主軸），「通過鏡心」那條光線的法線會疊在主軸上等於看不見 —— (a) 該條不畫法線／(b) 照畫並在圖例註明。模型傾向 (b)，這反而是教學點。

#### 座標系統驗算（fresh-context agent 算、主對話親核）

三種尺寸（橫向固定 22 格、格子正方形、光路收上 2/3、主軸在上 2/3 區縱向中間）：

| 尺寸 | 繪圖區 | 格邊長 | 橫向 | 縱向 | 主軸在第幾格 | 像高上限 |
|---|---|---|---|---|---|---|
| 1366×768 | 995×638 | 45.23px | 22 | 14.11 | 4.70 | 4.70 格 |
| 1024×768 | 738×638 | 33.55px | 22 | 19.02 | 6.34 | 6.34 格 |
| 768×1024 | 740×814 | 33.64px | 22 | 24.20 | 8.07 | 8.07 格 |

**結論：座標系統本身自洽（格子正方、主軸位置、透鏡 6 格三種尺寸都放得下），但橫向 22 格不夠用。**

三個獨立缺口〔已親核〕：
1. **物體本身就出界**——半寬 11 格，但既有定案「物體最遠可拖到 12 格」
2. 放大實像出界：f=3 `u=4`→v=12 格｜f=4 `u=5`→**v=20 格**（超出 82%）｜f=4 `u=6`→v=12 格
3. 虛像出界：f=4 `u=3`→左側 12 格、像高 8 格

縱向出界（1366×768 門檻 4.70 格）：f=3 `u=2`、f=3 `u=4`、f=4 `u=3`、f=4 `u=5` 四筆。
**1366×768 是最吃緊的尺寸**，也是唯一連 f=3 都會縱向出界的。

#### 建議（待使用者裁決）

**橫向改 26 格**〔C，模型建議〕。使用者要求是「**至少**符合課本」，26 ≥ 22 仍成立：

- 格邊長 `995÷26 = 38.3px`，半寬 13 格 → **物體 12 格有 1 格餘裕**
- 縱向 16.67 格，像高門檻由 4.70 提升到 **5.56 格**
- **2.6 第 8 點五段拖桿的五個代表位置，三檔焦距下全部畫得完整**（最吃緊者為 f=4「f~2f間」取中點 u=6 → v=12 格、像高 4 格）
- 物體 2 格高 ＝ 76.6px

不建議 40 格（雖能免除多數出界，但格邊長掉到 24.9px、物體只剩 49.8px，教室大屏數不出格）。

**出界處理是常態功能，不是備案**〔已驗算〕：連續拖曳時 `u→f⁺` 使 `v→∞`，**任何有限格數都無法全覆蓋**。2.2 節既有的「邊緣方向箭頭＋『像在更遠處』提示、格數讀數仍正確」必須在階段 1 就實作到位，不得延後。這同時也是「焦點上不成像」的教學演示。

### 2.8 第五輪裁決與成像數據表（2026-09-06）

> 數據表經**兩次獨立計算**（主對話一次、fresh-context agent 一次）交叉比對，25 個組合全部一致。

#### 已定案

| 項目 | 裁決 | 來源 |
|---|---|---|
| 法線與主軸重合 | **(b) 照畫**，三條特徵光線的法線都畫，包含「通過鏡心」那條（它與主軸重合），並在圖例註明 | **A**「選B」 |
| 可拖動的視覺暗示 | **雙向箭頭圖示**（`fa-arrows-left-right`；遮板用 `fa-arrows-up-down`），灰色，貼器材足端 | **A**「視覺提示用雙向箭頭」 |
| 像的濃淡 | 實像與虛像**統一 `fill-opacity: .45`** | **A**「濃淡值先依你建議」，模型建議值 |
| 像的外框線寬 | **0.1 格**（≈4px @格邊長 40px）。**作廢** D 表的 `2.8px`（乙4 結案） | **A**「先用0.1格」，標記為**暫定** |
| 完整光束透明度 | **40%**。**作廢** D1／D2 的「開啟時降到 28%」（乙8 結案） | **A**「透明度先40%」，標記為**暫定** |

〔**2026-09-06 更正圖示名**：「可拖動的視覺暗示」那一列原本寫遮板用 `fa-arrow-up-down`，
**那個名稱在 Font Awesome 6 不存在**（方向類雙向箭頭一律是複數 `arrows`）。錯名不會報錯、
只會靜默渲染成 0 寬，階段 2 照抄後畫面上根本沒有提示，是量 `getBoundingClientRect().width` 才發現的。
已改為 `fa-arrows-up-down` 並實測 `w=12.5`。通用教訓已寫進 `handbook\pitfalls-frontend.md` 第 2 條。〕

#### 成像數據表（五檔拖桿 × 五種焦距）

**通式**：令 `u = k·f`，則 `v = kf/(k−1)`、`m = 1/(k−1)`、**像高 `= 2/|k−1|`**。
**關鍵性質：像高只與 k 有關，與焦距完全無關**（f 在推導中約掉）；但 `v` 與 f 成正比 —— **所以出界問題出在像距，不在像高**。

代表位置取值：`2f外`=3f｜`2f上`=2f｜`f~2f間`=1.5f｜`f上`=f｜`f內`=0.5f

| f | 2f外 (3f) | 2f上 (2f) | f~2f間 (1.5f) | f上 (f) | f內 (0.5f) |
|---|---|---|---|---|---|
| 2.0 | u6 v3 h1 | u4 v4 h2 | u3 v6 h4 | **奇異點 v→∞** | u1 v−2 h4 |
| 2.5 | u7.5 v3.75 h1 | u5 v5 h2 | u3.75 v7.5 h4 | **奇異點** | u1.25 v−2.5 h4 |
| 3.0 | u9 v4.5 h1 | u6 v6 h2 | u4.5 v9 h4 | **奇異點** | u1.5 v−3 h4 |
| 3.5 | u10.5 v5.25 h1 | u7 v7 h2 | u5.25 v10.5 h4 | **奇異點** | u1.75 v−3.5 h4 |
| 4.0 | u12 v6 h1 | u8 v8 h2 | u6 v12 h4 | **奇異點** | u2 v−4 h4 |

v 為正＝實像倒立；`f內` v 為負＝虛像正立（|v| = f）；`f上` 出射光平行、不成像。

#### 出界判定

**縱向：25 組全部安全。** 有限像高只有 1／2／4 格三種值，最大 4 格 < 1366×768 的上限 4.70 格。
（2.7 節算出的「像高 6 格、8 格」只在**任意連續拖曳**時出現，五檔拖桿碰不到。）

**橫向是唯一瓶頸**（透鏡在正中，半寬 ＝ 格數 ÷ 2）。含物體／像的寬度後（物半寬 0.6 格、像半寬 `0.6|m|`），最遠邊緣通式為 `f~2f間` 的像外緣 `3f + 1.2`：

| 橫向格數 | 半寬 | 出界組合（含寬度） |
|---|---|---|
| **22**（課本） | 11 | f=3.5：2f外 11.10、f~2f間 11.70｜f=4：2f外 12.60、f~2f間 13.20 |
| **26** | 13 | f=4 的 f~2f間 13.20（**差 0.20 格**） |
| **28** | 14 | 無（最大 13.20） |

**`f上` 五種焦距全部恆定出界**（v→∞），加寬無解，必須用「平行光射出畫面、不成像」的畫法處理。

#### 三個方案（**待使用者裁決**）

| 案 | 內容 | 代價 |
|---|---|---|
| **甲**（模型推薦） | **22 格不變**，代表值改 `2f外 = 2.5f`、`f~2f間 = 1.7f` | 「f~2f間」放大率從 2.0 降到 1.43，放大感稍弱 |
| 乙 | **28 格**，代表值維持中點 | 格子比課本密 27%，物體 2 格高剩 71px |
| 丙 | 22 格 ＋ 維持中點 | f=4 的兩個位置常態出界，靠邊緣箭頭提示 |

**甲案的兩個代表值必須同時改，缺一不可**〔已驗算〕：只改 `2f外` → `f~2f間` 仍 13.20；只改 `f~2f間` → `2f外` 仍 12.60。
精確臨界：`2f外` 係數 **≤ 2.60**、`f~2f間` 係數 **≥ 1.657**（1.66f 僅剩 0.03 格餘裕，不可用；取 1.7f）。

**橫向格數必須是偶數**〔agent 提出，主對話採納〕：透鏡固定在橫向正中，奇數格會讓它落在**格子中央**而非格線上，學生從透鏡起算就得數半格。所以乙案是 28 格而不是 26.4 取整的 27 格。

#### 焦距檔數的連帶問題（**待使用者回覆**）

使用者要求計算的是 **5 種焦距**，但現行定案是**三檔（4／3／2）**。若焦距改五檔（2／2.5／3／3.5／4）：
**f=3.5 與 f=4 兩檔都需要調代表值**（f=3.5 含寬度後 2f外 11.10、f~2f間 11.70，皆超過半寬 11）。
規範面無礙 —— `DESIGN_SPEC` 第 4 節「物理參數滑桿一律離散檔位 3–7 檔」，五檔仍在範圍內。

### 2.9 座標系統定案（2026-09-06）

> **這一節是階段 1 的地基，數字已定，不要再改動。** 取代 2.8 的「三個方案」。

#### 定案

| 項目 | 值 | 來源 |
|---|---|---|
| **橫向格數** | **22 格**（＝課本），必須是偶數（透鏡壓在格線上） | **A**「一樣用課本的」 |
| **焦距檔數** | **三檔：2／3／4 格**（中檔 3 格 ＝ 課本那張圖） | **A**「三檔」 |
| 採用方案 | **甲案**：22 格不變，改代表值 | **A**「用22格改代表值」 |
| `f~2f間` 代表值 | **1.75f** | **A**「課本蠟燭F~2F間的物距大約就是1.75F」 |
| `f內` 代表值 | **0.375f** | **A**「F內的數字大約是0.375F」 |
| `2f外` 代表值 | **2.5f**〔C，暫定〕 | 見下「未定」 |

#### 五檔 × 三檔焦距的完整數據

通式 `u = k·f` → `v = kf/(k−1)`、`m = 1/(k−1)`、像高 `= 2/|k−1|`
k 值：`2f外`=2.5｜`2f上`=2｜`f~2f間`=**1.75**｜`f上`=1｜`f內`=**0.375**

| f | 2f外 | 2f上 | f~2f間 | f上 | f內 |
|---|---|---|---|---|---|
| **2** | u5.0 v3.33 h1.33 | u4 v4 h2 | u3.50 v4.67 h2.67 | 奇異點 | u0.75 v−1.20 h3.2 |
| **3** | u7.5 v5.00 h1.33 | u6 v6 h2 | u5.25 v7.00 h2.67 | 奇異點 | u1.125 v−1.80 h3.2 |
| **4** | u10.0 v6.67 h1.33 | u8 v8 h2 | u7.00 v9.33 h2.67 | 奇異點 | u1.50 v−2.40 h3.2 |

**出界檢查（22 格、半寬 11、含寬度）**：最遠邊緣為 f=4「2f外」的物體外緣 **10.6 格**，餘裕 0.4 格 ✓
**縱向**：最大像高 **3.2 格** ≪ 上限 4.70 格 ✓
使用者給的兩個課本值都優於模型原建議（1.7f／0.5f）：`1.75f` 像距更短、`0.375f` 像高由 4 格降為 3.2 格。

**`f上` 仍恆定無解**（v→∞），必須用「平行光射出畫面、不成像」的畫法處理，與格數無關。

#### 未定

1. **`2f外` 的代表值**：課本那張圖是 **3.33f**（蠟燭距鏡心約 10 格、f=3），但 f=4 檔會變 u=13.3 格、超出半寬 11。**數學上限 2.6f**，暫用 2.5f。若使用者另有「2F 外」情境的課本圖比例，重算。
2. **f=2 檔 ＋ `f內` 的物體與透鏡重疊**〔模型發現，待解〕：`f=2, k=0.375` → u = **0.75 格**（34px）。若蠟燭半寬 0.6 格、透鏡半厚約 0.56 格，蠟燭右緣落在 0.15 格 —— **插進透鏡內部**。取決於兩個未定值：**蠟燭寬度**與**透鏡厚度**。使用者將提供蠟燭 SVG，屆時一併定案。緩解方向：蠟燭半寬收到 **0.4 格以內**。

#### 蠟燭 SVG 的規格（已向使用者提出）

使用者反映預設蠟燭畫法「有點醜」，將提供 SVG。要求：① 分層（燭身／火焰／燭芯各自 path 具 id）② **額外提供一條整體外輪廓 path**——像的灰色外框沿它畫，否則只能退化成外接矩形 ③ 總高 2 格、足端貼主軸、**半寬 ≤0.4 格**（為解上述重疊問題）④ 座標建議 `viewBox="-0.5 -2 1 2"`，或註明 1 格 = 幾 px ⑤ 現有蠟燭用色皆在定案表內，若改色需比照眼球另立豁免 ⑥ 不用漸層／濾鏡。
箭頭與 F 形是否也由使用者提供，待回覆。

### 2.10 使用者素材驗收（2026-09-06）

使用者提供 `optics-sim/蠟燭.svg`（8.3 KB）與 `optics-sim/眼球.svg`（24.9 KB），皆為 Inkscape 匯出。**已驗收通過，可直接使用。**

#### 蠟燭.svg

`viewBox="0 0 92.802659 300.66766"`，**高寬比 3.24** → 總高 2 格時**半寬 0.312 格**（符合 ≤0.4 格要求）。
5 條 path，全部純扁平填色、無 stroke、無漸層濾鏡：

| id | 色 | 角色 |
|---|---|---|
| `path1094` | `#fcd267` | **整體外輪廓**——單一封閉路徑（1 subpath、1 z、182 個數字）。**像的灰色外框沿這條畫** |
| `path1093` | `#fcc01a` | 燭身主體＋燭蠟面（2 subpath） |
| `path1092` | `#fcb31c` | 火焰外層 |
| `path1091` | `#fa7014` | 火焰內層 |
| `path1090` | `#5f3624` | 燭芯 |

焰尖＝物體頂端，三條特徵光線從此射出（沿用 2.2 的定義）。

#### 眼球.svg

`viewBox="0 0 596.59338 469.08084"`（Inkscape 標 mm，實作以 viewBox 縮放即可），寬高比 1.272。**朝左**（角膜在左、視神經在右下），符合四頁光線方向一致的要求。

| id | 色 | 角色 |
|---|---|---|
| `path49` | `#fcd4c2` | 玻璃體 |
| `path48` | `#eac48f` | 鞏膜＋角膜（**同一條 path，分不開**） |
| `path47` | `#b34846` | 脈絡膜／視網膜（含睫狀小帶細線） |
| **`path46`** | `#b3d3ef` | **水晶體（厚）** bbox w=67.55 |
| **`path50`** | `#b3d3ef` | **水晶體（薄）** bbox w=46.63 |

**水晶體可連續變形（關鍵驗收項，通過）**：兩條 path 各 **182 個數字、30 段貝茲曲線**，起點 y 相同（`-36.899467`）、垂直範圍相同（158.05），**只有寬度不同**（厚薄比 1.45）。錨點數與順序一致 → **可逐點插值做連續 morph**，不是兩張圖切換。
注意：兩者水平中心差 **6.11 單位**（≈3px，手繪誤差），morph 時需**對齊中心**〔C〕。

缺少但不影響〔C〕：角膜無獨立 path（國中不講角膜折射）｜無虹膜／瞳孔｜視網膜與脈絡膜合併（模糊斑改用「主軸與眼球後壁的交點」幾何計算定位，不從 path 取樣）。

#### 由素材產生的三個待決事項

1. **蠟燭與光線幾乎同色** ⚠️：火焰 `#fa7014`／`#fcb31c` vs 規範光線色 `#D2700F`，三條特徵光線的**起點會與火焰糊在一起**。方案 (a) 光線靠近物體那一段加**白色外描邊**（模型建議，代價最小）／(b) 接受視覺連續／(c) 火焰改色。**待使用者裁決**。
2. **蠟燭配色需要豁免**：5 色全部不在 `DESIGN_SPEC` 定案表內。眼球已於 2.6 節取得豁免（**A**「眼球為了像課本的圖，顏色違反沒關係」），**蠟燭尚未**。待使用者裁決。
3. **透鏡厚度上限定為 0.85 格**〔C，暫定〕：蠟燭半寬 0.312 格，`f=2` 搭 `f內=0.375f`（u=0.75 格）時蠟燭右緣在 **0.44 格**，透鏡半厚須 < 0.44 格（即厚度 < 0.88 格）才不重疊。課本圖的透鏡約 1.1 格厚、略超標。此項解決 2.9 節「未定 2」。

**版控**：這兩個 svg 是**產品素材**（與設計稿性質不同），應進版控。設計稿是否移入 `design_handoff/` 待使用者回覆。
箭頭與 F 形是否也由使用者提供，待回覆。

### 2.11 素材相關裁決（2026-09-06，結案 2.10 的待決事項）

| 項目 | 裁決 | 來源 |
|---|---|---|
| 蠟燭配色 | **豁免** `DESIGN_SPEC` 色票限制，照使用者的 svg 用色（`#fcd267`／`#fcc01a`／`#fcb31c`／`#fa7014`／`#5f3624`）。與眼球同性質的例外 | **A**「蠟燭豁免」 |
| 光線與火焰同色 | **光線加白色外描邊**（`paint-order: stroke`），讓三條特徵光線的起點與火焰分離 | **A**「光線加白外描邊」 |
| 透鏡厚度 | **最厚 0.85 格**（半厚 0.425 格）。確保 `f=2` 搭 `f內=0.375f`（u=0.75 格、蠟燭右緣 0.44 格）時蠟燭不會插進透鏡 | **A**「透鏡最厚0.85格」 |
| 箭頭與 F 形 | **由模型繪製**，不另外提供 svg。須與蠟燭共用同一組基準：總高 2 格、足端貼主軸、頂端為特徵光線起點、半寬 ≤0.4 格 | **A**「箭頭和f你來畫」 |

**至此 2.9「未定 2」（f=2 檔物體與透鏡重疊）與 2.10 的三個待決事項全部結案。**

#### 素材與設計稿的版控處置

`蠟燭.svg`、`眼球.svg`、`光的成像 設計稿.dc.html` 一併納入版控〔C〕。
設計稿**不移入** `design_handoff/`，理由：`PROJECT_PLAN` §2.5–§2.10 多處以**行號**引用它（如 C1 在 1027、A4 眼球在 685–769、虛像三個透明度值在 1066／1577／1603），檔案若被 `.gitignore` 排除，這些引用在下一個 session 就查不到；且 §2.5 末已標明哪些區塊失效，不會被誤用。

### 2.12 階段 1 實作定案（2026-09-06，頁 3 凸透鏡完成）

| 項目 | 定案 | 來源 |
|---|---|---|
| 畫布渲染技術 | **inline SVG**，不用 canvas。理由：白色外描邊要 `paint-order`、紙屏小窗要 `filter:blur()`，兩者都是 SVG／CSS 才有的能力，canvas 得另做離屏快取（`DEV_SPEC` 6.4 禁止每幀 filter）；且本模擬無動畫、無 rAF 迴圈 | **B**，選項原文「inline SVG（推薦）」 |
| 開發期的分頁露出 | 四頁全露、未完成標「開發中」，**部署前隱藏** | **B**，選項原文「四頁都露、標開發中，部署前再隱藏（推薦）」 |
| 左欄捲動（解 §2.5 第 15 點） | 七個核取改**兩欄排列**、移除物距拖桿的兩端標籤（與 head 的段名重複）、卡片 padding 14→12、區塊 gap 8→6。核取列高 44 是命中區下限**沒有動** | C（使用者授權「你幫我調整」） |
| 透鏡厚度 | f=2／3／4 → **0.85／0.65／0.45 格** | C，暫定 |
| F 形半寬 | **0.3 格**（原訂 0.35 會在 f=2 搭「f 內」插進透鏡） | C |
| 箭頭半寬 | 0.28 格 | C |
| 完整光束預設值 | **關**（`DESIGN_SPEC` 第 4 節「教學輔助疊層一律預設關」，順帶結掉 §2.6 結轉的「己4」） | C |
| 預設物距檔位 | **f~2f 間**（放大倒立實像，`DEV_SPEC` 9.4「一開機就看得到核心命題」） | C |
| 紙屏開啟位置 | 打開時自動放到**對焦處**；虛像／不成像時放 9 格 | C |
| 觀察者眼睛位置 | 鏡心右 8.5 格、主軸上 1.6 格，不可拖 | C |
| 像的外框色值 | 灰 `#67717F`（4.95:1）。實像實線／虛像虛線，寬 0.1 格 | C（§2.5 第 2 點只寫「灰色」未給值） |
| **切焦距時的行為** | **物距不隨焦距改變**。只重新判定 u 落在哪一段（拖桿跟著移、段名跟著換），物體本身不動 | **A**「切換焦距的時候，物距不要隨著調整」（2026-09-06） |
| 切焦距時的**紙屏** | 一併固定在原處（未手動拖過時，切焦距前先把當下位置固化）。教學效果：換一個透鏡，原本對焦的屏就糊了——實測 f=3 對焦 blur 0 → f=4 變 27px → f=2 變 54px | C（模型延伸判斷，使用者只指定物距） |
| **物距拖桿刻度** | 五格刻度線，「2f 上」「f 上」兩格標 **2F／F**。刻度**絕對定位疊在滑桿自己的 44px 命中區內**（軌道中心 22px，刻度 26–30、字 30–42），不佔額外高度——左欄在大屏只剩 13.7px 餘裕，多一列就會又開始捲動。`left` 用 `calc(12px + (100% − 24px) × n/4)` 對齊 thumb 中心，不是等分 0–100%（兩端各被 thumb 半徑 12px 內縮） | **A**「物距的滑桿可以畫刻度，並且標F、2F」（2026-09-06） |
| **③「通過焦點」預設值** | **預設關**（①② 仍預設開）。三處（HTML `checked`、`initState`、重置還原值）已一併改，DESIGN_SPEC 第 4 節。**取代 §2.2 的「三條各自一個核取方塊，預設全開」** | **A**「預設不顯示通過焦點的光」（2026-09-06） |
| **新增物體選項「平行光」** | 物體膠囊第四顆。選它＝**物在無限遠**：5 條平行主軸的光射入，出射後全部通過焦點——焦點的定義本身。模型上等價於 `v = f、m = 0`，所以紙屏的彌散斑公式原封不動就能用（屏放在 F 上光斑收成一點，拖開就散）。此模式**沒有物體也沒有像**；物距與三條特徵光線**停用**（`.group.off` 半透明，不是隱藏——隱藏會讓左欄高度跳動），物距讀數改寫「**物在無限遠**」 | **A**「物體的部分幫我多做一個平行光」（2026-09-06） |
| **紙屏小窗高度** | **佔滿整條留白帶**（1366×768 下 188px，內容區 164px）。`top` 由 JS 依 `OPTICS_FRAC` 設定，不在 CSS 寫第二個 66.667%。內部尺度固定為「小窗高 ＝ 10 格」，模糊半徑同尺度換算，之後改窗高不必重調常數 | **A**「底下紙屏小窗的高度我希望可以佔滿空白區」（2026-09-06） |

**物體選擇器維持「膠囊」而沒有改成 `.seg`**〔C，待使用者確認〕：`DESIGN_SPEC` 第 4 節的判準是
「選了它之後，畫面上其他的控制項會跟著改變 → 用 `.seg`」，而「平行光」確實會把物距與特徵光線停用。
維持膠囊的理由有二：① 四個選項中只有一個會連動，其餘三個（蠟燭／箭頭／F）互切時旁邊完全不動
② 停用不等於「整組內容更換」——左欄的項目、順序、高度全部沒變。
且 §2.3 的「物體外觀⋯膠囊切換」是 **A 類定案**，改元件等於推翻它。
若使用者認為該照判準改用 `.seg`，換法是把四顆膠囊整組換成 `.seg` 四格（容器總高 44，其餘規格照第 4 節）。

**實作時踩到並修掉的三個坑**（下一階段沿用同一套繪製程式碼，不要重蹈）：

1. **`<use href="#某個子元素">` 不會套用該元素父層 `<g>` 的 transform**——只取被引用的元素本身。
   蠟燭外框因此整個偏移 (276, 37)，畫面上每個蠟燭右邊都多一個輪廓。
   解法：另包一層帶同樣 transform 的 `#cdOut`，並把 `fill` 從 path1094 移到外層 g 才能覆寫成 `none`。
2. **`marker-end` 只認整條 path 的最後一個頂點**：把多段光線合併成一條 path，就只有最後一段長得出箭頭。
   解法：橘線逐段各自一個 `<path>`，marker 掛在群組上靠繼承套用（白色外描邊層可以合併，它不需要箭頭）。
3. **完整光束在虛像時出射方向會反**（射向畫布左緣）：虛像的像點在透鏡左側，但實際光線是往右發散、
   只是看起來像從那一點射出，方向必須取反。三條特徵光線沒有這個問題，只有光束的通用式會踩到。

**驗證基準**（下次改動後要重跑）：§2.9 的 15 組數據（3 焦距 × 5 檔位）已全部逐格核對相符；
光線③出射的水平高度與像頂高度差為 **0**（三條特徵光線精確交於像點）。

### 2.13 階段 2 實作定案（2026-09-06，頁 3 凹透鏡＋可拖遮板）

| 項目 | 定案 | 來源 |
|---|---|---|
| 凹透鏡的模型 | **焦距取負號，薄透鏡公式一個字都不改**。`state.f` 一律存絕對值（五檔物距、F／2F 記號、透鏡厚度都用它），只有代進成像公式與算光線斜率時經 `signedF()` 取號 | C |
| 透鏡種類的元件 | `.seg` 兩格（凸透鏡／凹透鏡）。容器總高 44 ＝ 1+3+36+3+1 | §2.3 既有定案 |
| 第三條特徵光線的名稱 | 凸「③ 通過焦點」／凹「③ 射向焦點」。左欄核取的文字與圖例卡讀同一份 `RAY_NAMES`，兩處不會分岔 | C |
| 三條光線的式子 | **兩種透鏡完全共用**：① 的出射線一定通過 `(cx + f·g, ax)`、③ 的入射線一定射向 `(cx − f·g, ax)`，f 取號之後同一式子就換了意義（凸＝真的通過，凹＝反向延長線才到得了 / 朝著它去但被折開） | C，已驗算 |
| ② 的反向延長線 | **只有「虛像比物體更外側」才畫**（凸透鏡當放大鏡）。凹透鏡的虛像落在物與鏡之間，那一段本來就是實際光線走過的路，再畫一條虛線會疊在實線上 | C |
| 凹透鏡外形 | 中央固定薄 `LENS_TC = 0.18` 格、鏡緣厚 `0.18 + LENS_T[f]`。**與凸透鏡共用同一張曲率表 `LENS_T`**，「焦距越短鏡面越彎」只有一份定義 | C |
| 五檔物距在凹透鏡的意義 | **沿用 `u = k·|f|` 與同一組段名**（2f 外／2f 上／f~2f 間／f 上／f 內）。凹透鏡不論物距落在哪一段都是正立縮小虛像——**「五段全都一樣」正是凹透鏡的教學重點**，不是段名失去意義 | C |
| 凹透鏡的最近物距 | `uRange` 取**鏡緣**半厚（最保守），所以 f=2 搭「f 內」是 **0.827 格**而不是凸透鏡的 0.75。物體只有 2 格高、那個高度上的鏡面其實還很薄（實測約 0.279 格），寧可留白也不要讓蠟燭插進鏡片 | C |
| 遮板的操作方式 | **左欄「顯示」多一個核取「遮板」，與紙屏一樣預設關**；勾選後板子出現在透鏡上方的停放位置（只露 0.35 格板緣），抓著往下拉。取消勾選＝整片移出光路，不畫也不擋光；重新勾選一律從 0 重新開始，不留殘留深度 | **A**「遮擋板做在旁邊可以選擇開關(和紙屏一樣，預設關)」 |
| 遮板的拖曳提示 | `fa-arrows-up-down`，灰色，貼板子下緣左側 13px。**停在最上面時它是唯一告訴學生「可以往下拉」的東西**，所以必須量出寬度 >0 才算做好 | **A**「遮擋版要有能上下拖動的視覺提示」＋ §2.8 |
| 核取的排列順序 | 完整光束 → **遮板** → 紙屏 → 觀察者眼睛 → 圖例卡，照光行進的順序（光 → 透鏡處的遮板 → 像處的紙屏 → 眼睛） | C |
| 被遮住的光線 | **整條不畫**（§2.3 明文「被擋的光線不畫」），並在圖例卡標「（被遮板擋住）」。線為什麼不見了必須看得到（`DEV_SPEC` 5.2 狀態可見） | §2.3 ＋ C |
| 只有打在透鏡上的光線會被擋 | 三條特徵光線是**作圖工具**，落在鏡緣以外時本來就不受口徑限制，不能因為板身畫過去就當它被擋住。`isBlocked()` 因此夾在 `[透鏡上緣, 遮板下緣)` 之間 | C |
| 遮板造成的變暗 | 亮度倍率 ＝ 透光比例的 **0.45 次方**，不是線性。線性在遮掉七成時像就幾乎看不見了，學生反而看不到「形狀完全沒有變」——這一頁要證的是形狀不變，不是亮度精確 | C |
| 全遮（6 格） | 像完全不畫 ＋ 畫布上標「透鏡被完全遮住，沒有光通過」＋ 圖例最後一列改寫 | C |
| 切透鏡種類時的行為 | **物距不動、紙屏一併固定在原處**，比照 §2.12 切焦距的 **A** 類裁示「切換焦距的時候，物距不要隨著調整」。兩者共用 `freezePaper()` | C（延伸自 A） |

**連帶修訂了階段 1 的一個行為**〔C，**要保留舊行為請告訴我**〕：`blurOf()` 原本 `!im.real` 直接回 `null`，
屏上永遠畫同一團固定大小的光。凹透鏡**只有**虛像，那樣拖屏完全沒有回饋，學生驗證不到「越拉遠越散」。
改成 `口徑 × |屏距 − 像距| ÷ |像距|`（取絕對值之後實像、虛像、平行光三種情形通用），屏拉得越遠光斑越大越淡。
**受影響的是凸透鏡 `u<f`（放大鏡）那一檔**的紙屏側視與紙屏小窗，凸透鏡其餘四檔的實像表現完全沒動。

**驗算（斷言全部通過，腳本在 session scratchpad，未入版控）**：

- 凸透鏡 15 組（3 焦距 × 5 檔位）與 §2.9 的表**逐格相符** → 無回歸
- 凹透鏡 15 組全部滿足 `v < 0`（虛像）、`0 < m < 1`（正立縮小）、`|v| < |f|`、像落在物與鏡之間
- 光線③ 打在透鏡上的高度與像頂高度差 **< 1e-9**（兩種透鏡都成立，沿用 §2.12 的驗證基準）
- 瀏覽器實測：遮板拉到 2.0 格 → 只有①被擋、像透明度 0.81（＝(4/6)^0.45）；拉到 6.5 格被夾在 6 格 → 像消失、提示字出現；重置全部還原

#### 版面：左欄開始捲動（**已裁決：接受捲動**）

加了「透鏡種類」那一組（`.seg` 44 ＋ 標籤 ＋ 兩個 gap，1366×768 實測佔 **74～81px**）之後，
§2.12 好不容易解掉的「左欄不捲動」被吃回去了。1366×768 實測餘裕：

| 尺度 | §2.12（階段 1） | 現在 |
|---|---|---|
| 桌機 `--u=1` | ＋58px | **−17px（捲動）** |
| 平板 `--u=1.15` | ＋41px | **−35px（捲動）** |
| 1920×1080 `--u=1.4` | — | ＋0px（剛好不捲） |

視窗越高餘裕越大，所以吃緊的是 **1366×768 的筆電**，不是大屏。三個選項：

1. **接受捲動**——`.panel` 本來就有 `overflow-y:auto`，功能不受影響，只是回到階段 1 之前的狀態。
2. **把「重置這一頁」移到 topbar 的圖示鈕，整張動作卡拿掉**——實測動作卡連 gap 佔 **82px**，
   拿掉之後三個尺度全部回到不捲動（桌機 ＋65、平板 ＋47）。**但這會推翻 §2.1 的 A 類定案**
   〔**A** 使用者原話「原本抽屜放的按鈕移到左欄」〕，所以不能由模型自己決定。
3. **壓縮間距**（卡片 padding 12→10、區塊 gap 6→5 之類）——實測只能省個位數 px，救不了 −35。

**裁決結果**：**B**，選項原文「**接受捲動**」（2026-09-06）。`.panel` 本來就有 `overflow-y:auto`，
功能不受影響，只是回到階段 1 解掉這個問題之前的狀態；動作卡與 §2.1 的 A 類定案**維持不動**。

**同一次裁示又加了遮板核取**（見上表），所以「顯示」那組從 4 個變 5 個、從 2 列變 3 列，
1366×768 桌機的餘裕從 −17 再降到 **−61px**。這是裁決當下就知道的連帶代價，不是後來才發現的問題。
**日後若要回頭消掉捲動**，額度最大的仍是動作卡那 82px（要先推翻 §2.1 的 A 類定案）。

### 2.14 階段 3＋4 實作定案（2026-09-07，頁 2 面鏡三種＋頁 1 針孔）

> 使用者指示「直接把階段3、階段4做完」，兩階段併為一次交付。

#### 架構：四頁共用一張畫布

| 項目 | 定案 | 來源 |
|---|---|---|
| 畫布 | **四頁共用同一個 `<svg id="board">`**，不做四張。格線、主軸、物體、紙屏、觀察者眼睛、圖例卡、拖曳提示四頁完全一樣，分開做會讓這些共用元素長出四份實作 | C |
| 尚未實作的頁 | 用 `.screen` 蓋板疊在畫布上（`z-index:2` ＋ 不透明底），左欄的控制項同時整組收起來——留著一排按不出效果的控制項會讓人以為壞了 | C |
| 左欄 | **分頁專屬組（`.pagectl`）＋ 共用組**。共用的是焦距／物距／物體／特徵光線／顯示；專屬的是孔徑三組（頁 0）、鏡種（頁 1）、透鏡種類（頁 2） | C |
| 換頁時 | 紙屏位置回到該頁預設（不把上一頁的座標帶過來）、重算可拖範圍、重寫特徵光線名稱。**物體造型、焦距、核取狀態刻意保留**，換頁比較同一個情境才不用重設一次 | C |
| 「重置這一頁」 | 只還原設定，**留在原本那一頁**——重置不該把學生踢回別的分頁 | C |

#### 面鏡（頁 2）：與透鏡共用同一支成像引擎

| 項目 | 定案 | 來源 |
|---|---|---|
| **成像公式** | **面鏡的 1/u + 1/v = 1/f 展開後就是 `v = uf/(u−f)`，與薄透鏡公式完全同式**。五種器材（凸／凹透鏡、平面／凹／凸面鏡）共用同一支 `imaging()`，差別只有兩件事：`signedF()` 的正負號，與 `outSign()`（像量在哪一側） | C，已驗算 |
| 平面鏡 | **焦距＝無限大**。直接代 `Infinity` 會得到 NaN，但取極限乾淨：`v → −u`（鏡後等距）、`m → 1`（正立同大）。這就是平面鏡成像的全部內容，不必另寫一套 | C |
| `outSign()` | 透鏡 +1（光穿過去）、面鏡 −1（光折回同側）。像、紙屏、觀察者眼睛、出界提示、拖曳座標**全部**乘它 | C |
| 鏡種元件 | `.seg` 三格（平面鏡／凹面鏡／凸面鏡）。切到平面鏡時**焦距與物距整組收起來**（不是停用）——那兩組對平面鏡不是「暫時不能用」，是不存在 | §2.3 既有定案 |
| 鏡面弧度 | 矢高用球面鏡幾何算：`R = 2f`、`s = h²/(4f)`，f=2/3/4 → 1.125／0.75／0.5625 格。**不另編一張表**，「焦距越短鏡面越彎」與透鏡共用同一條規則 | C |
| 鏡子畫法 | 反射面朝物體（左）並畫粗，鏡背是 0.22 格的不透光帶。**哪一面在反射必須一眼看得出來**，否則凹凸只是兩條長得差不多的弧 | C |
| 特徵光線 | 三條的式子與透鏡**完全共用**：①出射通過 `cx + sx·f·g`、②在鏡心依法線（主軸）反射所以斜率取反號、③入射射向 `cx − f·g`、出射平行主軸 | C，已驗算 |
| 第③條的名稱 | 凹面鏡「③ 通過焦點」、凸面鏡「③ 射向焦點」（與同型的透鏡一致）；平面鏡**整條不存在**，核取與圖例列都收掉——列出來標「已關閉」會讓學生以為只是被關掉了 | C |
| **法線** | 三條特徵光線打在鏡面的位置**都畫**，黑虛線（虛線的第三種用途）。方向＝該點指向曲率中心（`R = 2f`），平面鏡恆為水平。②的法線與主軸重合，**照畫並在圖例註明**「兩條疊在一起」 | **A**「選B」（§2.8） |
| F／2F 記號 | **透鏡兩側都標、面鏡只標一側**：凹面鏡在鏡前、凸面鏡是鏡後的虛焦點、平面鏡整組不畫。鏡後標一個不存在的 F 會直接教錯 | C |
| 平面鏡的等距標記 | 主軸下方畫**兩段等長的標記線段**（鏡前物距、鏡後像距），拖物體時兩段一起變、永遠相等 | 見下 |

**平面鏡等距標記是折衷方案**〔C，**待使用者確認**〕：§2.3 頁 2 的迷思概念（**B** 類）寫的是
「在鏡面**兩側各標出格數**」，但 §2.2 的「**一律不標任何數值**」是 **A** 類且更晚，兩者直接衝突。
折衷是改畫等長線段——學生看到的是「一樣長」，仍然靠格線自己數格，畫面上沒有出現任何數字。
**§2.5 的丁2 未結案項在此結掉。** 若使用者堅持標格數，改回來只需在 `drawEqualMarks()` 加兩個 text。

#### 針孔（頁 1）：模型完全不同

| 項目 | 定案 | 來源 |
|---|---|---|
| 模型 | 沒有焦距、沒有特徵光線，只有**相似三角形**與離散光線 | §2.3 頁 1 |
| 光線畫法 | 從發光點射出一組離散光線，**等間隔打在擋板平面上**（不是等角度）——「孔幾個間距寬 → 幾道通得過」才是學生數得出來的對應。被擋的畫到板面就停、淡色；通過的直線走到屏 | **A**（勿改回連續光束） |
| 孔徑 | **三檔膠囊**（小／中／大），各放行 **1／3／5 道**。間距 `PIN_SPACING = 0.26` 格 | 三檔＝**A**（§2.5 第 9＋17 點）；1/3/5 與間距為 C |
| 擋板 | **孔是板子上留的空隙**，畫成上下兩段實心中間留空，不是在板子上畫一個圓。學生要看得出光是從缺口穿過去的 | C |
| 扇形張角 | 涵蓋所有的孔 ＋ 外加 4 道被擋的當對照組。單孔時約十幾道，三孔時才變寬 | C |
| 示範點 | 預設只有物體頂端；「完整光束」在本頁＝再加底端與中段兩點 | §2.3 頁 1 第 6 點 |
| 紙屏 | **預設開、不加開關**（主要器材不是教學疊層），預設放在孔右側 6 格，可拖 | **A**（§2.7） |
| 屏上的像 | 沿**孔的輪廓**取樣 14 點＋中心點，各畫一份半透明疊起來 ＝ 用孔形對像做卷積。圓孔糊成圓斑、方孔糊成方斑，**整幅像的形狀完全不變** | §2.3 頁 1 迷思概念 |
| 多孔 | 每個孔各自放行一組光線、各自成一個完整的像，位移 `c·(1 + vs/u)`。孔心間距 ＝ 孔徑 ＋ 0.6 格，孔與孔之間一定看得到板子 | §2.3 頁 1 迷思概念 ＋ C |
| 本頁沒有 | 焦距、物距拖桿、特徵光線、觀察者眼睛、平行光、遮板 | C |

**驗算（斷言全部通過，腳本在 session scratchpad，未入版控）**：

- 四種有焦距的器材（凸／凹透鏡、凹／凸面鏡）各跑五檔：會聚型在 `u>f` 為實像倒立、`u<f` 為虛像正立放大；發散型恆為虛像正立縮小且 `|v| < |f|`
- **像畫在正確的一側**：實像落在出射側、虛像落在反方向，四種器材全部成立
- 平面鏡：`m = 1`、虛像、像在鏡後等距（誤差 0）
- **光線③ 與像頂的高度差 < 1e-9，四種器材都成立**（沿用 §2.12 的驗證基準）
- 針孔：孔徑三檔實測放行 1／3／5 道；**屏上像高固定 2.40 格不隨孔徑變，只有彌散從 0.57 → 1.72 → 2.86 格**——這就是「孔的大小只改變糊的程度，不改變像的形狀」的數值證據
- 瀏覽器實測：孔數 1/2/3 → 擋板實心段 2/3/4、屏上疊繪 15/30/45 份（每孔一份完整的像）、通過道數 5/10/15；換孔形時疊繪份數與通過道數**完全不變**（只有位移排列變）
- 面鏡頁拖物體：拖到 8 格（>2f）→「2f 外」實像倒立；拖到 1.2 格（<f）→「f 內」虛像正立
- console 無錯誤

**左欄捲動量**（承 §2.13 的「接受捲動」裁示）：1180×700 實測餘裕——透鏡頁 −127、凹面鏡 −83、
**平面鏡 0、針孔頁 0**（這兩頁的控制項比較少，剛好放得下）。

### 2.15 階段 5 實作定案（2026-09-07，頁 3 眼睛完成，四頁全滿）

> 使用者指示「**開始做完、標格數**」。前者授權做完階段 5，後者結掉 §2.14 末的平面鏡待確認項。

#### 平面鏡等距標記：改標格數（**A**，結案）

| 項目 | 定案 | 來源 |
|---|---|---|
| 呈現 | 等長線段**保留**，鏡前鏡後各再標一個 `N.N 格`，兩個數字永遠相同 | **A**「標格數」 |
| 與 §2.2 的衝突 | 這兩個讀數是「一律不標任何數值」的**唯一豁免**。§2.3 頁 2 的 B 類定案（兩側各標格數）與 §2.2 的 A 類定案直接衝突，使用者裁定**由 B 類勝出**，且豁免只限這一處 | **A** |
| 為什麼線段不拿掉 | 線段給的是「一樣長」的直觀、數字給的是精確讀數。只留數字會讓學生得自己比對兩個數，反而比原本難看出相等〔C〕 | C |
| 小數位 | 一位（物體是連續拖的，`5.25` 顯示成 `5.3 格`）〔C〕 | C |

#### 眼睛頁的模型：一片焦距會變的凸透鏡 ＋ 一個固定的像距

**全頁只有一個變數在動**：水晶體的焦距。眼軸長是常數（**不做眼軸過長**，§2.5 第 7 點），
所以「像落在視網膜前還是後」完全由水晶體決定——這就是「近／遠視只做水晶體太厚太薄」的意思。

| 項目 | 定案 | 來源 |
|---|---|---|
| **眼軸長 `EYE_L`** | **4.647 格**。不是估的：量 `眼球.svg` 的水晶體中心（viewBox x=112.35）到主軸高度上的眼球後壁（x=475.67，由相鄰兩端點內插），再乘「眼球總高 6 格」的縮放 | 量測 |
| 眼球定位 | **水晶體中心對到鏡心、水晶體垂直中心對到主軸**（不是拿眼球外框置中）。「主軸通過水晶體中心」因此在幾何上是真的 | C |
| 瞳孔口徑 | **1.2 格** ＝ 彌散斑公式裡的 A。`aperture()` 四頁只有一個定義處，透鏡／面鏡回 `LENS_H`、眼睛回它 | C |
| 物距 | 看遠 **10 格**（畫布半寬 11 扣掉蠟燭後的極限）、看近 **4 格** | C |
| 調節範圍 [最厚, 最薄] | 正常 `[2.00, 3.35]`／近視 `[1.75, 2.30]`／遠視 `[2.90, 3.55]`。看遠需 3.173、看近需 2.150 | C |
| **刻意沒做等寬** | 三段範圍是為了讓三種症狀都明顯而選的**教學參數**，沒有讓三者的調節幅度相等（以屈光力計＝0.20／0.31／0.06）。幅度做等寬會讓遠視在這個尺度下對得到近物，這一頁就沒有遠視可教 | C |
| 水晶體變形 | 兩條 path 逐數字線性插值。指令序列都是 `mcz`、各 182 個數字、逐位置型別對齊（**已驗算**）。原檔「薄」的中心比「厚」少 **6.10867** 單位，補進它的**第一個 m 的 x**（只有那個是絕對座標）就同心 | C |
| 厚薄的尺 | `EYE_F_RANGE` ＝ 三種眼睛調節範圍的**聯集**（`[1.75, 3.55]`）。三種眼睛共用同一把尺，「近視眼看遠時的水晶體比正常眼厚」才比得起來 | C |
| 矯正鏡位置 | 水晶體左 **2.0 格**（眼球左緣在 1.437 格，鏡片落在它前方）。鏡片高 3 格 | C |
| 矯正的算法 | **兩次成像**：鏡片先成一個像，**那個像才是水晶體的物**。像落到水晶體右側就是**虛物**（`uEye` 為負），`imaging()` 原式照樣成立不必特判 | C，已驗算 |
| 度數五檔 | 屈光力 `1/|f|`：近視 `[0.12, 0.30, 0.55, 0.85, 1.30]`（凹）／遠視 `[0.20, 0.28, 0.34, 0.40, 0.47]`（凸）。兩組都刻意跨過「剛好」的區間 | C |
| 鏡片彎曲下限 | `bow = clamp(0.55/|f|, 0.12, 0.34)` 格。**下限不可省**：最弱那幾檔算出來只有 0.03 格，畫出來凹凸分不出來，而這一頁的迷思概念正是「近視眼鏡是凸透鏡」 | C |
| 光線畫法 | 先定光線在**水晶體平面**上的落點（等距分佈在瞳孔口徑內），再往回推它在鏡片上的落點。這樣戴不戴眼鏡進到眼睛的都是同一束光，兩種情況才比較得起來 | C |
| 本頁沒有 | 焦距、物距拖桿、特徵光線、物體造型（一律蠟燭）、遮板、觀察者眼睛、紙屏開關、完整光束開關 | §2.5 第 7、10 點 ＋ C |
| 視網膜 | 屬眼球構造，**常駐不做開關**（結掉 §2.6 的 N3）。小窗標題由 JS 換成「視網膜上的像」 | §2.6 N3 |
| 視網膜小窗尺度 | **窗高 ＝ 4 格**（另外兩頁是 10 格）。眼軸只有 4.6 格、像高不到 2.4 格，沿用 10 格會小到看不出糊不糊 | C |
| 視覺模擬 | 留白帶再開一個小窗，排在視網膜小窗左邊。樹與書用 Font Awesome ＋ CSS `filter:blur()`，糊的程度讀**同一支** `blurOf()` | §2.3 頁 4 |
| 視覺模擬**預設開** | 不套「教學輔助疊層一律預設關」：畫布上的光路是抽象的，這個小窗是唯一把「像沒落在視網膜上」翻成學生自己的感受的東西，一開機就要看到核心命題（DEV_SPEC 9.4）。它在留白帶，不疊在光路上 | C |
| 正常眼 | 「戴上眼鏡」與「度數」**整組收起來**（比照平面鏡收焦距）——正常眼沒有要矯正的東西，鏡片種類也沒有依據 | C |
| 換眼睛狀態 | 度數回到最弱那一檔。把上一種眼睛的度數帶過來，學生一切過去就看到一副「已經配好」的眼鏡，而這一頁要的正是他自己調到剛好 | C |
| 眼球內部 | **不畫蠟燭的像**，只畫光線的交會與視網膜上的光斑。眼球內部只有 4.6 格，畫一個像進去會與玻璃體、視網膜的填色糊在一起，反而看不出「聚焦點在視網膜前面」 | C |
| 成像面記號 | **不另外標**。光線自己的交會點已經很明顯，再畫一條橘虛線會變成「虛線的第四種用途」，違反 §2.5 第 4 點 | C |

#### 一個實作坑（**已修**，值得記著）

**「度數不足／過頭」不可以用 `need` 判**。第一版寫成「`need` 偏大 ＝ 薄不下來 ＝ 近視鏡度數不足」，
瀏覽器實測抓到：**遠視眼戴眼鏡看遠**的五檔全部誤報「度數不足」，實際上像都落在視網膜**前**、
五檔都是過矯正。根因是那個情境下 `uEye` 是**虛物**（負），`need` 跟著變成負數（−105.8 / −6.2 / −2.5 / −1.1），
「need 偏小」就不再等於「厚不起來」。**改成直接比 `im.v` 與 `EYE_L`**：像在視網膜前，對近視鏡是不足、
對遠視鏡是過頭（兩種鏡片方向相反）。畫布上那句標籤本來就是這樣判的，是 `powerLabel()` 沒跟上。

**驗算（瀏覽器實測，斷言全部通過）**：

- 六種基本組合：正常眼看遠／看近都對得到；**近視眼看遠 v=2.99 < 4.647（視網膜前）**、看近對得到；
  **遠視眼看近 v=10.55 > 4.647（視網膜後）**、看遠對得到
- **水晶體厚薄**（bbox 實測寬度）：近視看遠 **61.97** vs 正常看遠 **51.76**（近視明顯厚）；
  遠視看近 **54.95** vs 正常看近 **63.73**（遠視明顯薄）；近視看近與正常看近**完全相同**（63.73，物理正確）
- **變形本身**：`t=0` 的 bbox 與原檔「厚」逐位相同、`t=1` 與「薄」相同；寬度線性等差（68.41→63.15→57.88→52.61→47.34）；
  高度五個 t 全部 158.292（只有寬度變）；中心在 t=0→1 只飄 **0.036 單位**（不對齊會飄 6.11）
- **度數五檔**：近視看遠＝不足→剛好×3→過頭；遠視看近＝不足×2→剛好×2→過頭；
  遠視看遠＝五檔**全部過頭**（本來就不需要戴）；近視看近＝剛好×3→過頭×2
- **視覺模擬**：正常眼看遠 樹 0px／書 7.53px、看近 樹 7.53px／書 0px（完全對稱）；
  近視看遠 樹 6.00／書 1.53；遠視看近 樹 1.49／書 6.04
- **回歸**（另外三頁沒被弄壞）：凸透鏡 u=5.25/f=3 → v=7、m=−1.333（與 §2.9 數據表吻合）；
  凹透鏡 → v=−1.909、m=0.364 虛像正立；針孔孔徑三檔放行 1/3/5 道、孔數 1/2/3 → 疊繪 15/30/45 份；
  平面鏡兩側標出 **`5.3 格`／`5.3 格`**；四頁的左欄控制項顯隱各自正確
- **內嵌素材**：`index.html` 的五條眼球 path 與 `眼球.svg` **逐字相同**（22,894 字元，腳本比對零差異）
- console 全程無錯誤

**左欄捲動量**（1366×768）：眼睛頁**三種狀態（含戴眼鏡）在 U=1／1.15／1.4 三個字級尺度下餘裕都是 0**
——不捲動。其他頁維持原狀（透鏡頁 −79、凹面鏡 −35），眼睛頁沒有讓情況變差。

---

### 2.16 第六輪裁決（2026-09-07，眼睛頁重解 ＋ 面鏡反射點）

使用者一則訊息 8 個編號回饋。前四點互相牽動（動一個就要重解整組參數），合併成一次重算；
後四點各自獨立。**§2.15 的眼睛頁參數整組作廢**，以本節為準。

**1〔A〕「如果是眼睛的模擬，我可以接受蠟燭高1格。眼鏡矯正的度數3段」**

- `EYE_OBJ_H = 1`（眼睛頁物高，格）。另外三頁的 `OBJ_H = 2` **未改**。
  取值統一走新函式 `objH()`，半寬走 `objHalfW()`，四頁只有這兩支在決定物體尺寸。
- `EYE_POWER` 五檔 → 三檔：近視 `[0.112, 0.181, 0.281]`、遠視 `[0.069, 0.111, 0.240]`。
  連帶 `POWER_NAME = ['較弱', '中等', '較強']`、`powerSlider` 的 `max` 4 → 2、刻度五格改三格。

**2〔A〕「近視眼未矯正看遠時像落在視網膜前1.5格、遠視眼未矯正時像落在視網膜後1.5格」**

- 這兩個數字**反解出**調節範圍的端點，不再是掃描出來的：
  `EYE_ACC.myopia = [2.450, 2.589]`（上限 ＝ 讓 u=14.6 時 v=3.147 的焦距）、
  `EYE_ACC.hyper = [2.903, 3.590]`（下限 ＝ 讓 u=5.5 時 v=6.147 的焦距）、
  `EYE_ACC.normal = [2.450, 3.590]`（兩者聯集 → 全程對焦）。
- 瀏覽器實跑驗證：近視 u=14.6 → 偏離 **−1.500 格**、遠視 u=5.5 → 偏離 **+1.501 格**。

**3〔A〕「凹凸透鏡和凸凹透鏡的方向錯了」** → `drawSpectacle()`

- 根因：舊寫法兩片的凸面**都朝右**（朝眼睛），只差中央厚薄，於是兩個名稱只有一個說得通。
- 改成兩片互為鏡像：`sg = o.gf > 0 ? 1 : -1`，`c1 = B - D/2`（彎得淺）、`c2 = B + D/2`（彎得深）
  固定不再跟著正負互換。遠視＝**凹凸透鏡**（凸面朝眼睛、中央厚）、近視＝**凸凹透鏡**（凸面朝物體、中央薄）。
- 這一點由 fresh-context 審查 agent 獨立定位；我原本的判讀停在「整體鏡像」，沒看出「兩片同向」才是根因。

**4〔A 提議 ＋ C 取值〕「如果眼球整體右移幾格會不會解決這問題」** → 會，這正是前四輪缺的自由度

- `EYE_SHIFT = 4`（格）：水晶體從畫布正中的第 11 格移到第 15 格，
  `EYE_U_RANGE.max` 10.5 → **14.6**，可拖範圍上下限比值 1.9 → 2.65。`min` 維持 **5.5 未改**。
- 4 格是**模型判斷（C）**：畫布 22 格，眼球右緣落在 21.19、遠視未矯正時的像落在 21.15，再多就出界。
- 器材 x 收斂到新函式 `optCx()`：眼睛頁回 `G.cx + EYE_SHIFT·g`、另外三頁回 `G.cx`。
- **矯正模型連帶改寫**：`EYE_POWER_OK = 1`，中間那檔直接取 `f = need`、不受調節範圍限制。
  〔**B** 使用者先前選定「保留度數，只有一檔全對焦」〕原本打算用「換成正常眼的調節範圍」實作，
  但遠視戴上正確度數再看遠時，鏡片會把光聚成**虛物**、所需焦距衝到 5.5 格，換範圍救不了，只能直接強制。
- 實跑驗證：兩種眼睛的中間檔在 u=5.5／8／11／14.6 全部「膜上」；兩側檔位的糊法方向正確
  （近視太弱→前、太強→後；遠視太弱→後、太強→前）。
- 水晶體厚薄：未矯正的近視恆 `lensT=0`（最凸）、遠視恆 `1`（最薄）、正常眼 0.06→0.94 動態；
  **戴上眼鏡後改用「正常眼看這個距離該有的厚薄」插值**，否則強制對焦的 f 會讓它黏死在一端。

**5〔A〕「其他頁面物體選f，切換到眼睛頁物體會維持f，應該要是蠟燭才對」**

- 分頁切換加一行 `if (n === 3 && state.shape !== 'candle') pickShape('candle')`。
  眼睛頁的造型組本來就 `hidden`（`syncControls`），缺的只是切進來時沒重設。

**6〔A〕「凸面鏡和凹面鏡的光線，反射的位置不在鏡面而是在垂直軸上…你可以用拋物面鏡」**

- **鏡面本來就是拋物面**：`drawMirror()` 的二次貝茲展開後正好是 `x = cx − dy²/(4f)`，
  與 `mirrorSagitta()` 同係數（審查 agent 用 1001 點掃描比對，最大差 4.4e-16 格）。
  **所以鏡面畫法一個字都沒改**，改的只有光線的落點。
- 新增 `hitX(y)`：三條特徵光線、法線、完整光束、平行光的轉折點全部接到這條弧上。
  實測 f=3 時①的轉折點 x=481.54，拋物線預測 481.55；六種鏡子×焦距組合的弧上誤差 ≤0.005px。
- **出射方向同時改**（新函式 `outEnd()`）：反射點離開主軸後不能再用近軸斜率，
  改成從弧上落點連向 `imaging()` 的像點。透鏡頁 `hitX` 回 `cx`，兩式數學上恆等——
  實測 12 個樣本的出射段起點離 `cx` **0.000px**，那一頁零回歸。
- 法線方向改用**拋物面自己的法線** `(1, dy/(2fg))`，不再借球面鏡的「指向 2f」。
- 連帶修正 `gearMaxHalfT()`：凹面鏡的可拖下限吃進鏡面在物高處的矢高 `h²/(4f)`，
  否則物體會插進鏡子。下限 f=2 → 0.812、f=3 → 0.645、f=4 → 0.562（原本一律 0.412）。
  副作用：f=2 搭「f 內」的定案位置 u=0.75 被夾到 0.812（與 §2.9 凹透鏡那個先例同一種讓步）。
- **取捨（C 類，待裁示）**：三條線精確共點於公式像點，代價是第①條不再精確通過焦點記號。
  偏差（單位：格，物高 2 格）：

  | f | 2f 外 | 2f 上 | f ~ 2f 間 | f 內 |
  |---|---|---|---|---|
  | 2 | 0.235 | 0.286 | 0.320 | 0.941 |
  | 3 | 0.095 | 0.118 | 0.133 | 0.500 |
  | 4 | 0.052 | 0.065 | 0.073 | 0.302 |

  另一個選項是讓①精確通過焦點（拋物面對平行光是恆等式），代價是三條線**交不到同一點**、
  散開約 0.18 格，且交點離公式像點 0.33–0.51 格（f=2、u=5 實測）。
  我選了前者：畫面上到處都在用 v（像的位置、紙屏、讀數），保住像點比保住焦點重要。
  根本原因是這一頁不近軸（物高 2 格 ÷ f 2 格 ＝ 1.0），薄鏡公式對它本來就不成立。

**7〔A〕「拖動的提示有點不明顯」** → `.draghint`

- 灰色 13px 裸箭頭（壓在同樣灰的格線上就融進背景）→ 主色 15px ＋ 白底膠囊 ＋ 邊框 ＋ 陰影。
  `hintMask` 的水平定位常數 13 → 18（膠囊變寬）。

**8〔A〕「眼睛模擬的視網膜上面都有奇怪的橘色垂直線」** → 移除 `drawRetinaSpot()`

- 那條線本意是「視網膜上接到的光斑」，卻用光線的橘色與粗細畫，既不是光線也不是像；
  審查 agent 另外指出 `spreadOpacity()` 讓它**對焦最準時最濃**，語意剛好相反。
- 同一件事已有兩個更好的載體：右下角「視網膜上的像」小窗（`EYE_WIN_GRID` 4 → **2** 格，
  因為像高只剩 0.32～0.85 格），以及第 2 點要求的「一律畫出成像」。要看彌散斑就勾「完整光束」。

## 3. 已回流 `_shared/` 的規範例外（2026-09-04 完成）

使用者裁示「**幫我回流**」（**A**），**已於 2026-09-04 全部執行**，母倉 commit `634b652`。
下表保留是為了記錄改了什麼、為什麼改——這三條現在是**全域規則**，不是本專案的特例。

| # | 改哪裡 | 要寫進去的內容 | 狀態 |
|---|---|---|---|
| X1 | `DESIGN_SPEC` 第 1 節 | 數據能直接標示在畫布上的模擬，允許**兩欄 `1fr_3fr`**（設定：畫布 ＝ 1:3，比例規則不變）。判準是即時讀數本來就長在圖形上——光學的物距、像距是光軸上的一段，搬進右欄卡片反而違反「讀數貼在它描述的圖形旁」那一條 | ✅ 已完成 |
| X2 | `DESIGN_SPEC` 第 2 節格線那條 | 格線**分主次**：次格線沿用 `#D5DAE1`（1.41）；格線當**判讀工具**時，每 5 格一條主格線用 **`#A6AEBA`**（白底 **2.24:1**、頁面底 2.09），仍不套 3:1 但明顯高於裝飾網底。另註明光軸沿用座標軸定案值 `#7A8496`（3.77） | ✅ 已完成 |
| X3 | `DESIGN_SPEC` 第 1 節 iPad 直向那條 | 沒有右欄的模擬，`<900px` 時**只有「設定」一顆 chip** | ✅ 已完成 |
| X4 | 不必改規範 | 物距用**畫布拖曳**不是滑桿，`DESIGN_SPEC` 第 4 節「物理參數滑桿一律離散檔位 3–7 檔」管的是滑桿，未違反。焦距用的是膠囊三檔，本來就合規 | 已釐清 |

`#A6AEBA` 標 **C**——使用者沒指定過主格線要多深，按「教室大屏遠看數得出格子、又不能蓋過光線」抓的。

---

## 4. 開發階段切分（每階段一個 session）

| 階段 | 內容 | 為什麼是這個順序 |
|---|---|---|
| 1 | 骨架（兩欄、無抽屜、動作卡、四分頁殼）＋共用引擎＋**頁 3 凸透鏡** | 凸透鏡情境最完整，引擎、格線、光屏模糊、拖曳、觀察者眼睛全在這階段磨好 |
| 2 | 頁 3 凹透鏡＋可拖遮板 | 共用階段 1 的引擎，只換符號約定 |
| 3 | 頁 2 面鏡三種 | 同一套成像公式，換器材外觀與光線同側 |
| 4 | 頁 1 針孔 | 引擎不同（相似三角形＋離散光線），但光屏模糊模型共用 |
| 5 | 頁 4 眼睛 | 凸透鏡引擎的延伸應用，最後做 |

〔C — 使用者尚未表態，規劃時他說「都先不要做，繼續問我問題」。〕

---

## 5. 進度

### 已完成（2026-09-04 規劃 session）

- 需求討論完畢，四個分頁的畫布內容、左欄控制項、迷思概念功能全部定案（見第 2 節）
- 六步檢查表：① `optics-sim/` 建立、複製 `_shared/template/index.html` ② 母倉 `.gitignore` 加 `/optics-sim/` ③ 母倉 `.claude/launch.json` 加 `optics`（port 4177，serve root 限本目錄）④ 專案 `.gitignore` 寫 `design_handoff/` ⑤ `git init -b main` ⑥ 本檔建立
- 順手把先前 session 遺留的共用規範用詞修訂 commit 掉（母倉 `b77aeef`，「播放速度」→「動畫速度」，六個檔，本 session 一行都沒改）
- **X1–X3 回流完成**（母倉 `634b652`）：改了 `DESIGN_SPEC` 第 1／2／3 節、`DEV_SPEC` 檔案結構註解與 5.3／5.4、`layout.html`、`components-spec.html`（新增主格線色卡）、`template/index.html`、`README.md` 共六個檔。主格線對比度用腳本實算 **2.24／2.09**，同一支腳本驗出次格線 1.41、座標軸 3.77，與規範既有記載吻合
- 三個 commit 落地：母倉 `634b652`（規範回流）、`04bb7ab`（母倉設定），`optics-sim` `7f02dd4`（初始化）。兩個工作樹都乾淨
- **`DESIGN_BRIEF.md` 完成**（尚未提交），可交給 Claude Design 出稿
- **專案 memory 更新**：`sim-spec-repo-status`（結構圖補 dissolution 與 optics、remote 現況查核）、新增 `two-column-gridline-decision`（兩欄配置與格線分級的定案與大屏待測）、`MEMORY.md` 索引補一行

### 已完成（2026-09-06 階段 1 開發 session）

- **骨架**：兩欄 `minmax(302px,1fr) 3fr`、無抽屜、左欄下方動作卡、四分頁殼（透鏡頁預設選中）、
  `<900px` 只有「設定」一顆 chip。1366×768 實測左欄 **331.5**／畫布 **992.5**（比例 2.994）
- **座標系統**：橫向 22 格、格邊長 **45.11px**、縱向 **14.10 格**、主軸在第 **4.699 格**、光路收上 2/3。
  主次格線以**鏡心與主軸**為基準每 5 格加深
- **共用成像引擎**：`imaging(u,f)` 純函式；五檔離散拖桿與畫布拖曳雙向連動（單點／區間不對稱）；
  出界處理（「像在更遠處」「虛像在更遠處」「出射光互相平行，不成像」）
- **頁 3 凸透鏡**：透鏡厚度隨焦距、三種物體造型（蠟燭 svg／箭頭／F 形，全部 2 格高）、
  三條特徵光線（白色外描邊＋箭頭）、反向延長線、完整光束、實／虛像、紙屏＋屏面小窗（真高斯模糊）、
  觀察者眼睛、圖例卡（關掉的光線變灰虛線標「已關閉」）
- **左欄捲動問題已解**（§2.5 第 15 點）：桌機餘裕 58px、平板（--u=1.15）41px、大屏（1.4）13.7px，三種都不捲動
- 詳細定案與三個實作坑見 **§2.12**

### 已完成（2026-09-06 階段 2 開發 session）

- **凹透鏡**：`.seg` 兩格切換；焦距取負號後成像引擎、三條特徵光線、完整光束、觀察者眼睛
  **全部原式共用**；雙凹外形與凸透鏡共用同一張曲率表；第三條光線的名稱隨透鏡種類換
- **可拖遮板**：從透鏡上方往下拉，被擋的光線整條不畫、圖例標「（被遮板擋住）」，
  像只變暗不變形（亮度 ＝ 透光比例^0.45），全遮時像消失並標出原因
- **紙屏對虛像有回饋了**：`blurOf()` 改用絕對值通式，屏拉得越遠光斑越大越淡
  （連帶改到凸透鏡 `u<f` 那一檔，見 §2.13）
- 驗算 30 組（凸 15 無回歸 ＋ 凹 15 全部正立縮小虛像）、光線③ 收斂誤差 <1e-9、console 無錯誤
- **遮板改為可開關的核取**（與紙屏一樣預設關）＋ 拖曳提示圖示，使用者 2026-09-06 追加裁示
- **左欄捲動**：使用者裁示「接受捲動」，桌機餘裕 −61px，動作卡維持不動，見 §2.13 末
- 修掉一個靜默 bug：§2.8 記的 `fa-arrow-up-down` 在 FA6 不存在，錯名不報錯只渲染成 0 寬
  （已更正為 `fa-arrows-up-down`，通用教訓落檔於 `pitfalls-frontend.md` 第 2 條）
- 詳細定案見 **§2.13**

### 已完成（2026-09-07 階段 3＋4 開發 session）

- **架構**：四頁改用同一張畫布；左欄拆成分頁專屬組＋共用組；尚未實作的頁蓋板並收起左欄
- **頁 2 面鏡三種**：`.seg` 平面／凹面／凸面；**與透鏡共用同一支成像引擎**（面鏡公式展開後同式，
  差別只有 `signedF()` 與 `outSign()`）；平面鏡＝焦距無限大；三條特徵光線的法線（黑虛線）；
  平面鏡的等距標記；F／2F 只標在有焦點的那一側
- **頁 1 針孔**：離散光線等間隔打在擋板平面；孔徑三檔放行 1／3／5 道；擋板是留空隙不是畫圓；
  沿孔形輪廓取樣疊繪（圓／方／三角斑，像的形狀不變）；多孔 → 多個完整的像；紙屏預設開不加開關
- 驗算：四種器材 × 五檔全部通過（像的側別、實虛、正倒、放大縮小）、光線③ 誤差 <1e-9、
  針孔像高不隨孔徑變、瀏覽器實測孔數與疊繪份數對應正確、console 無錯誤
- 詳細定案見 **§2.14**

### 已完成（2026-09-07 階段 5 開發 session）

- **平面鏡改標格數**（使用者裁示「標格數」）：等長線段保留，兩側各加一個 `N.N 格`，
  結掉 §2.5 丁2 一路帶到 §2.14 末的未定項。這是 §2.2「一律不標任何數值」的唯一豁免
- **頁 3 眼睛**：眼球剖面用使用者的 svg（三條 path 逐字內嵌、機械比對過）；
  **水晶體逐數字插值做連續變形**（不是兩張圖切換）；三種眼睛狀態 × 看遠／看近的調節；
  矯正鏡走**兩次成像**（鏡片的像才是水晶體的物，虛物情況原式通用）；度數五檔跨過「剛好」；
  視網膜光斑與視網膜小窗；**視覺模擬小窗**（遠處的樹＋近處的書本，同一支彌散斑公式）
- **四頁全滿**：畫布蓋板 `.screen` 與 `unbuilt` 那段左欄收合邏輯一併移除，
  導覽列不再有點進去是死路的頁——`DEV_SPEC` 9.5 的部署前提已滿足
- 修掉一個自己寫出來的 bug：`powerLabel()` 用 `need` 判「度數不足／過頭」，
  在虛物情境下會講反（遠視眼戴眼鏡看遠五檔全誤報），改成直接比像距與眼軸。詳見 §2.15
- 落檔一個工具坑：`javascript_tool` 讀不到頁面的頂層 `let`／`const`
  （→ `handbook\pitfalls-services.md` #13，同時補進該檔 §0 症狀索引）
- 詳細定案與驗算見 **§2.15**

### 已完成（2026-09-07 第六輪修正 session）

- 使用者 8 點回饋全部實作，逐項去向見 **§2.16**（§2.15 的眼睛頁參數整組作廢）
- 眼睛頁重解：眼球右移 4 格、蠟燭 1 格高、調節範圍由「像距 ±1.5 格」反解、度數三檔、
  正確度數強制對焦。瀏覽器實跑驗證：偏離量誤差 ≤0.001 格、正確檔在四個抽樣物距全部落在視網膜上
- 面鏡頁光線改在**鏡面弧**上反射（鏡面本來就是拋物面，不必換器材），
  出射方向改連向像點。六種鏡子×焦距的弧上誤差 ≤0.005px；**透鏡頁 12 個樣本零位移**
- 移除視網膜上那條橘色垂直線；矯正眼鏡改成兩片互為鏡像；拖曳提示膠囊化；切到眼睛頁強制蠟燭
- `.seg` 佔滿寬度**回流完成**（`_shared/template/index.html`、`design/DESIGN_SPEC.md`、
  `design/components-spec.html`）。回流驗收：來源與標的七個值逐一相等
- 派了一個 fresh-context 審查 agent 獨立診斷第 3／6／8 點（dispatch §7 的目視推翻第 2 輪要求），
  第 3 點的根因由它定位

### 待辦

- [x] ~~X1–X3 回流~~ — **2026-09-04 完成**（母倉 `634b652`）
- [x] ~~`DESIGN_BRIEF.md`~~ — **2026-09-04 完成**（**尚未提交**）。通用規格全部寫成「照 `DESIGN_SPEC` 第 N 節」的引用表，**沒有手抄任何規格表**；第 2 節特別警告「`layout.html` 畫的是三欄＋抽屜，本模擬兩者都不同，不要照它畫」；列出八個難題要 Design 解
- [x] ~~階段 1 開發~~ — **2026-09-06 完成**（骨架＋共用成像引擎＋頁 3 凸透鏡，見 §2.12。**尚未提交**）
- [x] ~~階段 2：頁 3 凹透鏡＋可拖遮板~~ — **2026-09-06 完成**（見 §2.13。**尚未提交**）
- [x] ~~左欄捲動的三選一裁決~~ — **2026-09-06 裁決：接受捲動**（**B**，選項原文「接受捲動」）
- [ ] 建 GitHub remote 與 Pages（`main` / root）
- [x] ~~階段 3：頁 2 面鏡三種~~ — **2026-09-07 完成**（見 §2.14。**尚未提交**）
- [x] ~~階段 4：頁 1 針孔~~ — **2026-09-07 完成**（見 §2.14。**尚未提交**）
- [x] ~~平面鏡等距標記的折衷待確認~~ — **2026-09-07 裁決：標格數**（**A**，使用者原話「標格數」）。
      等長線段保留、兩側各加一個 `N.N 格`，見 §2.15
- [x] ~~階段 5：頁 4 眼睛~~ — **2026-09-07 完成**（見 §2.15。**尚未提交**）
- [x] ~~部署前隱藏未完成的分頁~~ — **2026-09-07 自動消解**：四頁全部實作完成，蓋板與收合邏輯已移除
- [ ] 建 GitHub remote 與 Pages（`main` / root）。**這是部署動作**，做之前先跑 `judgment.md` §7 兩道閘
- [ ] **iPad 與教室大屏真機實測**——階段 1 起的〔真〕項目一律未驗：觸控拖曳物體與紙屏、
      `filter:blur()` 在 iPad 上的效能（§2.5 第 3 點的模糊畫法**要等真機才能定案**，備案是離屏快取）、
      大屏 6 公尺外能否數得出格線。**階段 5 新增**：視網膜小窗與視覺模擬小窗**兩個 blur 同時在畫面上**，
      iPad 的 filter 效能要一起看；眼球 svg 的細節（睫狀小帶那些細線）在大屏 6 公尺外會不會糊成一團
- [x] ~~第六輪修正批次（使用者 8 點回饋）~~ — **2026-09-07 完成**（見 §2.16。**尚未提交**）
- [x] ~~`.seg` 佔滿寬度回流 `_shared/`~~ — **2026-09-07 完成**（三個共用檔，**尚未提交**）
- [ ] **面鏡頁的取捨待裁示**（§2.16 第 6 點的表）：三條特徵光線精確共點於公式像點，
      代價是第①條與焦點記號差 0.05～0.94 格（f 越小、物體越近差越多）。
      接受就不動；不接受的替代方案是讓①精確通過焦點，代價換成三條線交不到同一點
- [ ] **階段 5 的 C 類參數待確認**（都能單點改，不動架構）：
      ~~① 調節範圍~~／~~② 看遠看近的代表位置~~／~~③ 度數五檔的屈光力~~／~~⑥ 小窗尺度 4 格~~／
      ~~⑦ 眼球內部不畫像~~ —— **這五項已被 §2.16 取代**（端點改由使用者指定的 ±1.5 格反解、
      看遠改 14.6 格、度數改三檔、小窗改 2 格、像一律畫出）
      ④ **視覺模擬的樹與書用實物色** `#4A7C59`／`#8B5E3C`——與蠟燭、眼球同性質（教學插圖的實物色），
         但**沒有走過色票豁免程序**。要嚴格守色票就換成 `var(--c-text-2)`，CSS 兩個值即可
      ⑤ 視覺模擬**預設開**（不套「教學輔助疊層一律預設關」）
      ⑧ **新增**：`EYE_SHIFT = 4` 格是模型判斷，畫布只剩 0.8 格餘裕；大屏實測若覺得眼球太靠右，
         這個值可以往下調，但每調一次都要重跑 scratchpad 的參數驗算（調節範圍是它反解出來的）

## 6. 2026-09-08 使用者裁示修訂（12 ＋ 1 項）

來源標記：**A** ＝ 使用者原話（附引文）｜**B** ＝ AskUserQuestion 選定（抄選項原文）｜
**C** ＝ 模型判斷、使用者未表態。**只有 A、B 算定案。**

| # | 修訂 | 推翻了什麼 | 來源 |
|---|---|---|---|
| 1 | 刪除眼睛頁「看到的樣子」小窗（連同 `visionwin`、`syncVision()`、`visionBlur()`、CSS、核取） | §2.3 頁 4「視覺模擬」、§2.7 眼睛頁「視覺模擬預設開」 | **A**「眼睛頁面我希望刪除看到的樣子小窗」 |
| 2 | 眼睛頁改成**三條**特徵光線，①平行主軸 ②通過鏡心 預設開、③通過焦點 預設關 | 2026-09-07 的「只畫兩條、不畫通過鏡心」 | **A**「一樣做3條特徵光線，預設顯示平行主軸、通過鏡心」 |
| 3 | 勾「戴上眼鏡」直接配上**正確度數**（中間檔）；換眼睛種類也回到中間檔 | 「預設最弱、先讓學生看到還是糊」 | **A**「戴上眼鏡後預設顯示正確的矯正度數」 |
| 4 | 蠟燭可拖範圍**三等分**：看近｜中間｜看遠。中間**不給膠囊**；兩顆膠囊的亮暗改由所在區決定 | 「看遠／看近兩個代表位置」比對法 | **A**「分為3等分，依序為看遠、中間、看近，不要做出中間的膠囊」 |
| 5 | 近視眼裸眼：最近端最凸，看近區水晶體正常變薄且都能對焦，進中間區起薄不下來、開始糊 | `EYE_ACC.myopia` 原由「像落在視網膜前 1.5 格」反解；「未矯正一律畫成最凸」 | **A**（原話見使用者 2026-09-08 訊息第 5 點） |
| 6 | 遠視眼裸眼：最遠端最薄，看遠區正常變厚且都能對焦，進中間區起凸不起來 | 同上（`EYE_ACC.hyper`） | **A**（第 6 點） |
| 5b | 戴上正確眼鏡後**水晶體厚薄變化與裸眼完全相同**，只是必定落在視網膜上 | 「戴眼鏡後改用正常眼的厚薄」 | **A**「水晶體的厚薄變化如戴上去前，只是必定都能在視網膜成像」 |
| 7 | 箭頭與 F 改用使用者提供的 `箭頭.svg`／`F.svg`，頂點換算後內嵌 | §2.6「箭頭與 F 由模型繪製、半寬 ≤0.4 格」 | **A**「我覺得你畫的箭頭和F很醜，請用我新給你的SVG檔」 |
| 7b | 等比縮到**高 2 格**，半寬變成 箭頭 0.4302／F 0.5185 格；代價是 f=2 搭「f 內」被夾在 0.855／0.944 格 | 「半寬 ≤0.4 格」與 §2.9「f 內 ＝ 0.75 格」（只在 f=2 這一個組合） | **B**「等比、高度守 2 格」 |
| 7c | F 用原檔紅色 `#FF0000`，箭頭用原檔 `#2a6fdb` | 「箭頭與 F 同色」 | **B**「照 SVG 用紅色」 |
| 8 | 針孔擋板**可左右拖**（`state.pinX`）；物距與像距都改成從孔量起 | §2.5 第 9＋17 點「作廢可拖擋板」 | **A**「針孔成像的針孔我也希望有左右移動功能」 |
| 8b | 主畫布的紙屏上**畫出成像** | — | **A**「紙屏上也要畫出成像」 |
| 8c | 紙屏小窗由「沿孔形**輪廓**取樣」改成「孔形**面積**取樣」，透明度由 `1−(1−p)ⁿ = 0.92` 反算 | §2.3 頁 1「沿孔形輪廓取樣疊繪」 | **B**「改成孔形面積取樣」 |
| 8d | 小窗尺度由固定「窗高 10 格」改成**自動貼合內容**（像佔窗高約 75%） | §2.7 針孔頁小窗尺度 | **C**（使用者未表態；起因是像只佔窗高兩成，看不出形狀。像的大小改由 8b 承擔） |
| 9 | 面鏡成的像**左右相反**（`imgFlip()`，三種面鏡都適用；透鏡不相反） | 原本沒做 | **A**「屏面鏡成像的F應該是左右相反的，你沒畫出來」 |
| 10 | **暫時移除**觀察者眼睛（控制項、`state.eye`、`drawEye()`、色票 `C.eye` 全部拿掉，不留死碼） | §2.1 表、§2.4 頁 2／頁 3 左欄、§2.7 排序 | **A**「請暫時移除觀察者眼睛的功能」 |
| 11 | 四個拖曳握把（物體／紙屏／遮板／針孔）本身可拖，不再只是提示 | §2.8「拖曳提示」 | **A**「我希望拖動物體或是拖動標籤都能做到拖動的效果」 |
| 12 | 刪除圖例卡開關，圖例卡常駐 | §2.6 修訂表第 9b 條「圖例卡要有開關」 | **A**「刪除圖例卡開關，預設顯示」 |
| — | 矯正度數弱／強的倍率：近視 0.55×／3.2×、遠視 0.55×／1.7×（由 `scratchpad/power-scan.js` 掃出） | 原 0.62×／1.55×／1.90× | **C**（使用者未表態；判準是「參考端要明顯糊且方向正確」） |
| — | 鏡片曲率改依**檔位**平均分（0／0.5／1），不依屈光力比例 | §2.7 `drawSpectacle` | **C**（弱強倍率不對稱後，照比例算會讓中間檔看起來跟最弱一樣薄） |

**第 13 項（凹面鏡的真實反射）尚未定案。** 使用者 2026-09-08 原話：「凹面鏡平行主軸光線反射後
不會通過，物體放在F時的2條光線反射後也不平行，請依照焦點的位置計算繪製拋物面鏡的曲線以符合真實
數學行為」。已驗算（`scratchpad/mirror-check.js`）並向使用者提出三個選項，**等裁示**：

- 現況（反射點在弧上、出射線連向薄鏡公式的像點）：三線精確交於像點，但第①條偏離焦點記號
  0.095～0.286 格（f=2 最明顯）——這正是使用者看到的現象。
- 若改成真實反射：①③ 精確，但三線交會處與 `v = uf/(u−f)` 差 0.29～8.5 格；**`u = 2f` 時像跑到
  5.4 格、放大率 0.90**，課本「物在 2F → 像在 2F、倒立同大」在畫面上直接對不起來。
- 物高減半只能把 `u=2f` 的誤差壓到 0.16 格，靠近 F 時仍差 1.6～4.4 格。

**這一段在使用者裁示前不得自行實作**；鏡面曲線本身已經是正確的拋物線（`x = −y²/4f`），問題出在
「有高度的物體 ＋ 拋物面」本來就不成完美像，與課本的近軸公式不可兼得。

## 7. 2026-09-10 使用者裁示修訂（4 項已實作 ＋ 第 13 項再提）

| # | 改動 | 原本 | 依據 |
|---|---|---|---|
| 1 | 遮板拖曳提示補上頁面條件（`hintMask.hidden = !state.maskOn \|\| state.screen !== 2`） | 只看 `state.maskOn`，切頁後箭頭鈕留在畫面上 | **A**「擋板的上下移動鈕，在切換頁面後不會消失」 |
| 2a | 針孔小窗尺度改回固定「窗高 10 格」（`PIN_WIN_GRID`） | §6 第 8d 條的「自動貼合內容」 | **A**「像的大小沒改變反而時變模糊」＋ **B**「比照另外兩頁：窗高等於 10 格」 |
| 2b | 小窗的糊改讀 `pinBlurGrid()`，正比於像高，與 `pinSpread()` 分家 | 小窗與主畫布共用 `pinSpread()`，內含 (1+v/u) | **A**「移動這三個時僅改變大小，模糊程度不變」 |
| 2c | `HOLE_CORE` 由單一 0.92 改成三檔 `[0.55, 0.75, 0.95]`；每層透明度不再過 `px()` | 三檔一樣亮，只有糊在變 | **A**「孔徑最小時，直接用最清晰的且稍暗，孔徑越大越模糊且越亮」 |
| 2d | 主畫布紙屏上的像改成**每個孔各畫一份** | 不論幾個孔都只畫一份 | **A**「孔洞數量變多時，可拖動的紙屏上成像也應該變多」 |
| 3 | 像的外框線寬改成隨放大率縮放（`imgSW()`，上封頂原值、下限 1 px），四頁六個呼叫點全套 | 四處寫死 `係數 × g`，`non-scaling-stroke` 使框恆定粗細 | **A**「F 和箭頭的像，外框不會隨著像的大小而改變我覺得很醜」＋ **B**「四頁所有的像都套」 |
| 4 | 兩片眼鏡改成**同向**（凸面一律朝左、朝物體），厚薄改由 c1／c2 誰彎得深決定 | 2026-09-07 定的「兩片互為鏡像」 | **A**「遠視眼的凹凸透鏡幫我左右鏡像」＋ **B**「只翻遠視，兩片變同向」 |

**第 3 項的誠實註記**：使用者原話含「蠟燭的就處理得很好，請比照辦理」，但讀過 `drawShape()`
（`index.html` 第 1063–1082 行）後確認，蠟燭與 F／箭頭走的是**同一段程式、同一組描邊參數**，
沒有可搬的既有實作。視覺差異來自形狀本身：蠟燭保留紅黃填色、輪廓細瘦，F 與箭頭是大面積純色塊，
像縮小時那圈固定粗細的灰框佔比暴增。**所以這一項是新寫的規則，不是回流。**

**第 4 項推翻 §6 的既有裁示**：2026-09-07「凹凸透鏡和凸凹透鏡的方向錯了」定為兩片互為鏡像，
依據是課本沿光行進方向的命名。2026-09-10 使用者改以真實鏡片外形為準（不分近視遠視都外凸內凹）。
修訂鏈：09-07 **A** → 09-10 **A**（後者生效）；命名那一層不再由畫面承擔。

## 8. 2026-09-10 第二批裁示（4 項已實作，第 5 項另立階段）

| # | 改動 | 原本 | 依據 |
|---|---|---|---|
| 1 | 小窗的糊改以「放行道數 − 1」為基準（`pinBlurHalf()`），三檔散布比 0 : 1 : 2 | 以半孔徑為基準，比為 1 : 3 : 5，最小孔仍有 0.13 格散布 | **A**「最小孔時還是模糊，我希望更清楚一點」 |
| 2 | 面鏡①改走 `axisEnd()` 直接連焦點，不再連向像點 | 連向像點，f=2／3／4 各偏焦點 0.286／0.118／0.065 格 | **A**「平行主軸的光竟然會從焦點越偏離越遠…太過不合常理」＋ **B**「像維持課本位置，三線不再交於一點」 |
| 3a | 眼睛頁光線畫到 `endX`，像落在視網膜後時延伸到交會處 | 一律止於視網膜 | **A**「當成像位於視網膜後，我還是希望可以畫出在視網膜後相交的部分」 |
| 3b | `EYE_U_PRESET.near` 由 5.5 改為 7.5 格 | 5.5 格時遠視眼的像落在第 23.35 格，畫布只有 22 格 | **A**「遠視眼看近物的切換膠囊，點選後像會直接超出螢幕範圍」 |
| 5 | 導覽列右側加 `#navMeta` 署名，文字由 `APP_VERSION`／`APP_AUTHOR` 兩個常數決定 | 沒有版本標示 | **A**「我想要加入版本號與作者的標示，我想做在導覽列右側」＋ **B**「版本號 · 作者姓名」 |

**`APP_AUTHOR` 目前是空字串**，因為使用者選了含姓名的格式但尚未提供姓名；留空時只顯示版本號，
姓名一填就會自己出現。**這是待辦，不是已完成。**

### 第 13 項（面鏡光線畫法）2026-09-10 定案：採 A 方案

**A**「我可以接受成像的位置或是大小可以跑掉一點（2f 上的要維持正確）」，
**B** 選定「像維持課本位置，三線不再交於一點」。實作為 `axisEnd()`。

實測（物在 2f，解析畫出來的 path 座標，不是重算公式）：

| 焦距 | ①在焦點處偏離主軸 | ①在像距處偏離像高 |
|---|---|---|
| 2 格 | −0.0001 格 | 0.667 格 |
| 3 格 | −0.0001 格 | 0.250 格 |
| 4 格 | −0.0001 格 | 0.133 格 |

②③仍精確交在課本像點上（②通過鏡心、③出射平行主軸，兩者在近軸下恆交於 `v = uf/(u−f)`，
已驗算 `y3 − ax = h·f/(u−f) ＝ 像高`），所以**畫面上只有①不通過像**。
透鏡頁三檔的兩欄實測皆為 0，即 `axisEnd()` 與舊寫法在透鏡上數學等價（`1/f = (u+v)/uv`），
**這次改動沒有動到透鏡頁一個像素**。

### 下一階段：透鏡頁的播放動畫（2026-09-10 **B** 選定「先做透鏡頁一頁，驗過再擴」）

需求（**A** 原話）：「預設未播放時都是顯示光線的，當按下播放時，光線會從物體同時發出，
交會在同一點後或發散。如果發散後會再重新向後延伸虛線。光線交會時或延伸光線交會時才會顯示像。」
抽屜內容 **B** 選定「只放播放暫停與速度」。加抽屜要處理 [[drawer-playback-only-decision]] 記的
兩個必踩的反向操作（`main` 的 padding 下緣、`<900px` 的浮出面板定位）。

## 9. 2026-09-10 第三批（作者欄、播放動畫；面鏡另有兩項待裁決）

| # | 改動 | 依據 |
|---|---|---|
| 1 | 作者欄改成兩行：第一行 `v1.0.0`、第二行 `by 宏福`，`.navmeta` 改 column 排列 | **A**「作者宏福」「第一行標版號，第二行by宏福」 |
| 2 | 加回底部抽屜（`main` 的 padding 下緣改回 0），只放播放鈕與動畫速度三檔 | **B**「只放播放暫停與速度」 |
| 3 | 光線資料結構由路徑字串改成端點座標（`S()`／`D()`），`clipByAnim()` 依「光走了多遠」截短 | **A**「光線會從物體同時發出」 |
| 4 | 反向延長線排在該條光線的實線之後，實線走完才開始長 | **A**「如果發散後會再重新向後延伸虛線」 |
| 5 | 像等到光線或延伸線走到它身上才出現（`animRevealAt`） | **A**「光線交會時或延伸光線交會時才會顯示像」 |
| 6 | 參數指紋 `animGuard()`：改任何參數就回到完整顯示 | 模型判斷（**C**）：半條光線配新物距是錯畫面 |

實測（1366×768、凸透鏡 f=3、u=6、F 造型）：光線總長 868 px，像在 596 px 處出現（全程 69%），
進度 68% 時像未出現、72% 已出現。凹透鏡虛像的虛線在 60% 之後才開始長（0 → 4 → 103 px）。
四頁只有透鏡頁的播放鈕可用，其餘三頁停用並顯示「這一頁沒有可播放的光路動畫」。

**速度**：`ANIM_GRID_PER_SEC = 4`（1× 每秒 4 格，約 2.5 秒走完）。先取 6 格／秒，實測 1.7 秒太趕。

## 10. 2026-09-10 第四批：反射點改回垂直平面（A 方案）

**B** 使用者選定「照 PhET，反射點回到垂直平面」。**這推翻 2026-09-07 的 A 類裁示**
「反射的位置不在鏡面而是在垂直軸上，我希望可以在鏡面反射」。修訂鏈：09-07 **A** → 09-10 **B**。

實作只有一行：`hitX` 由拋物線式改成恆回 `cx`。另同步改 7 段會變成謊話的註解
（`hitX` 自身、`outEnd` 的取捨敘述、`axisEnd`、平行光模式、法線、完整光束、`clipByAnim`）。
**邏輯 0 處必改**：法線方向式 `(1, dy/(2fg))` 同時等於拋物面法線與「落點連曲率中心 2f」，
兩者方向相同；`gearMaxHalfT()` 的預留照舊需要（鏡面外觀仍是彎的，物體仍會插進弧裡）。

實測（凹面鏡，解析畫出來的 path 座標）：

| 焦距 | 物在 2f 的像距 | 放大率 | ①在焦點處偏離 | ①在像距處偏離 | 物在 F 時出射斜率 |
|---|---|---|---|---|---|
| 2 格 | 4.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −1.0000／−1.0000 |
| 3 格 | 6.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −0.6667／−0.6667 |
| 4 格 | 8.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −0.5000／−0.5000 |

凸面鏡（虛像）三條反向延長線偏離像點 0.0001 格；平面鏡像距 −5 格、放大率 1。
**第 13 項的取捨到此消滅**：三線交於課本像點、①通過焦點、物在 F 時平行，三者同時成立。
上一批「①在像距處偏 0.667／0.250／0.133 格」的代價一併歸零。

### 球面鏡 vs 拋物面鏡（**A** 使用者 2026-09-10 問「哪個讓反射點比較靠近面鏡」）

反射點在垂直平面上，它離鏡面多遠 ＝ 鏡面在該高度的矢高。同焦距同高度下球面恆較凸：
球面 `R−√(R²−h²)` 展開 ＝ 拋物面 `h²/(4f)` 再加一串正的高階項，差值主項 `h⁴/(64f³)`。

| 焦距 | 高 2 格（①入射高）拋物／球面 | 高 3 格（鏡緣）拋物／球面 |
|---|---|---|
| 2 格 | 0.500／0.536 | 1.125／1.354 |
| 3 格 | 0.333／0.343 | 0.750／0.804 |
| 4 格 | 0.250／0.254 | 0.563／0.584 |

**結論：維持拋物面**（現況即是），鏡面畫法不動。

**已知的視覺代價**：轉折點在 `cx`、鏡體畫在 `cx−矢高` 處，所以光線會穿過鏡面在鏡後轉折，
f=2、高 2 格時約 0.28 格（扣掉鏡體厚度後）。PhET 的緩解做法是把鏡面畫細畫淡。**未處理，待使用者看過再定。**

### 待裁決：審查發現的兩個既有缺陷（2026-09-10，fresh-context opus 審查）

1. **③的落點會跑到器材外**（高）。`lensHitY()` 回的命中高 ＝ 物高 × f/(f−u)，f=2 時
   實測 u=1.2／2.5／3 各落在 5／−8／−4 格，而鏡（透鏡）半高只有 3 格。**u=3 是常用值**。
   現行只有「超過畫布高 4 倍才不畫」的門檻，與器材口徑無關。**透鏡頁與面鏡頁都中**。
   選項：(a) 落點超出口徑就不畫這一條 (b) 截在鏡緣但不轉折 (c) 照現況畫在空白處。
2. ~~**虛像時①的反向延長線不與另兩條交會**（中）。凹面鏡 u=1 偏 1.333 格、凸面鏡 f=−2 偏 0.222 格。~~
   **已由 §10 的 A 方案自動解決**（2026-09-10 同日）：落點回到 `cx` 之後，`axisEnd()` 與
   `outEnd()` 變成同一條線，實測凸面鏡三條反向延長線偏離像點 0.0001 格。不需另外處置。

審查同時獨立確認：需求「反射點在曲面上」＋「①精確過焦點」＋「u=f 出射平行」三者
**在數學上不可兼得**——①的出射斜率恆為 h/(f−a)、②恆為 h/u，兩者相等 ⟺ 矢高 a=0。
與程式怎麼寫無關。

### 舊記錄：第 13 項提出時的三個選項

使用者原話：「我發現面竟成像你用的公式是球面鏡的，難怪會有誤差。你幫我判斷用球面鏡好還是拋物面鏡好」。

**澄清**：`1/u + 1/v = 1/f` 不是球面鏡專屬，是**近軸近似**，球面鏡與拋物面鏡共用。程式裡唯一借用
球面幾何之處（法線指向曲率中心 2f）在階段 2 已改成拋物面法線，鏡面弧本身也已是拋物線。
**所以「換成拋物面鏡」這件事已經做完了**，誤差不來自鏡面形狀。

**本次獨立複驗**（f=3、u=6、物高 2 格，頁面實跑）：

| 項目 | 課本近軸公式（現況） | 拋物面真實反射 |
|---|---|---|
| 像距 | 6.000 格 | 5.400 格 |
| 放大率 | −1.000 | −0.900 |
| ①是否通過焦點記號 | 否，偏 0.118 格 | 是，精確 |

與 2026-09-08 `scratchpad/mirror-check.js` 的記載完全一致。**模型建議：維持現況**——「物在 2F →
像在 2F、倒立同大」是國中課本的核心結論，畫面上讓它變成 0.90 倍的代價，遠大於一條光線偏離焦點
記號 0.118 格。**仍等使用者裁示**。

## 11. 2026-09-10 第五批：動畫擴到四頁、面鏡法線改規則、針孔發光點拆開（5 項）

使用者單則訊息含 5 個編號需求，全部實作完成。四個決策點由 AskUserQuestion 選定（**B**）。

### 11.1 發散光的虛線改走「共同進度」（**A**）

使用者原話：「通過鏡面後跑一小段距離就 2 條虛線一起延伸，同時抵達虛像的成像點形成虛像」。

`clipByAnim()` 原本把虛線接在該條實線之後照距離累加，於是虛線要等出射線走到畫布邊才開始長，
三條又長短不一、先後抵達，看不出交會的那一刻。改成：每條虛線在自己的出射段跑滿一小段之後開始長，
收束時刻取**共同值**（各條開始時刻加上自己的虛線長度，取最大），路程長的走得快、短的走得慢。

識別符 = 值：`ANIM_LEAD = 1`（格，出射之後再走多遠虛線才開始）。
`ANIM_GRID_PER_SEC = 4`（每秒走幾格）**未改**。

實測（凹透鏡 f=3、u=6、含完整光束，量各條虛線末端到像點的距離）：

| anim（px） | 特徵虛線末端到像點 | 光束虛線末端到像點（最小～最大） |
|---|---|---|
| 180 | 2.115／1.804 格 | 1.809 ～ 2.742 格 |
| 240 | 1.082／0.923 格 | 0.925 ～ 2.694 格 |
| 300 | 0.049／0.041 格 | 0.042 ～ 0.122 格 |
| 303（共同收束點） | 0／0 格 | 0 ～ 0 格 |

凸面鏡虛像 16 條、凸面鏡平行光 18 條，同樣在收束點那一刻全部歸零。
凹透鏡虛像只有 2 條特徵虛線（②的反向延長線落在物與鏡之間，與實際光路重疊所以不畫），
正好對應使用者說的「2 條虛線」。

### 11.2 完整光束納入動畫、虛像時往回延伸（**A**）

使用者原話：「完整光束開啟後產生的那些光，如果會形成虛像的話也要向後延伸。
如果開啟完整光束並按下播放鍵，這些多產生的光線也會有動畫」。

完整光束原本是路徑字串、直接 append，不進裁剪也沒有反向延長線。改成與三條特徵光線相同的
端點結構，段上加 `beam` 標記走另一組輸出（細而淡）；`ray` 編號從 100 起跳，與特徵光線分開累加
距離但**共用同一個時鐘**。虛像時每一條都補一段反向延長線，與特徵虛線一起同時抵達。

### 11.3 四頁都有光路動畫（**A**，推翻 §9 的 B 裁示）

使用者原話：「每一頁的每個模擬都要有光路動畫」。修訂鏈：09-10 **B**「先做透鏡頁一頁，驗過再擴」
→ 同日 **A**「每一頁都要」（後者生效）。

| 頁 | 作法 | 像出現的時機 | 實測總長 |
|---|---|---|---|
| 0 針孔 | 每道光一個 ray，被擋的走「細而淡」那一組 | 最後一道光打到紙屏（`revealAtEnd`） | 13.45 格 |
| 1 面鏡 | 與透鏡頁共用 `drawRays()`，只放行開關 | 三條交會處，或虛線共同收束點 | 25.23 格 |
| 2 透鏡 | 上一批已做 | 同上 | 25.23 格 |
| 3 眼睛 | `drawEyeRays()` 由路徑字串改成端點結構 | 光走到交會處 | 13.74 格 |

`animPlayable()` 由 `state.screen === 2` 改成 `animTotal > 0`；`animTotal` 是上一次 `render()`
算出來的，所以 `render()` 結尾要再同步一次按鈕狀態，不然按鈕會慢一拍。
`animGuard()` 的參數指紋擴充到四頁全部會改變光路的參數（孔徑、孔形、孔數、發光點、擋板位置、
紙屏位置、紙屏開關、眼型、眼鏡、度數、眼睛頁三條光線）。
針孔頁三個發光點都不勾時仍要跑一次空的裁剪，否則 `animTotal` 會留著上一幀的值。

### 11.4 針孔頁的發光點拆成三個（**A** ＋ **B**）

使用者原話：「讓我可以分開顯示從頂點發出的通過針孔光、中點發出的通過針孔光、底部發出的通過針孔光。
另外可以勾選完整光束，會根據我選了哪些點，顯示出其他有發出但是被針孔板擋住的光」。

**B** 選定「頂端／中段／底端，只開頂端」與「完整光束維持原名」。
**這推翻 §2.3 頁 1 第 6 點**「單點光束示範開頂端那一點、完整光束再補上底端與中段」。

- `state.dot`（單一布林）→ `state.pinPts`（三個布林，預設 `[true, false, false]`）
- 新增左欄群組「發光點」（`#pinPtGroup`，只在針孔頁顯示），三個核取 `chkP1`／`chkP2`／`chkP3`
- 中段的高度由物高的 0.55 倍改成 **0.5 倍**（名稱既然叫「中段」就取真的中點）
- 「完整光束」在這一頁改成只控制**被擋板擋住的光**；沒勾就整組不畫，圖例那一列標「（已關閉）」
- 三處預設值必須一致：HTML 的 `checked`、`initState()`、重置清單（DESIGN_SPEC 第 4 節）

實測：發光點三開時被擋的光 30 段、只開頂端時 10 段；不勾完整光束時 0 段。

### 11.5 面鏡法線只在平行光模式畫，平行光真的在鏡面反射（**A**，推翻 §2.7 定案）

使用者原話：「蠟燭、箭頭、f 的光線就不要畫法線了，因為為了達到像的正確性畫法線沒意義。
平行光的部分我就希望可以讓他在鏡面反射，並且畫出法線，因為這沒有成像大小、位置的問題」。

**推翻 §2.7 定案**「三條特徵光線打在鏡面的那一點都畫法線，含與主軸重合的第②條」。
修訂鏈：09-06 **B**（選 (b) 照畫並在圖例註明）→ 09-10 **A**（後者生效）。
理由：三條特徵光線的落點在通過鏡心的垂直線上（§10 的 A 方案），那個位置沒有鏡面，
法線是一條沒有依據的垂線。

`hitX` 因此分成兩支：平行光模式（且面鏡、且有限焦距）落在鏡面弧上，其餘仍回 `cx`。
兩者不會同時出現在畫面上——平行光模式把三條特徵光線整組關掉。

**拋物面在這裡是精確的**：`x = cx − (y−ax)²/(4fg)` 的焦點恰好是 `fx`（代入 u²=4pX 得 p=−fg，
面鏡的 `outSign()=−1` 讓 `fx = cx − fg`，兩者同一點）。所以平行光落在弧上、依真實反射律反射之後
**精確通過焦點**，出射線的式子一個字都不必改；法線方向 ∝ (1, (y−ax)/(2fg)) 就是拋物線的真法線。

實測（凹面鏡 f=3，解析畫出來的 path 座標）：

| 入射高度 | 落點的矢高 | 落點與拋物線的偏差 | 出射線在焦點處的高度 |
|---|---|---|---|
| 2.46 格 | 0.504 格 | 0.00004 格 | −0.00019 格 |
| 1.23 格 | 0.126 格 | −0.00005 格 | 0.00020 格 |
| 0 格 | 0 格 | 0 格 | 0.00014 格 |

蠟燭／箭頭／F 三種造型在平面鏡、凹面鏡、凸面鏡下，法線條數均為 0；轉折點均在鏡心（0 格）。
圖例卡的「法線（垂直鏡面）」那一列移到平行光分支，「②的法線與主軸重合」那一列刪除。

### 11.6 第三條光線落點超出器材：照現況（**B**）

上一批提出、本批裁示。落點高度 = 物高 × f/(f−u)，f=2 格時物距 3 格就落在 4 格處，
而透鏡與面鏡的半高只有 3 格。**B** 使用者選定「照現況，畫在器材外」。
作圖法的三條線都保留，代價是那個轉折點沒有實體。透鏡頁與面鏡頁均維持不動。

### 11.7 順手修的既有缺陷（**C** 模型判斷，使用者未表態）

從面鏡頁選著「平行光」切到針孔頁時，`state.shape` 停在 `parallel`，而針孔頁的平行光 chip 是
收起來的 → 四顆 chip 全不亮、物體也畫不出來（`OBJ_HALF_W.parallel = 0`）。
與眼睛頁那一條（`if (n === 3 && state.shape !== 'candle') pickShape('candle')`）是同一型，
補上針孔頁的對應處置。**本輪之前就存在，不是這批改動造成的。**

## 12. 2026-09-11 第六批：抽屜回歸規範、交界線、署名回流、上線（4 項）

### 12.1 抽屜的大小與按鈕以 dissolution-sim 為準（**A**）

使用者原話：「播放列抽屜的大小、按鈕等我發現和溶解度模擬的不一致，我希望以溶解度模擬的為準，
他的高度是最小高度。**如果我要求加入元件，一定要先看有沒有既有規範。**」

上一批（09-10）加抽屜時，我照抄 `motion-graphs-sim/index.html:262-277`，**沒有回頭查 `_shared` 規範**。
`DESIGN_SPEC.md` 第 1 節「抽屜盒模型」早就訂了定值，而 motion 那一份是 2026-08-26 使用者為它單獨
指定的壓縮值。照抄等於把例外擴散出去。派 fresh-context agent 盤點四份實作後逐項改回：

| 屬性 | 規範值 | 改前（optics） | 改後 | 與 dissolution |
|---|---|---|---|---|
| `margin` | `12px 14px` | `8px 14px` | `12px 14px` | 相同 |
| `padding` | `10px 20px 12px` | `6px 18px 8px` | `10px 20px 12px` | 相同 |
| `border-radius` | `12px` | `12px` | 不變 | 相同 |
| `.row` gap | `20px` | `20px` | 不變 | 相同 |
| 盒高／含 margin | — | 60／76 px | **68／92 px** | 相同 |
| `.seg button` min-width | — | `0` | `56px` | 相同 |
| `.seg button` padding | — | `0 10px` | `0 14px` | 相同 |

另外三處偏離同時修正：

1. **重置鈕搬回抽屜**。規範第 1 節寫明抽屜放「播放／暫停、單步、速度、時間軸捲軸，
   **以及跟著它們走的重置**」。改成 `.round-btn` 圓鈕，與 dissolution 的兩顆並排。
2. **叫出設定面板的 chip 搬回抽屜**。規範第 1 節：「有抽屜的模擬放抽屜裡，沒抽屜的放動作列裡」。
3. **動作卡整個移除**。規範第 1 節寫明動作卡是**沒有抽屜**的模擬才用的元件（`Density_experiment`
   是那個情形）。連帶 `main` 的 `grid-template-rows` 由 `1fr auto` 改回 `1fr`、`.stage` 的
   `grid-row` 由 `1 / span 2` 改回 `1`、`.btn` 與 `.btn:hover` 整組刪除。

`.seg` 的滿寬規則（2026-09-07 **A**「seg元件一律佔滿寬度」）**沒有被推翻**：那一條講的是左欄裡的
切換器要左右對齊，抽屜是橫向排列，撐滿只會把速度切換器拉成整排最長的東西。抽屜裡的例外
（`width:auto` ＋ 區段固定 56／14）寫在 `.speed .seg` 那兩行，與 dissolution 逐字相同。

**方法論**：使用者這一句「加入元件前一定要先看有沒有既有規範」已回流提議寫進 handbook，見交付說明。

### 12.2 折射／反射的交界線（**A**）

使用者原話：「透鏡成像、面鏡成像的蠟燭、箭頭、f 可以像 phet 的圖一樣用淺淺灰色的垂直線畫
反射／折射的交界線」。

在通過鏡心的平面上畫一條垂直線（`renderOptic` 的第 3b 層，畫在器材之下）。
這正好補上 §10 A 方案留下的視覺代價：轉折點在垂直平面、鏡體畫在矢高處，f=2、高 2 格時光線
在鏡體後方約 0.28 格處轉彎；有了這條線，那個轉折就有依據，也是 PhET 的緩解做法。

- 識別符 = 值：`C.plane = '#9FA8B5'`，線寬 `3 * G.sc`。
- **顏色不能更淡**：鏡心那一格本來就是主格線（格線以鏡心為基準每 5 格加深），
  初版取 `#C3C9D2` 比主格線 `#A6AEBA` 還淡，畫出來只像「那條主格線變淡了」，實測目視分不出來。
  現值比主格線深一級、比主軸 `#7A8496` 淡。
- 平行光模式**不畫**：那個模式的面鏡是真的在弧上反射（§11.5），再畫一條垂直交界面會自相矛盾。
- 圖例卡加一列「光轉向的平面（折射／反射面）」，操作說明加一段。

### 12.3 版本與作者署名回流成共用規範（**A** ＋ **B**）

使用者原話：「打板標＋作者的部分回流到共同規範，並統一調整現有的模擬」。
版號由 **B** 選定「都標 v1.0.0」。

來源實作：`optics-sim/index.html:161-166`（CSS）、`:623-624`（HTML）、`:707-710`（常數）、
`:2965-2979`（注入）。**逐字搬**，四處落點：

| 檔案 | 加了什麼 |
|---|---|
| `_shared/design/DESIGN_SPEC.md` 第 1 節 | 新增「版本與作者署名」條目（含定值、窄螢幕處置、常數注入的要求與來源標註） |
| `_shared/design/layout.html` | 導覽列示意加兩行署名 |
| `_shared/design/components-spec.html` | 導覽列元件示範加署名，說明文字補一段 |
| `_shared/template/index.html` | CSS ＋ HTML ＋ `APP_VERSION`／`APP_AUTHOR` 常數 ＋ 初始化注入 |
| `Density_experiment`、`motion-graphs-sim`、`dissolution-sim` | 同上四處，各自 `v1.0.0` |

實測三份（1366×768）：文字 `v1.0.0` ／ `by 宏福` 兩行、色 `rgb(157,170,187)`、
字級 `12.075px`（＝10.5 × u 1.15）、距導覽列右緣 18px、console 無錯誤。
640px 時 `display:none`，768px（iPad 直向）維持顯示。

### 12.4 上線（**B** repo 名）

`optics-sim` 先前沒有 GitHub remote。**B** 使用者選定 repo 名 `optics-simulator`，
比照另外三個的 `-simulator` 尾綴。公開 repo ＋ GitHub Pages（免費方案的 Pages 只能用公開，
2026-08-24 **A** 裁示採公開）。

**收尾閘**（`judgment.md` §7）：本 session 有「待 iPad 與教室大屏實測」的核心交付物，
所以**部署可做**（真機驗證的前提就是先上線），但版號升級、git tag、GitHub Release、
CHANGELOG 標完成、「測試通過」類 memory 這五項降為待辦。四個模擬的 `v1.0.0` 是**首次標註**
而不是升版，且由使用者當下裁示，不受該閘阻擋。

## 13. 2026-09-13 新增頁籤「光的折射」（鳥看魚／魚看鳥；09-14 頁籤標題改為「視深」）

使用者原話（**A**）：「我希望光學模擬器可以多一個頁籤，內容是折射，放在面鏡成像和透鏡成像之間」
「我希望可以模擬出鳥看水中魚以及魚看空中鳥的視深與實深，可以明確呈現光路與形成的虛像」。
規劃全文：`C:\Users\Hope\.claude\plans\svg-dazzling-eclipse.md`（2026-09-13 寫、2026-09-14 經 ExitPlanMode 核可）。
分兩階段：**階段 1** 頁籤＋場景＋兩情境的靜態光路與虛像；**階段 2** 拖曳、完整光束、動畫實測、重置、說明。

### 13.1 已定案

| 項目 | 裁決 | 來源 |
|---|---|---|
| 頁籤位置 | 面鏡成像與透鏡成像之間 | **A**「放在面鏡成像和透鏡成像之間」 |
| 情境呈現 | 左欄 `.seg`「鳥看魚｜魚看鳥」，一次一組光路與一個虛像 | **B**「.seg 切換（推薦）」 |
| 虛像位置 | 真實光路：進入觀察者眼睛的兩條光線反向延長，交點＝虛像 | **B**「真實光路（推薦）」 |
| ~~可拖~~ | ~~鳥與魚都能上下左右拖~~（**已被下方 09-14 的 A 推翻**） | **B**「鳥和魚都能上下左右拖（推薦）」 |
| 數值 | 只標名稱不標數字（守 §2.2） | **B**「只標名稱不標數字（推薦）」 |
| 頁籤標題 | **視深**（取代「光的折射」；程式碼註解仍以「光的折射頁」稱呼） | 2026-09-14 **A**「頁籤標題改成視深」 |
| 可拖（修訂） | **鳥與魚只能水平移動**，高度與深度固定在預設值。修訂鏈：09-13 **B**「鳥和魚都能上下左右拖（推薦）」→ 09-14 **A**（後者生效） | **A**「魚和鳥能水平動就好」 |
| 格線 | 與其他四頁對齊（以主軸高度為基準），切換頁籤時格線不跳動 | 2026-09-14 **A**「方格線我希望能對準其他的頁面」 |
| 水面位置 | 主軸往下 1 格的那條格線（`REFR_SURF_DROP = 1`）；1366×768 下水面以上 5.12 格、以下 3.12 格 | 2026-09-14 **B**「主軸往下 1 格（推薦）」 |

### 13.2 素材驗收（2026-09-13／09-14 實測）

| | 魚 `圖畫.svg`（09-13 23:09 重存，85 KB） | 鳥 `鳥.svg`（62 KB） |
|---|---|---|
| 外觀 | 側面、頭朝左 | 正面展翅海鷗、左右對稱 |
| viewBox | `162.24016 × 80.490814` | `158.12759 × 49.868801` |
| 兩層 g 的累積位移 | `translate(-20.361979,-50.60387)` | `translate(-19.764593,-44.26229)` |
| 整體外輪廓 | `path12` #ffffff（單一封閉） | `path5` #b1b7c2（單一封閉） |
| 參考點 | 魚眼中心 `(21.02, 37.48)`，半徑 4.76 | 兩眼 `(75.95, 16.5)`／`(82.25, 16.5)` → 取中點 `(79.1, 16.5)` |
| style | 只有 fill、`display:inline`，無 `fill-rule`、無漸層濾鏡 | 同左（另有 `fill-opacity:1`，無作用） |

- 魚的 `path5`（#457da5，69 塊、佔全檔約 60%）是魚鰭與外框之間的細縫填色，與 `path6` 幾乎同色；拿掉會露白縫，保留。
- 檔名 `圖畫.svg` 沿用不改：**圖畫.svg＝魚**。

### 13.3 物理模型與驗算

水面 y=0、往上為正；水 n=4/3。光線與水面的交點以二分法解 `nS·sinθS = nE·sinθE`。
兩條光線射向觀察點兩側各 `REFR_PUPIL/2`（垂直於中心光線），反向延長的交點＝虛像。
**口徑幾乎不影響交點**（node 實算：口徑 0.3／0.6／1.0 格時交點差 ≤ 0.02 格），所以口徑是純視覺參數。

| 實深／實高 3 格、觀察者離水面 3 格 | 水平錯開 0 | 2 | 4 | 6 | 8 |
|---|---|---|---|---|---|
| 鳥看魚：視深（格） | 2.25 | 2.05 | 1.55 | 1.01 | 0.60 |
| 魚看鳥：視高（格） | 4.00 | 4.40 | 5.80 | 8.92 | 15.02 |

**已知後果**：魚看鳥一斜看，虛像就衝出畫面（越接近斯涅爾窗邊緣放大越劇烈，真實物理）。

### 13.4 模型判斷（C，使用者未表態）

1. 新鈕 `data-screen="4"`，DOM 排在面鏡與透鏡之間；既有 0–3 不重新編號（30 多處 `state.screen === N` 不必重驗）。標籤「光的折射」（09-14 **A** 改為「視深」，見 §13.1）
2. 場景：鳥在左上空中、魚在右下水中；兩情境**共用**位置，切 `.seg` 只換誰發光、誰觀察。魚頭一律朝向鳥
3. 魚寬 2.4 格、鳥寬 3.0 格。~~水面在光路區由上往下 57%（`REFR_SURF_FRAC`），主格線以水面為基準~~
   → **09-14 推翻**：格線改回以主軸為基準（**A**），水面在主軸往下 1 格（**B**），見 §13.1。
   預設位置鳥 `{x:-1.0, h:2.0}`、魚 `{x:0.5, d:2.2}`（水平錯開 1.5 格）。**計畫原寫「錯開約 2.5 格」，階段 1 實測推翻**：
   1366×768 水面以上只有 4.69 格、以下 3.54 格，錯開 2.5 格時魚看鳥的虛像頂端出界；
   64 組候選（鳥高×錯開×魚深各 4 檔）只有錯開 1.5、鳥高 2 這一帶同時滿足「虛像不出界、魚不沉底、視深 ≤ 0.75 實深」
4. 參考點：魚＝魚眼、鳥＝兩眼中點；發光與觀察都用這一點，實深／視深量到這一點
5. 法線：兩個折射點各一條垂直水面的黑虛線（§2.5 第 4 點之③），常駐不設開關
6. 完整光束（階段 2）：從參考點發出、**沒進入觀察者眼睛**的光，淡色、無反向延長線；水中發光時扇形限 ±40°（避開全反射）
7. 虛像：外輪廓灰虛線框＋`IMG_OPACITY` 填色；整隻動物的像用**以參考點為錨的線性映射**
   （參考點的像固定＝虛線交點；方向與伸縮由包圍盒四角＋四邊中點各自求像、最小平方擬合）。
   **計畫原寫「包圍盒三個角擬合、第 4 角誤差 <0.1 格」，階段 1 實測推翻**：斜看時非線性明顯
   （鳥看魚錯開 4 格差 0.23 格、魚看鳥錯開 6 格差 3.99 格），且角點擬合出來的像，眼睛不保證落在虛線交點。
   中間試過「在參考點取微分」：眼睛對了，但翅尖差到 0.90 格，所以改成錨點＋最小平方
8. 出界：虛像在光路區外 → 不畫像、虛線畫到邊界、邊緣箭頭＋「虛像在畫面上方更遠處」；**不**夾限可拖範圍去遷就像
9. 標示：鳥看魚「實深／視深」、魚看鳥「實高／視高」，垂直量測線＋端點短橫，無數字。
   兩條並排在動物**遠離觀察者**那一側（內側量實的、外側量像的），各拉淡灰引線回到量的那一點；
   **初版畫在參考點正上／正下方，階段 1 實測名稱被虛像蓋住**，因此改擺
10. 水：DESIGN_SPEC 第 3 節水色取平塗 `#DBEAFE`、水面線 `rgba(42,111,219,.45)` 2px。水色會把格線對比壓到下限以下
    （次格線 1.15、主格線 1.83），水域內改用 **次 `#C1C5CC`（1.42）／主 `#959DA8`（2.25）**——在線性空間等比壓暗既有格線色求得
11. 本頁沒有：主軸、焦距、物距拖桿、特徵光線核取、物體造型、遮板、紙屏與紙屏小窗

### 13.5 撞到既有定案的兩條（2026-09-14 **B**「兩條都定案（推薦）」）

> 修訂鏈：09-14 早上隨計畫一併核可、未逐項表態（C）→ 同日使用者實測後以 AskUserQuestion 選定（B，生效）。

- **本頁不設紙屏**——撞到 §2.2 **B**「四頁都有（推薦）」：沒有實像可接，觀察者本身就是眼睛
- **鳥、魚配色比照 §2.11 蠟燭豁免** DESIGN_SPEC 色票（兩張共 10 個色）

### 13.6 本次刻意沒做

| 限制 | 理由 | 解除條件 |
|---|---|---|
| 入射角／折射角標示 | 需求是視深與虛像，角度標示會加線條 | 使用者要求 |
| 全反射 | 國中此單元不含；完整光束扇形限在臨界角內避開 | 使用者要求 |
| 介質／折射率可選 | 未要求 | 使用者要求 |
| 「觀察者看到的樣子」小窗 | 使用者 09-08 刪過眼睛頁同類小窗（§6 第 1 項） | 使用者要求 |

### 13.7 進度

- [x] 階段 1：頁籤＋場景＋兩情境靜態光路與虛像 —— 2026-09-14 實作與瀏覽器驗證完成；**使用者實測過（A「已測試」）**，
      同一則訊息附三項修改（頁籤改名、只能水平移動、格線對齊其他頁），併入階段 2 做
      - 素材：`鳥.svg`、`圖畫.svg` 各 5 條 path 以 scratchpad 的 node 腳本逐字內嵌（147 KB 路徑資料無法手打），比對 10 條、差異 0
      - 物理（1366×768 模擬）：鳥看魚錯開 0／2／4／6 格 → 視深 2.247／2.041／1.541／0.998；
        魚看鳥 → 視高 4.005／4.406／5.818（出界）／8.934（出界）；兩折射點斯涅爾誤差 ≤ 2.2e-16；像點到兩條虛線 0 px；
        虛像眼睛落在交點 0 px；像的 8 點最大偏差：鳥看魚 0.05～0.13 格、魚看鳥預設位置 0.67 格（非線性拉伸，線性映射的極限）
      - 回歸：四頁格線路徑與改動前演算法逐字相同；凸透鏡 v=7、m=−1.333；凹透鏡 v=−1.909；平面鏡 `5.3 格`；針孔 1／3／5 道；
        透鏡頁開著紙屏切到折射頁不出現紙屏小窗；導覽列 768 與 640 寬不溢出；console 無錯誤
      - 本階段刻意收起：左欄「顯示」組（完整光束屬階段 2）；折射頁的畫布拖曳一律不接（pointerdown 直接返回）
- [ ] 階段 2：拖曳、完整光束、動畫、重置、說明 —— **2026-09-14 實作與瀏覽器驗證完成，待使用者實測後打勾**（併入使用者 09-14 三項修改）
      - 頁籤標題「視深」；鳥與魚只能水平拖（畫布本體或藍框握把），抓取偏移保留不跳位，整隻夾在畫布內、左右各留 0.1 格；
        魚跨過鳥的正下方就翻面。鳥的握把在朝向魚那一側的翅尖外（正下方是光線、正上方是魚看鳥的虛像），魚的在身體正下方〔C〕
      - 格線與另外四頁逐條相同（直 22、橫 9 條）；水面在主軸往下 1 格（`REFR_SURF_DROP = 1`），1366×768 上 5.12／下 3.12 格
      - 完整光束：兩情境各 10 條（與進眼光線方向太近的略過），水面轉折處符合折射定律（px 取整誤差 0.0001）；水中發光扇形 ±40°、空中 ±65°〔C〕
      - 動畫（逐點設定 `state.anim` 驗證）：光到水面前沒有法線、之後才有；虛線抵達前 5 px 沒有虛像與「視深」，抵達後一起出現。
        **實際播放沒能在 Browser pane 內跑完**：pane 在背景時 `document.hidden === true`，`visibilitychange` 把動畫暫停（pitfalls-services #3 已載），待使用者實測
      - 真實滑鼠拖曳：握把 +2.91 格、鳥高不變；抓鳥身拖回 −1.000（偏移 −0.013 格）；魚拖到右界停在 8.811（＝算出的上限）；
        鳥往正下方拖 x、h 皆不變；拖到畫布外的導覽列仍持續跟隨、放開後結束
      - 重置留在本頁並回預設；左欄在 U=1.15 無捲動（透鏡頁 −33 px）；四頁回歸（格線逐字相同、v=7／−1.909、`5.3 格`）；
        魚看鳥錯開 6 格虛像高 12.19 格 → 出界提示；console 無錯誤
- [ ] iPad 與教室大屏實測：水域格線對比、鳥與魚在 6 公尺外的辨識度、二維觸控拖曳
- [ ] 升版與 Release —— 2026-09-14 收尾時 **B** 選「⑤ 部署與版號」「⑥ Release Notes」：本次只 commit ＋ push 上線；
      依收尾閘（handbook judgment §7），階段 2 使用者實測前不升版、不打 tag、不發 Release。實測通過後補做：
      - `APP_VERSION` `v1.0.0` → `v1.1.0`〔C：新增頁籤屬次版號〕
      - GitHub Release 草稿〔C，未發佈〕，標題「v1.1.0 新增『視深』與『色光』頁籤」
        （2026-09-18 收尾 **B** 選「⑤ 部署與版號」「⑥ Release Notes」：色光頁併入同一版，兩頁都待實測，仍不升版、不發佈）：
        - 新增頁籤「視深」，排在面鏡成像與透鏡成像之間：鳥看水中的魚、魚看空中的鳥
        - 光線在水面依折射定律轉彎；進入眼睛的兩條光往回延長，交點就是看到的虛像，並標出實深與視深（實高與視高）
        - 鳥與魚可以左右拖曳；斜著看時魚看起來更淺、鳥看起來更高，虛像超出畫面時標出方向
        - 支援播放動畫與「完整光束」
        - 新增頁籤「色光」，排在最後：紅、綠、藍光可複選，「白光」一鍵三色全開
        - 不透明體：光照到色紙，只有同色的光被反射進眼睛，其餘被吸收；色紙有紅、綠、藍、白、黑五色
        - 透明體：光穿過最多三片紅、綠、藍玻璃紙，每片只讓同色光通過
        - 右下小窗顯示眼睛看到的顏色，左下與右下的三原色圈圈分別是發出的光與眼睛接收的光，光源方框顯示混合後的顏色
        - 左下卡片寫出每一種光被反射、通過或吸收；支援播放動畫

### 本專案尚未產生的檔案

`HANDOFF.md`（四頁全部完成，接下來是部署與真機實測，需要時再寫）、
`design_handoff/`（Design 出稿後放，已被 `.gitignore` 排除）

---

## 14. 2026-09-18 新增頁籤「色光」（色光與物體的顏色）

使用者原話（**A**）：「光學模擬我希望可以增加一個頁籤，是用來模擬色光與顏色的。可以切換透明體與不透明體的呈色原理。光可以選紅、綠、藍三種顏色」。
2026-09-18 14:25 模型提出 10 題，14:42 使用者以編號回覆 1–9（原文「1.a 2.a 3.c 可3片 4.a 5.a 6.色紙 7.a 8.a 9.a」），第 10 題未回覆。

### 14.1 已定案

| 題 | 項目 | 裁決 | 來源 |
|---|---|---|---|
| — | 呈色原理 | 左欄可切換**透明體／不透明體** | **A**「可以切換透明體與不透明體的呈色原理」 |
| 1 | 頁籤位置與名稱 | 排**最後**（眼睛之後），標題「**色光**」 | **B** 選項原文「放最後（眼睛之後），標題『色光』（推薦）」 |
| 2 | 光源 | 紅、綠、藍**三顆獨立開關，可同時開**，疊出黃、青、洋紅、白 | **B**「三顆各自開關，可同時開，疊出黃、青、洋紅、白（推薦）」 |
| 3 | 透明體 | **不畫光屏，眼睛直接透過玻璃紙看**；玻璃紙**最多可疊 3 片** | **B**「同 A，但不畫光屏，改由眼睛直接透過玻璃紙看」＋ **A**「可3片」 |
| 4 | 不透明體 | 光照到物體→反射光射向眼睛；被吸收的光線停在物體表面；**色紙塗它本來的顏色**，看起來的顏色由小窗、示意圖、結論卡呈現。修訂鏈：09-18 14:42 **B**「物體畫成它看起來的顏色」→ 09-18 22:22 **A**（後者生效） | **B**「光照物體→反射光射向眼睛…被吸收的光線停在表面（推薦）」＋ **A**「我希望色紙呈現的是他本來就該有的顏色，不是他照到光呈現的」 |
| 5 | 可選顏色 | **紅、綠、藍、黑、白，共 5 種**（物體與玻璃紙皆同）。修訂鏈：09-18 14:42 **B**「紅、綠、藍、黃、青、洋紅、白、黑 8 種（推薦）」→ 09-18 14:45 **A**（後者生效） | **A**「顏色先3原色+黑白就好」 |
| 6 | 不透明物體外觀 | **色紙** | **A**「色紙」 |
| 7 | 顏色模型 | **理想三原色**：只看物體含不含 R、G、B 三個成分 | **B**「理想三原色：只看物體含不含 R、G、B 成分，課本講法（推薦）」 |
| 8 | 動畫 | **要**，和其他頁籤一樣用抽屜的播放鈕 | **B**「要，跟其他頁一樣用抽屜播放鈕（推薦）」 |
| 9 | 文字結論 | **要**，例如「綠色物體：反射綠光，吸收紅光、藍光」 | **B**「要（推薦）」 |

| — | 「眼睛看到的顏色」小窗 | **要**，常駐右下（借用紙屏小窗的位置），內容＝一整塊看到的顏色＋顏色名；播放中光沒走完前畫淺灰〔位置、內容、時機為 C〕。修訂鏈：09-18 14:42 第 4 題選 **B**「光照物體→反射光射向眼睛…（推薦）」、未選帶小窗的 C → 09-18 15:34 **A**（後者生效，只限色光頁） | **A**「請幫我多做一個小窗，呈現眼睛看到的顏色」 |

| — | 白光選項 | 色光膠囊加「白光」，與紅綠藍連動：三色全開時亮；按下＝三色全開，已全開再按＝全關〔連動規則 C〕 | **A**「多加一個白光，會和紅、綠、藍光的選項連動」 |
| — | 三原色混色示意圖 | 方格區左下「發出的光」、右下「眼睛接收的光」；深色底，三圓切成 7 塊、每塊直接塗 `COLOR_FILL` 的對應色（09-18 22:22 改；初版 `mix-blend-mode:screen`＋純紅綠藍疊出 `#FFFF00`，與光源方框、小窗的 `#F5D21C` 不同，**A**「眼睛看到的顏色、光源的顏色、三原色圈圈疊出來的顏色看起來是不一樣的」），沒有的色只畫虛線圈；播放中右圖等光走完才亮〔畫法 C〕 | **A**「在方格紙畫布的左下和右下角增加三原色三個圈圈疊加混色的示意圖，分別對應發出的光和眼睛接收到的光」 |
| — | 光源方框 | 燈罩中間方框塗發出的合成光（紅＋綠＝黃），全關為黑 | **A**「光源的灰色間可以有一個方框，會隨著射出的光線變色」 |

> 以上三項 09-18 15:51 同一則訊息提出。透明體的「光源」標字因此移到燈罩上方（左下角讓給示意圖）〔C〕；
> 示意圖框寬至少容得下標題（窄畫布上字比三個圈寬，實測 543 px 寬時被切）。

> 與 §6 的關係：§6 ① 推翻的是**成像四頁**的「觀察者眼睛」功能；本頁的眼睛是第 3、4 題 **B** 新定的，只適用色光頁，不回頭復活其他頁的眼睛。
> 第 4 題回覆 A，沒有選 C 的「看到的樣子」小窗，§6 ② 的否決仍然有效。

### 14.2 未回覆（第 10 題，原句重述）

> 「10. 明確不做（我預設都不做，要做請說）：稜鏡分光（白光→彩虹）、顏料減色混合、螢光燈／日光等不同光源光譜」

使用者未回覆 → 暫以 **C** 處理：三項都不做。使用者表態後改標 A／B。

### 14.3 模型判斷（C，使用者未表態）

1. 新鈕 `data-screen="5"`，DOM 排在眼睛之後；既有 0–4 不重新編號（沿用 §13.4 第 1 點的理由）
2. 版面：光源在左、色紙／玻璃紙在中、眼睛在右，眼睛固定、不可拖。紅、綠、藍三道光**分開畫成三條平行光線**，才看得出哪一條被吸收、哪一條通過；三條同時開時合成白光
3. 玻璃紙：3 個槽位，每槽可選「不放／紅／綠／藍」。修訂鏈：原寫「無或 8 色」→ 09-18 15:20 C「不放／紅／綠／藍／無色／黑」→
   09-18 15:27 **A**「玻璃紙刪除黑、白」（後者生效；色紙仍是五色）。每經過一片就只剩兩者共有的成分，停下的光線畫在那一片的入光面上
4. 物體色塊的「看起來的顏色」＝照射光 ∩ 物體成分；全被吸收就畫成黑色。透明體沒有物體可著色，改由射入眼睛的光線顏色呈現
5. 文字結論寫在畫布角落，不標數值（守 §2.2）
6. 不透明體的反射光線畫成射向眼睛的單一方向（課本示意圖的畫法），不做漫射扇形
7. 導覽列增為 6 顆，768 與 640 寬可能溢出 → 實作時先量測，溢出再提方案給使用者決定

### 14.4 階段切分（C，待使用者核可）

- [ ] 階段 1：頁籤＋左欄控制（透明體／不透明體 `.seg`、RGB 三開關、顏色選擇）＋**不透明體**靜態光路、物體著色、文字結論
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 導覽列第 6 顆「色光」（`data-screen="5"`）；左欄：物體 `.seg`（透明體鈕**先停用**，階段 2 接上）、色光三顆膠囊可複選、色紙 5 色單選
      - 場面：光源在左上（出光口三顆小圓顯示各色開關）、色紙平放中下（斜看的平行四邊形，塗「看起來的顏色」）、眼睛在右上（FA `fa-eye`，不可拖）〔C〕
      - 光源 `y=−2.0`、色紙 `y=2.2`（格，相對主軸）：原取 ±2.6，燈罩頂端貼到畫布上緣
      - 圖例卡三列一律用色塊（照射光／色紙本色／看起來的顏色），不用 `.ln` 線段——`.ln` 預設是橘色，會被讀成「橘光」
      - 驗證（1366×768）：8 組光×色紙的結論文字與色紙塗色全對（例：綠光照紅紙 → `#1C1C1C`、1 道光、「看起來是黑色：沒有光進入眼睛」；紅＋綠照白紙 → 黃）；
        動畫播到 40% 光線照截；重置留在本頁且膠囊回預設；切回透鏡／視深／針孔頁，圖例、「顯示」組、眼睛圖示皆正確；
        導覽列 768 與 640 寬不溢出；640 寬結論卡不超出畫布；console 無錯誤
      - 本階段刻意沒做：色紙在播放時「光到了才變色」（屬階段 3 動畫）
- [x] 階段 1 已 commit（`ae672e4`，2026-09-18 15:20 使用者指示「commit，接著做階段 2」；使用者實測狀態未表態）
- [ ] 階段 2：**透明體**：1–3 片玻璃紙疊加、逐片吸收、眼睛透過看
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 場面〔C〕：光源（主軸上、中央左 8 格）→ 三個槽位 `SHEET_X = [-4, 0, 4]` → 眼睛（右 8.6 格），三色光水平平行；
        玻璃紙畫成直立薄片（高 3.6、厚 0.4 格，厚度誇大才看得到顏色），半透明塗自己的顏色，下方標名稱
      - 左欄：「物體」切到透明體時「色紙的顏色」整組換成「第 1／2／3 片玻璃紙」三組膠囊（JS 依 `SHEET_CHOICES` 產生）；預設第 1 片紅、其餘不放〔C〕
      - 結論卡逐片寫「照到它的光」讓誰通過、吸收誰（多片時加「第 N 片」），最後一列「眼睛看到 X 色」
      - 驗證（1366×768）：無色＋紅＋綠 → 紅光停在第 3 片入光面（668 px ＝ 677 − 半厚）、綠藍停在第 2 片（488 px）、看到黑；
        三片都不放 → 白；黑 → 三道都停在第 1 片；關藍光後無色＋藍 → 黑；動畫 50% 照截；重置回不透明體與預設玻璃紙；
        切到透鏡頁再回來保留透明體；`?u=1.15` 左欄透明體不捲動（透鏡頁 33 px 為既有）；console 無錯誤
      - 順手清掉階段 1 不透明體光路裡沒用到的 `ends` 陣列
- [x] 階段 2 已 commit（`9a9d3e4`，含 09-18 15:27 **A**「玻璃紙刪除黑、白」）
- [ ] 階段 3：播放動畫（抽屜）、重置、另外五頁回歸、導覽列寬度
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 光線本身在階段 1、2 已經走 `clipByAnim()`，播放鈕、速度、改參數即回到完整顯示都是共用機制；重置與導覽列寬度在階段 1 已驗
      - 本階段補「結果出現的時機」〔C〕：~~不透明體的色紙在第一道光抵達前畫淺灰~~（09-18 22:22 色紙改塗本來的顏色後作廢；
        `COLOR_UNLIT = #DDE1E7` 仍用於小窗與結論卡的「光還在路上」，不用黑：黑是「照到了但全被吸收」）；兩種物體的結論卡最後一列（看起來是／眼睛看到）等光全部走完才出現，之前顯示「光還在路上……」
      - 說明彈窗「四個頁面都可以播」→「每個頁面都可以播」；導覽列註解「五頁」→「六頁」
      - 驗證（逐點設 `state.anim`）：不透明體 0／30% 淺灰、60% 起紅、99% 仍「光還在路上」、100% 與 null 顯示「看起來是紅色」；透明體 50%／100% 同理。
        **實際按播放沒能在 Browser pane 跑完**：pane 在背景 `document.hidden === true`、1 秒 0 個 rAF 畫格（pitfalls-services #3 已載），待使用者實測
      - 五頁回歸：各頁圖例首列、左欄組別、播放鈕可按、眼睛圖示只在色光頁；重置後凸透鏡 v=7、凹透鏡 v=−1.909、平面鏡「5.3 格」×2；console 無錯誤
      - 旁註（既有行為，本次未改）：從眼睛頁切回透鏡頁時物距沿用被眼睛頁夾過的 5.5 格（v=6.6），重置才回 5.25
