# 光的成像（optics-sim）— 專案進度

> **本檔是跨 session 的唯一權威進度來源。** 不要靠 memory 或對話記憶推斷進度。
> **來源標籤**：**A**＝使用者原話（附引文）｜**B**＝AskUserQuestion 選定（抄選項原文）｜**C**＝模型判斷（使用者未表態）。
> **只有 A、B 算定案**；C 類是我按規範與教學經驗抓的預設值，使用者隨時可以推翻。
>
> 建立於 2026-09-04（規劃 session）。

---

## 1. 專案基本資訊

| 項目 | 內容 | 來源 |
|---|---|---|
| repo 名 | `optics-sim` | **A**「optics-sim」 |
| 標題 | 光的成像｜國中理化模擬 | C |
| 形式 | **一支模擬、一個 repo、一個 `index.html`** | **A**「這幾個模擬是在同一個網站」 |
| 底部分頁 | 針孔成像｜面鏡成像｜**視深**｜透鏡成像｜眼睛 | **A**「透過底下導覽列就是針孔成像、面鏡成像、透鏡成像、眼睛這四個主題」＋ 2026-09-13 **A**「多一個頁籤，內容是折射，放在面鏡成像和透鏡成像之間」＋ 2026-09-14 **A**「頁籤標題改成視深」（見 §13） |
| 本機預覽 | `.claude/launch.json` 的 `optics`，port **4177**，serve root 限 `./optics-sim` | C |
| 部署 | GitHub Pages，`main` / root（尚未建 remote） | — |

> **架構修訂鏈**：規劃過程中曾一度定為「4 支獨立模擬，照課本四節」（**B** 選項原文「4 支（照課本四節）」），
> 隨即由使用者更正為「同一個網站、四個底部分頁」。**4 支那個版本作廢**，留在這裡是避免下一個 session 又把它拆開。

---

## 2. 已定案

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §2（全文原樣保留，章節號不變）。

## 3. 已回流 `_shared/` 的規範例外（2026-09-04 完成）

使用者裁示「**幫我回流**」（**A**），**已於 2026-09-04 全部執行**，母倉 commit `634b652`。
下表保留是為了記錄改了什麼、為什麼改——這三條現在是**全域規則**，不是本專案的特例。

| # | 改哪裡 | 要寫進去的內容 | 狀態 |
|---|---|---|---|
| X1 | `DESIGN_SPEC` 第 1 節 | 數據能直接標示在畫布上的模擬，允許**兩欄 `1fr_3fr`**（設定：畫布 ＝ 1:3，比例規則不變）。判準是即時讀數本來就長在圖形上——光學的物距、像距是光軸上的一段，搬進右欄卡片反而違反「讀數貼在它描述的圖形旁」那一條 | ✅ 已完成 |
| X2 | `DESIGN_SPEC` 第 2 節格線那條 | 格線**分主次**：次格線沿用 `#D5DAE1`（1.41）；格線當**判讀工具**時，每 5 格一條主格線用 **`#A6AEBA`**（白底 **2.24:1**、頁面底 2.09），仍不套 3:1 但明顯高於裝飾網底。另註明光軸沿用座標軸定案值 `#7A8496`（3.77） | ✅ 已完成 |
| X3 | `DESIGN_SPEC` 第 1 節 iPad 直向那條 | 沒有右欄的模擬，`<900px` 時**只有「設定」一顆 chip** | ✅ 已完成 |
| X4 | 不必改規範 | 物距用**畫布拖曳**不是滑桿，`DESIGN_SPEC` 第 4 節「物理參數滑桿一律離散檔位 3–7 檔」管的是滑桿，未違反。焦距用的是膠囊三檔，本來就合規 | 已釐清 |

`#A6AEBA` 標 **C**——使用者沒指定過主格線要多深，按「教室大屏遠看數得出格子、又不能蓋過光線」抓的。

---

## 4. 開發階段切分（每階段一個 session）

| 階段 | 內容 | 為什麼是這個順序 |
|---|---|---|
| 1 | 骨架（兩欄、無抽屜、動作卡、四分頁殼）＋共用引擎＋**頁 3 凸透鏡** | 凸透鏡情境最完整，引擎、格線、光屏模糊、拖曳、觀察者眼睛全在這階段磨好 |
| 2 | 頁 3 凹透鏡＋可拖遮板 | 共用階段 1 的引擎，只換符號約定 |
| 3 | 頁 2 面鏡三種 | 同一套成像公式，換器材外觀與光線同側 |
| 4 | 頁 1 針孔 | 引擎不同（相似三角形＋離散光線），但光屏模糊模型共用 |
| 5 | 頁 4 眼睛 | 凸透鏡引擎的延伸應用，最後做 |

〔C — 使用者尚未表態，規劃時他說「都先不要做，繼續問我問題」。〕

---

## 5. 進度

### 已完成（2026-09-04 規劃 session）

- 需求討論完畢，四個分頁的畫布內容、左欄控制項、迷思概念功能全部定案（見第 2 節）
- 六步檢查表：① `optics-sim/` 建立、複製 `_shared/template/index.html` ② 母倉 `.gitignore` 加 `/optics-sim/` ③ 母倉 `.claude/launch.json` 加 `optics`（port 4177，serve root 限本目錄）④ 專案 `.gitignore` 寫 `design_handoff/` ⑤ `git init -b main` ⑥ 本檔建立
- 順手把先前 session 遺留的共用規範用詞修訂 commit 掉（母倉 `b77aeef`，「播放速度」→「動畫速度」，六個檔，本 session 一行都沒改）
- **X1–X3 回流完成**（母倉 `634b652`）：改了 `DESIGN_SPEC` 第 1／2／3 節、`DEV_SPEC` 檔案結構註解與 5.3／5.4、`layout.html`、`components-spec.html`（新增主格線色卡）、`template/index.html`、`README.md` 共六個檔。主格線對比度用腳本實算 **2.24／2.09**，同一支腳本驗出次格線 1.41、座標軸 3.77，與規範既有記載吻合
- 三個 commit 落地：母倉 `634b652`（規範回流）、`04bb7ab`（母倉設定），`optics-sim` `7f02dd4`（初始化）。兩個工作樹都乾淨
- **`DESIGN_BRIEF.md` 完成**（尚未提交），可交給 Claude Design 出稿
- **專案 memory 更新**：`sim-spec-repo-status`（結構圖補 dissolution 與 optics、remote 現況查核）、新增 `two-column-gridline-decision`（兩欄配置與格線分級的定案與大屏待測）、`MEMORY.md` 索引補一行

### 已完成（2026-09-06 階段 1 開發 session）

- **骨架**：兩欄 `minmax(302px,1fr) 3fr`、無抽屜、左欄下方動作卡、四分頁殼（透鏡頁預設選中）、
  `<900px` 只有「設定」一顆 chip。1366×768 實測左欄 **331.5**／畫布 **992.5**（比例 2.994）
- **座標系統**：橫向 22 格、格邊長 **45.11px**、縱向 **14.10 格**、主軸在第 **4.699 格**、光路收上 2/3。
  主次格線以**鏡心與主軸**為基準每 5 格加深
- **共用成像引擎**：`imaging(u,f)` 純函式；五檔離散拖桿與畫布拖曳雙向連動（單點／區間不對稱）；
  出界處理（「像在更遠處」「虛像在更遠處」「出射光互相平行，不成像」）
- **頁 3 凸透鏡**：透鏡厚度隨焦距、三種物體造型（蠟燭 svg／箭頭／F 形，全部 2 格高）、
  三條特徵光線（白色外描邊＋箭頭）、反向延長線、完整光束、實／虛像、紙屏＋屏面小窗（真高斯模糊）、
  觀察者眼睛、圖例卡（關掉的光線變灰虛線標「已關閉」）
- **左欄捲動問題已解**（§2.5 第 15 點）：桌機餘裕 58px、平板（--u=1.15）41px、大屏（1.4）13.7px，三種都不捲動
- 詳細定案與三個實作坑見 **§2.12**

### 已完成（2026-09-06 階段 2 開發 session）

- **凹透鏡**：`.seg` 兩格切換；焦距取負號後成像引擎、三條特徵光線、完整光束、觀察者眼睛
  **全部原式共用**；雙凹外形與凸透鏡共用同一張曲率表；第三條光線的名稱隨透鏡種類換
- **可拖遮板**：從透鏡上方往下拉，被擋的光線整條不畫、圖例標「（被遮板擋住）」，
  像只變暗不變形（亮度 ＝ 透光比例^0.45），全遮時像消失並標出原因
- **紙屏對虛像有回饋了**：`blurOf()` 改用絕對值通式，屏拉得越遠光斑越大越淡
  （連帶改到凸透鏡 `u<f` 那一檔，見 §2.13）
- 驗算 30 組（凸 15 無回歸 ＋ 凹 15 全部正立縮小虛像）、光線③ 收斂誤差 <1e-9、console 無錯誤
- **遮板改為可開關的核取**（與紙屏一樣預設關）＋ 拖曳提示圖示，使用者 2026-09-06 追加裁示
- **左欄捲動**：使用者裁示「接受捲動」，桌機餘裕 −61px，動作卡維持不動，見 §2.13 末
- 修掉一個靜默 bug：§2.8 記的 `fa-arrow-up-down` 在 FA6 不存在，錯名不報錯只渲染成 0 寬
  （已更正為 `fa-arrows-up-down`，通用教訓落檔於 `pitfalls-frontend.md` 第 2 條）
- 詳細定案見 **§2.13**

### 已完成（2026-09-07 階段 3＋4 開發 session）

- **架構**：四頁改用同一張畫布；左欄拆成分頁專屬組＋共用組；尚未實作的頁蓋板並收起左欄
- **頁 2 面鏡三種**：`.seg` 平面／凹面／凸面；**與透鏡共用同一支成像引擎**（面鏡公式展開後同式，
  差別只有 `signedF()` 與 `outSign()`）；平面鏡＝焦距無限大；三條特徵光線的法線（黑虛線）；
  平面鏡的等距標記；F／2F 只標在有焦點的那一側
- **頁 1 針孔**：離散光線等間隔打在擋板平面；孔徑三檔放行 1／3／5 道；擋板是留空隙不是畫圓；
  沿孔形輪廓取樣疊繪（圓／方／三角斑，像的形狀不變）；多孔 → 多個完整的像；紙屏預設開不加開關
- 驗算：四種器材 × 五檔全部通過（像的側別、實虛、正倒、放大縮小）、光線③ 誤差 <1e-9、
  針孔像高不隨孔徑變、瀏覽器實測孔數與疊繪份數對應正確、console 無錯誤
- 詳細定案見 **§2.14**

### 已完成（2026-09-07 階段 5 開發 session）

- **平面鏡改標格數**（使用者裁示「標格數」）：等長線段保留，兩側各加一個 `N.N 格`，
  結掉 §2.5 丁2 一路帶到 §2.14 末的未定項。這是 §2.2「一律不標任何數值」的唯一豁免
- **頁 3 眼睛**：眼球剖面用使用者的 svg（三條 path 逐字內嵌、機械比對過）；
  **水晶體逐數字插值做連續變形**（不是兩張圖切換）；三種眼睛狀態 × 看遠／看近的調節；
  矯正鏡走**兩次成像**（鏡片的像才是水晶體的物，虛物情況原式通用）；度數五檔跨過「剛好」；
  視網膜光斑與視網膜小窗；**視覺模擬小窗**（遠處的樹＋近處的書本，同一支彌散斑公式）
- **四頁全滿**：畫布蓋板 `.screen` 與 `unbuilt` 那段左欄收合邏輯一併移除，
  導覽列不再有點進去是死路的頁——`DEV_SPEC` 9.5 的部署前提已滿足
- 修掉一個自己寫出來的 bug：`powerLabel()` 用 `need` 判「度數不足／過頭」，
  在虛物情境下會講反（遠視眼戴眼鏡看遠五檔全誤報），改成直接比像距與眼軸。詳見 §2.15
- 落檔一個工具坑：`javascript_tool` 讀不到頁面的頂層 `let`／`const`
  （→ `handbook\pitfalls-services.md` #13，同時補進該檔 §0 症狀索引）
- 詳細定案與驗算見 **§2.15**

### 已完成（2026-09-07 第六輪修正 session）

- 使用者 8 點回饋全部實作，逐項去向見 **§2.16**（§2.15 的眼睛頁參數整組作廢）
- 眼睛頁重解：眼球右移 4 格、蠟燭 1 格高、調節範圍由「像距 ±1.5 格」反解、度數三檔、
  正確度數強制對焦。瀏覽器實跑驗證：偏離量誤差 ≤0.001 格、正確檔在四個抽樣物距全部落在視網膜上
- 面鏡頁光線改在**鏡面弧**上反射（鏡面本來就是拋物面，不必換器材），
  出射方向改連向像點。六種鏡子×焦距的弧上誤差 ≤0.005px；**透鏡頁 12 個樣本零位移**
- 移除視網膜上那條橘色垂直線；矯正眼鏡改成兩片互為鏡像；拖曳提示膠囊化；切到眼睛頁強制蠟燭
- `.seg` 佔滿寬度**回流完成**（`_shared/template/index.html`、`design/DESIGN_SPEC.md`、
  `design/components-spec.html`）。回流驗收：來源與標的七個值逐一相等
- 派了一個 fresh-context 審查 agent 獨立診斷第 3／6／8 點（dispatch §7 的目視推翻第 2 輪要求），
  第 3 點的根因由它定位

### 待辦

- [x] ~~X1–X3 回流~~ — **2026-09-04 完成**（母倉 `634b652`）
- [x] ~~`DESIGN_BRIEF.md`~~ — **2026-09-04 完成**（**尚未提交**）。通用規格全部寫成「照 `DESIGN_SPEC` 第 N 節」的引用表，**沒有手抄任何規格表**；第 2 節特別警告「`layout.html` 畫的是三欄＋抽屜，本模擬兩者都不同，不要照它畫」；列出八個難題要 Design 解
- [x] ~~階段 1 開發~~ — **2026-09-06 完成**（骨架＋共用成像引擎＋頁 3 凸透鏡，見 §2.12。**尚未提交**）
- [x] ~~階段 2：頁 3 凹透鏡＋可拖遮板~~ — **2026-09-06 完成**（見 §2.13。**尚未提交**）
- [x] ~~左欄捲動的三選一裁決~~ — **2026-09-06 裁決：接受捲動**（**B**，選項原文「接受捲動」）
- [x] ~~建 GitHub remote 與 Pages（`main` / root）~~ — **2026-09-20 查證已完成**（見下方同名條目）
- [x] ~~階段 3：頁 2 面鏡三種~~ — **2026-09-07 完成**（見 §2.14。**尚未提交**）
- [x] ~~階段 4：頁 1 針孔~~ — **2026-09-07 完成**（見 §2.14。**尚未提交**）
- [x] ~~平面鏡等距標記的折衷待確認~~ — **2026-09-07 裁決：標格數**（**A**，使用者原話「標格數」）。
      等長線段保留、兩側各加一個 `N.N 格`，見 §2.15
- [x] ~~階段 5：頁 4 眼睛~~ — **2026-09-07 完成**（見 §2.15。**尚未提交**）
- [x] ~~部署前隱藏未完成的分頁~~ — **2026-09-07 自動消解**：四頁全部實作完成，蓋板與收合邏輯已移除
- [x] ~~建 GitHub remote 與 Pages（`main` / root）。**這是部署動作**，做之前先跑 `judgment.md` §7 兩道閘~~
      — **2026-09-20 以 `gh api` 查證已完成**：remote ＝ `hfuhsu108/optics-simulator`、Pages `status: built`、
      來源 `main` / root、線上網址 <https://hfuhsu108.github.io/optics-simulator/>。
      （此條與 §12「09-11 第六批：上線」記載的部署是同一件事，這裡的未勾選是清單未同步。）
- [ ] **iPad 與教室大屏真機實測**——階段 1 起的〔真〕項目一律未驗：觸控拖曳物體與紙屏、
      `filter:blur()` 在 iPad 上的效能（§2.5 第 3 點的模糊畫法**要等真機才能定案**，備案是離屏快取）、
      大屏 6 公尺外能否數得出格線。**階段 5 新增**：視網膜小窗與視覺模擬小窗**兩個 blur 同時在畫面上**，
      iPad 的 filter 效能要一起看；眼球 svg 的細節（睫狀小帶那些細線）在大屏 6 公尺外會不會糊成一團
- [x] ~~第六輪修正批次（使用者 8 點回饋）~~ — **2026-09-07 完成**（見 §2.16。**尚未提交**）
- [x] ~~`.seg` 佔滿寬度回流 `_shared/`~~ — **2026-09-07 完成**（三個共用檔，**尚未提交**）
- [x] ~~**面鏡頁的取捨待裁示**（§2.16 第 6 點的表）：三條特徵光線精確共點於公式像點，
      代價是第①條與焦點記號差 0.05～0.94 格（f 越小、物體越近差越多）。
      接受就不動；不接受的替代方案是讓①精確通過焦點，代價換成三條線交不到同一點~~
      — **2026-09-20 結案：維持 A 案**〔**A** 使用者「維持A案」〕，即上述「接受就不動」。
      使用者看過 §15.3 的左右並排對照檔後裁示，詳見 §10 末尾與 §15.1 第 2 項。
      （這條掛在清單上 13 天，結案寫進了 §10／§15 卻沒回頭同步這裡。）
- [ ] **階段 5 的 C 類參數待確認**（都能單點改，不動架構）：
      ~~① 調節範圍~~／~~② 看遠看近的代表位置~~／~~③ 度數五檔的屈光力~~／~~⑥ 小窗尺度 4 格~~／
      ~~⑦ 眼球內部不畫像~~ —— **這五項已被 §2.16 取代**（端點改由使用者指定的 ±1.5 格反解、
      看遠改 14.6 格、度數改三檔、小窗改 2 格、像一律畫出）
      ④ **視覺模擬的樹與書用實物色** `#4A7C59`／`#8B5E3C`——與蠟燭、眼球同性質（教學插圖的實物色），
         但**沒有走過色票豁免程序**。要嚴格守色票就換成 `var(--c-text-2)`，CSS 兩個值即可
      ⑤ 視覺模擬**預設開**（不套「教學輔助疊層一律預設關」）
      ⑧ **新增**：`EYE_SHIFT = 4` 格是模型判斷，畫布只剩 0.8 格餘裕；大屏實測若覺得眼球太靠右，
         這個值可以往下調，但每調一次都要重跑 scratchpad 的參數驗算（調節範圍是它反解出來的）

## 6. 2026-09-08 使用者裁示修訂（12 ＋ 1 項）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §6（全文原樣保留，章節號不變）。

## 7. 2026-09-10 使用者裁示修訂（4 項已實作 ＋ 第 13 項再提）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §7（全文原樣保留，章節號不變）。

## 8. 2026-09-10 第二批裁示（4 項已實作，第 5 項另立階段）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §8（全文原樣保留，章節號不變）。

## 9. 2026-09-10 第三批（作者欄、播放動畫；面鏡另有兩項待裁決）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §9（全文原樣保留，章節號不變）。

## 10. 2026-09-10 第四批：反射點改回垂直平面（A 方案）

**B** 使用者選定「照 PhET，反射點回到垂直平面」。**這推翻 2026-09-07 的 A 類裁示**
「反射的位置不在鏡面而是在垂直軸上，我希望可以在鏡面反射」。修訂鏈：09-07 **A** → 09-10 **B**。

實作只有一行：`hitX` 由拋物線式改成恆回 `cx`。另同步改 7 段會變成謊話的註解
（`hitX` 自身、`outEnd` 的取捨敘述、`axisEnd`、平行光模式、法線、完整光束、`clipByAnim`）。
**邏輯 0 處必改**：法線方向式 `(1, dy/(2fg))` 同時等於拋物面法線與「落點連曲率中心 2f」，
兩者方向相同；`gearMaxHalfT()` 的預留照舊需要（鏡面外觀仍是彎的，物體仍會插進弧裡）。

實測（凹面鏡，解析畫出來的 path 座標）：

| 焦距 | 物在 2f 的像距 | 放大率 | ①在焦點處偏離 | ①在像距處偏離 | 物在 F 時出射斜率 |
|---|---|---|---|---|---|
| 2 格 | 4.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −1.0000／−1.0000 |
| 3 格 | 6.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −0.6667／−0.6667 |
| 4 格 | 8.000 格 | −1.000 | 0.0000 格 | 0.0000 格 | −0.5000／−0.5000 |

凸面鏡（虛像）三條反向延長線偏離像點 0.0001 格；平面鏡像距 −5 格、放大率 1。
**第 13 項的取捨到此消滅**：三線交於課本像點、①通過焦點、物在 F 時平行，三者同時成立。
上一批「①在像距處偏 0.667／0.250／0.133 格」的代價一併歸零。

### 球面鏡 vs 拋物面鏡（**A** 使用者 2026-09-10 問「哪個讓反射點比較靠近面鏡」）

反射點在垂直平面上，它離鏡面多遠 ＝ 鏡面在該高度的矢高。同焦距同高度下球面恆較凸：
球面 `R−√(R²−h²)` 展開 ＝ 拋物面 `h²/(4f)` 再加一串正的高階項，差值主項 `h⁴/(64f³)`。

| 焦距 | 高 2 格（①入射高）拋物／球面 | 高 3 格（鏡緣）拋物／球面 |
|---|---|---|
| 2 格 | 0.500／0.536 | 1.125／1.354 |
| 3 格 | 0.333／0.343 | 0.750／0.804 |
| 4 格 | 0.250／0.254 | 0.563／0.584 |

**結論：維持拋物面**（現況即是），鏡面畫法不動。

**已知的視覺代價**：轉折點在 `cx`、鏡體畫在 `cx−矢高` 處，所以光線會穿過鏡面在鏡後轉折，
f=2、高 2 格時約 0.28 格（扣掉鏡體厚度後）。PhET 的緩解做法是把鏡面畫細畫淡。**未處理，待使用者看過再定。**

### 審查發現的兩個既有缺陷（2026-09-10，fresh-context opus 審查）——**2026-09-20 兩項皆已結案**

1. ~~**③的落點會跑到器材外**（高）。~~ `lensHitY()` 回的命中高 ＝ 物高 × f/(f−u)，f=2 時
   實測 u=1.2／2.5／3 各落在 5／−8／−4 格，而鏡（透鏡）半高只有 3 格（`LENS_H = 6`）。**u=3 是常用值**。
   現行只有「超過畫布高 4 倍才不畫」的門檻，與器材口徑無關。**透鏡頁與面鏡頁都中**。
   原選項：(a) 落點超出口徑就不畫這一條 (b) 截在鏡緣但不轉折 (c) 照現況畫在空白處。
   **2026-09-20 裁決：(c) 照現況畫在空白處**〔**A** 使用者「3.c」〕。
   **即維持現行行為，程式不改**。三條特徵光線是作圖工具，落在鏡緣以外時本來就不受口徑限制
   （同一個道理已寫在 `index.html` 遮板那段的註解裡）。這條掛了 10 天，2026-09-20 由
   PROJECT_PLAN 歸檔前的開放項清查重新撈出來才交付裁決。
2. ~~**虛像時①的反向延長線不與另兩條交會**（中）。凹面鏡 u=1 偏 1.333 格、凸面鏡 f=−2 偏 0.222 格。~~
   **已由 §10 的 A 方案自動解決**（2026-09-10 同日）：落點回到 `cx` 之後，`axisEnd()` 與
   `outEnd()` 變成同一條線，實測凸面鏡三條反向延長線偏離像點 0.0001 格。不需另外處置。

審查同時獨立確認：需求「反射點在曲面上」＋「①精確過焦點」＋「u=f 出射平行」三者
**在數學上不可兼得**——①的出射斜率恆為 h/(f−a)、②恆為 h/u，兩者相等 ⟺ 矢高 a=0。
與程式怎麼寫無關。

### 舊記錄：第 13 項提出時的三個選項

使用者原話：「我發現面竟成像你用的公式是球面鏡的，難怪會有誤差。你幫我判斷用球面鏡好還是拋物面鏡好」。

**澄清**：`1/u + 1/v = 1/f` 不是球面鏡專屬，是**近軸近似**，球面鏡與拋物面鏡共用。程式裡唯一借用
球面幾何之處（法線指向曲率中心 2f）在階段 2 已改成拋物面法線，鏡面弧本身也已是拋物線。
**所以「換成拋物面鏡」這件事已經做完了**，誤差不來自鏡面形狀。

**本次獨立複驗**（f=3、u=6、物高 2 格，頁面實跑）：

| 項目 | 課本近軸公式（現況） | 拋物面真實反射 |
|---|---|---|
| 像距 | 6.000 格 | 5.400 格 |
| 放大率 | −1.000 | −0.900 |
| ①是否通過焦點記號 | 否，偏 0.118 格 | 是，精確 |

與 2026-09-08 `scratchpad/mirror-check.js` 的記載完全一致。**模型建議：維持現況**——「物在 2F →
像在 2F、倒立同大」是國中課本的核心結論，畫面上讓它變成 0.90 倍的代價，遠大於一條光線偏離焦點
記號 0.118 格。~~仍等使用者裁示。~~

**2026-09-20 結案：維持 A 案**〔**A** 使用者「維持A案」〕。使用者看過 §15.3 的左右並排對照檔後裁示。
對照檔複驗了上表：f=3、u=6、拋物面時真實反射的 ①② 交點 ＝ **5.400 格**，與本節 09-10 的記載一字不差；
另補了本節沒量過的兩件事——三條出射光的**兩兩交點散布** 0.053 格（f=3、u=6、拋物面）到 0.657 格
（f=2、球面、物在 2F），虛像時更達 3.571 格（f=3、u=1.5）。決定性的是「物在 2F → 像在 2F、倒立同大」
這一條：B 案在 f=2 時只剩 0.80 倍、球面掉到 0.59 倍。**反射點畫法自此不再更動**，
`hitX` 維持恆回 `cx`（平行光模式的鏡面弧落點是既有例外，不受影響）。

## 11. 2026-09-10 第五批：動畫擴到四頁、面鏡法線改規則、針孔發光點拆開（5 項）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §11（全文原樣保留，章節號不變）。

## 12. 2026-09-11 第六批：抽屜回歸規範、交界線、署名回流、上線（4 項）

**本章已於 2026-09-20 歸檔** → 見 [`PROJECT_PLAN_archive_202609.md`](PROJECT_PLAN_archive_202609.md) §12（全文原樣保留，章節號不變）。

## 13. 2026-09-13 新增頁籤「光的折射」（鳥看魚／魚看鳥；09-14 頁籤標題改為「視深」）

使用者原話（**A**）：「我希望光學模擬器可以多一個頁籤，內容是折射，放在面鏡成像和透鏡成像之間」
「我希望可以模擬出鳥看水中魚以及魚看空中鳥的視深與實深，可以明確呈現光路與形成的虛像」。
規劃全文：`C:\Users\Hope\.claude\plans\svg-dazzling-eclipse.md`（2026-09-13 寫、2026-09-14 經 ExitPlanMode 核可）。
分兩階段：**階段 1** 頁籤＋場景＋兩情境的靜態光路與虛像；**階段 2** 拖曳、完整光束、動畫實測、重置、說明。

### 13.1 已定案

| 項目 | 裁決 | 來源 |
|---|---|---|
| 頁籤位置 | 面鏡成像與透鏡成像之間 | **A**「放在面鏡成像和透鏡成像之間」 |
| 情境呈現 | 左欄 `.seg`「鳥看魚｜魚看鳥」，一次一組光路與一個虛像 | **B**「.seg 切換（推薦）」 |
| 虛像位置 | 真實光路：進入觀察者眼睛的兩條光線反向延長，交點＝虛像 | **B**「真實光路（推薦）」 |
| ~~可拖~~ | ~~鳥與魚都能上下左右拖~~（**已被下方 09-14 的 A 推翻**） | **B**「鳥和魚都能上下左右拖（推薦）」 |
| 數值 | 只標名稱不標數字（守 §2.2） | **B**「只標名稱不標數字（推薦）」 |
| 頁籤標題 | **視深**（取代「光的折射」；程式碼註解仍以「光的折射頁」稱呼） | 2026-09-14 **A**「頁籤標題改成視深」 |
| 可拖（修訂） | **鳥與魚只能水平移動**，高度與深度固定在預設值。修訂鏈：09-13 **B**「鳥和魚都能上下左右拖（推薦）」→ 09-14 **A**（後者生效） | **A**「魚和鳥能水平動就好」 |
| 格線 | 與其他四頁對齊（以主軸高度為基準），切換頁籤時格線不跳動 | 2026-09-14 **A**「方格線我希望能對準其他的頁面」 |
| 水面位置 | 主軸往下 1 格的那條格線（`REFR_SURF_DROP = 1`）；1366×768 下水面以上 5.12 格、以下 3.12 格 | 2026-09-14 **B**「主軸往下 1 格（推薦）」 |

### 13.2 素材驗收（2026-09-13／09-14 實測）

| | 魚 `圖畫.svg`（09-13 23:09 重存，85 KB） | 鳥 `鳥.svg`（62 KB） |
|---|---|---|
| 外觀 | 側面、頭朝左 | 正面展翅海鷗、左右對稱 |
| viewBox | `162.24016 × 80.490814` | `158.12759 × 49.868801` |
| 兩層 g 的累積位移 | `translate(-20.361979,-50.60387)` | `translate(-19.764593,-44.26229)` |
| 整體外輪廓 | `path12` #ffffff（單一封閉） | `path5` #b1b7c2（單一封閉） |
| 參考點 | 魚眼中心 `(21.02, 37.48)`，半徑 4.76 | 兩眼 `(75.95, 16.5)`／`(82.25, 16.5)` → 取中點 `(79.1, 16.5)` |
| style | 只有 fill、`display:inline`，無 `fill-rule`、無漸層濾鏡 | 同左（另有 `fill-opacity:1`，無作用） |

- 魚的 `path5`（#457da5，69 塊、佔全檔約 60%）是魚鰭與外框之間的細縫填色，與 `path6` 幾乎同色；拿掉會露白縫，保留。
- 檔名 `圖畫.svg` 沿用不改：**圖畫.svg＝魚**。

### 13.3 物理模型與驗算

水面 y=0、往上為正；水 n=4/3。光線與水面的交點以二分法解 `nS·sinθS = nE·sinθE`。
兩條光線射向觀察點兩側各 `REFR_PUPIL/2`（垂直於中心光線），反向延長的交點＝虛像。
**口徑幾乎不影響交點**（node 實算：口徑 0.3／0.6／1.0 格時交點差 ≤ 0.02 格），所以口徑是純視覺參數。

| 實深／實高 3 格、觀察者離水面 3 格 | 水平錯開 0 | 2 | 4 | 6 | 8 |
|---|---|---|---|---|---|
| 鳥看魚：視深（格） | 2.25 | 2.05 | 1.55 | 1.01 | 0.60 |
| 魚看鳥：視高（格） | 4.00 | 4.40 | 5.80 | 8.92 | 15.02 |

**已知後果**：魚看鳥一斜看，虛像就衝出畫面（越接近斯涅爾窗邊緣放大越劇烈，真實物理）。

### 13.4 模型判斷（C，使用者未表態）

1. 新鈕 `data-screen="4"`，DOM 排在面鏡與透鏡之間；既有 0–3 不重新編號（30 多處 `state.screen === N` 不必重驗）。標籤「光的折射」（09-14 **A** 改為「視深」，見 §13.1）
2. 場景：鳥在左上空中、魚在右下水中；兩情境**共用**位置，切 `.seg` 只換誰發光、誰觀察。魚頭一律朝向鳥
3. 魚寬 2.4 格、鳥寬 3.0 格。~~水面在光路區由上往下 57%（`REFR_SURF_FRAC`），主格線以水面為基準~~
   → **09-14 推翻**：格線改回以主軸為基準（**A**），水面在主軸往下 1 格（**B**），見 §13.1。
   預設位置鳥 `{x:-1.0, h:2.0}`、魚 `{x:0.5, d:2.2}`（水平錯開 1.5 格）。**計畫原寫「錯開約 2.5 格」，階段 1 實測推翻**：
   1366×768 水面以上只有 4.69 格、以下 3.54 格，錯開 2.5 格時魚看鳥的虛像頂端出界；
   64 組候選（鳥高×錯開×魚深各 4 檔）只有錯開 1.5、鳥高 2 這一帶同時滿足「虛像不出界、魚不沉底、視深 ≤ 0.75 實深」
4. 參考點：魚＝魚眼、鳥＝兩眼中點；發光與觀察都用這一點，實深／視深量到這一點
5. 法線：兩個折射點各一條垂直水面的黑虛線（§2.5 第 4 點之③），常駐不設開關
6. 完整光束（階段 2）：從參考點發出、**沒進入觀察者眼睛**的光，淡色、無反向延長線；水中發光時扇形限 ±40°（避開全反射）
7. 虛像：外輪廓灰虛線框＋`IMG_OPACITY` 填色；整隻動物的像用**以參考點為錨的線性映射**
   （參考點的像固定＝虛線交點；方向與伸縮由包圍盒四角＋四邊中點各自求像、最小平方擬合）。
   **計畫原寫「包圍盒三個角擬合、第 4 角誤差 <0.1 格」，階段 1 實測推翻**：斜看時非線性明顯
   （鳥看魚錯開 4 格差 0.23 格、魚看鳥錯開 6 格差 3.99 格），且角點擬合出來的像，眼睛不保證落在虛線交點。
   中間試過「在參考點取微分」：眼睛對了，但翅尖差到 0.90 格，所以改成錨點＋最小平方
8. 出界：虛像在光路區外 → 不畫像、虛線畫到邊界、邊緣箭頭＋「虛像在畫面上方更遠處」；**不**夾限可拖範圍去遷就像
9. 標示：鳥看魚「實深／視深」、魚看鳥「實高／視高」，垂直量測線＋端點短橫，無數字。
   兩條並排在動物**遠離觀察者**那一側（內側量實的、外側量像的），各拉淡灰引線回到量的那一點；
   **初版畫在參考點正上／正下方，階段 1 實測名稱被虛像蓋住**，因此改擺
10. 水：DESIGN_SPEC 第 3 節水色取平塗 `#DBEAFE`、水面線 `rgba(42,111,219,.45)` 2px。水色會把格線對比壓到下限以下
    （次格線 1.15、主格線 1.83），水域內改用 **次 `#C1C5CC`（1.42）／主 `#959DA8`（2.25）**——在線性空間等比壓暗既有格線色求得
11. 本頁沒有：主軸、焦距、物距拖桿、特徵光線核取、物體造型、遮板、紙屏與紙屏小窗

### 13.5 撞到既有定案的兩條（2026-09-14 **B**「兩條都定案（推薦）」）

> 修訂鏈：09-14 早上隨計畫一併核可、未逐項表態（C）→ 同日使用者實測後以 AskUserQuestion 選定（B，生效）。

- **本頁不設紙屏**——撞到 §2.2 **B**「四頁都有（推薦）」：沒有實像可接，觀察者本身就是眼睛
- **鳥、魚配色比照 §2.11 蠟燭豁免** DESIGN_SPEC 色票（兩張共 10 個色）

### 13.6 本次刻意沒做

| 限制 | 理由 | 解除條件 |
|---|---|---|
| 入射角／折射角標示 | 需求是視深與虛像，角度標示會加線條 | 使用者要求 |
| 全反射 | 國中此單元不含；完整光束扇形限在臨界角內避開 | 使用者要求 |
| 介質／折射率可選 | 未要求 | 使用者要求 |
| 「觀察者看到的樣子」小窗 | 使用者 09-08 刪過眼睛頁同類小窗（§6 第 1 項） | 使用者要求 |

### 13.7 進度

- [x] 階段 1：頁籤＋場景＋兩情境靜態光路與虛像 —— 2026-09-14 實作與瀏覽器驗證完成；**使用者實測過（A「已測試」）**，
      同一則訊息附三項修改（頁籤改名、只能水平移動、格線對齊其他頁），併入階段 2 做
      - 素材：`鳥.svg`、`圖畫.svg` 各 5 條 path 以 scratchpad 的 node 腳本逐字內嵌（147 KB 路徑資料無法手打），比對 10 條、差異 0
      - 物理（1366×768 模擬）：鳥看魚錯開 0／2／4／6 格 → 視深 2.247／2.041／1.541／0.998；
        魚看鳥 → 視高 4.005／4.406／5.818（出界）／8.934（出界）；兩折射點斯涅爾誤差 ≤ 2.2e-16；像點到兩條虛線 0 px；
        虛像眼睛落在交點 0 px；像的 8 點最大偏差：鳥看魚 0.05～0.13 格、魚看鳥預設位置 0.67 格（非線性拉伸，線性映射的極限）
      - 回歸：四頁格線路徑與改動前演算法逐字相同；凸透鏡 v=7、m=−1.333；凹透鏡 v=−1.909；平面鏡 `5.3 格`；針孔 1／3／5 道；
        透鏡頁開著紙屏切到折射頁不出現紙屏小窗；導覽列 768 與 640 寬不溢出；console 無錯誤
      - 本階段刻意收起：左欄「顯示」組（完整光束屬階段 2）；折射頁的畫布拖曳一律不接（pointerdown 直接返回）
- [x] 階段 2：拖曳、完整光束、動畫、重置、說明 ——（2026-09-18 23:21 **A**「實測都通過」）**2026-09-14 實作與瀏覽器驗證完成，待使用者實測後打勾**（併入使用者 09-14 三項修改）
      - 頁籤標題「視深」；鳥與魚只能水平拖（畫布本體或藍框握把），抓取偏移保留不跳位，整隻夾在畫布內、左右各留 0.1 格；
        魚跨過鳥的正下方就翻面。鳥的握把在朝向魚那一側的翅尖外（正下方是光線、正上方是魚看鳥的虛像），魚的在身體正下方〔C〕
      - 格線與另外四頁逐條相同（直 22、橫 9 條）；水面在主軸往下 1 格（`REFR_SURF_DROP = 1`），1366×768 上 5.12／下 3.12 格
      - 完整光束：兩情境各 10 條（與進眼光線方向太近的略過），水面轉折處符合折射定律（px 取整誤差 0.0001）；水中發光扇形 ±40°、空中 ±65°〔C〕
      - 動畫（逐點設定 `state.anim` 驗證）：光到水面前沒有法線、之後才有；虛線抵達前 5 px 沒有虛像與「視深」，抵達後一起出現。
        **實際播放沒能在 Browser pane 內跑完**：pane 在背景時 `document.hidden === true`，`visibilitychange` 把動畫暫停（pitfalls-services #3 已載），待使用者實測
      - 真實滑鼠拖曳：握把 +2.91 格、鳥高不變；抓鳥身拖回 −1.000（偏移 −0.013 格）；魚拖到右界停在 8.811（＝算出的上限）；
        鳥往正下方拖 x、h 皆不變；拖到畫布外的導覽列仍持續跟隨、放開後結束
      - 重置留在本頁並回預設；左欄在 U=1.15 無捲動（透鏡頁 −33 px）；四頁回歸（格線逐字相同、v=7／−1.909、`5.3 格`）；
        魚看鳥錯開 6 格虛像高 12.19 格 → 出界提示；console 無錯誤
- [x]（2026-09-18 23:21 **A**「實測都通過」）iPad 與教室大屏實測：水域格線對比、鳥與魚在 6 公尺外的辨識度、二維觸控拖曳
- [x] 升版與 Release —— **2026-09-18 完成**：實測通過後 **B**「發 v1.1.0 (Recommended)」→ `APP_VERSION` 改 `v1.1.0`、tag `v1.1.0`、
      以下列草稿發佈 GitHub Release（本專案第一個 tag／Release，v1.0.0 從未打 tag）。沿革：2026-09-14 收尾時 **B** 選「⑤ 部署與版號」「⑥ Release Notes」：本次只 commit ＋ push 上線；
      依收尾閘（handbook judgment §7），階段 2 使用者實測前不升版、不打 tag、不發 Release。實測通過後補做：
      - `APP_VERSION` `v1.0.0` → `v1.1.0`〔C：新增頁籤屬次版號〕
      - GitHub Release 草稿〔C，未發佈〕，標題「v1.1.0 新增『視深』與『色光』頁籤」
        （2026-09-18 收尾 **B** 選「⑤ 部署與版號」「⑥ Release Notes」：色光頁併入同一版，兩頁都待實測，仍不升版、不發佈）：
        - 新增頁籤「視深」，排在面鏡成像與透鏡成像之間：鳥看水中的魚、魚看空中的鳥
        - 光線在水面依折射定律轉彎；進入眼睛的兩條光往回延長，交點就是看到的虛像，並標出實深與視深（實高與視高）
        - 鳥與魚可以左右拖曳；斜著看時魚看起來更淺、鳥看起來更高，虛像超出畫面時標出方向
        - 支援播放動畫與「完整光束」
        - 新增頁籤「色光」，排在最後：紅、綠、藍光可複選，「白光」一鍵三色全開
        - 不透明體：光照到色紙，只有同色的光被反射進眼睛，其餘被吸收；色紙有紅、綠、藍、白、黑五色
        - 透明體：光穿過最多三片紅、綠、藍玻璃紙，每片只讓同色光通過
        - 右下小窗顯示眼睛看到的顏色，左下與右下的三原色圈圈分別是發出的光與眼睛接收的光，光源方框顯示混合後的顏色
        - 左下卡片寫出每一種光被反射、通過或吸收；支援播放動畫

### 本專案尚未產生的檔案

`HANDOFF.md`（四頁全部完成，接下來是部署與真機實測，需要時再寫）、
`design_handoff/`（Design 出稿後放，已被 `.gitignore` 排除）

---

## 14. 2026-09-18 新增頁籤「色光」（色光與物體的顏色）

使用者原話（**A**）：「光學模擬我希望可以增加一個頁籤，是用來模擬色光與顏色的。可以切換透明體與不透明體的呈色原理。光可以選紅、綠、藍三種顏色」。
2026-09-18 14:25 模型提出 10 題，14:42 使用者以編號回覆 1–9（原文「1.a 2.a 3.c 可3片 4.a 5.a 6.色紙 7.a 8.a 9.a」），第 10 題未回覆。

### 14.1 已定案

| 題 | 項目 | 裁決 | 來源 |
|---|---|---|---|
| — | 呈色原理 | 左欄可切換**透明體／不透明體** | **A**「可以切換透明體與不透明體的呈色原理」 |
| 1 | 頁籤位置與名稱 | 排**最後**（眼睛之後），標題「**色光**」 | **B** 選項原文「放最後（眼睛之後），標題『色光』（推薦）」 |
| 2 | 光源 | 紅、綠、藍**三顆獨立開關，可同時開**，疊出黃、青、洋紅、白 | **B**「三顆各自開關，可同時開，疊出黃、青、洋紅、白（推薦）」 |
| 3 | 透明體 | **不畫光屏，眼睛直接透過玻璃紙看**；玻璃紙**最多可疊 3 片** | **B**「同 A，但不畫光屏，改由眼睛直接透過玻璃紙看」＋ **A**「可3片」 |
| 4 | 不透明體 | 光照到物體→反射光射向眼睛；被吸收的光線停在物體表面；**色紙塗它本來的顏色**，看起來的顏色由小窗、示意圖、結論卡呈現。修訂鏈：09-18 14:42 **B**「物體畫成它看起來的顏色」→ 09-18 22:22 **A**（後者生效） | **B**「光照物體→反射光射向眼睛…被吸收的光線停在表面（推薦）」＋ **A**「我希望色紙呈現的是他本來就該有的顏色，不是他照到光呈現的」 |
| 5 | 可選顏色 | **紅、綠、藍、黑、白，共 5 種**（物體與玻璃紙皆同）。修訂鏈：09-18 14:42 **B**「紅、綠、藍、黃、青、洋紅、白、黑 8 種（推薦）」→ 09-18 14:45 **A**（後者生效） | **A**「顏色先3原色+黑白就好」 |
| 6 | 不透明物體外觀 | **色紙** | **A**「色紙」 |
| 7 | 顏色模型 | **理想三原色**：只看物體含不含 R、G、B 三個成分 | **B**「理想三原色：只看物體含不含 R、G、B 成分，課本講法（推薦）」 |
| 8 | 動畫 | **要**，和其他頁籤一樣用抽屜的播放鈕 | **B**「要，跟其他頁一樣用抽屜播放鈕（推薦）」 |
| 9 | 文字結論 | **要**，例如「綠色物體：反射綠光，吸收紅光、藍光」 | **B**「要（推薦）」 |

| — | 「眼睛看到的顏色」小窗 | **要**，常駐右下（借用紙屏小窗的位置），內容＝一整塊看到的顏色＋顏色名；播放中光沒走完前畫淺灰〔位置、內容、時機為 C〕。修訂鏈：09-18 14:42 第 4 題選 **B**「光照物體→反射光射向眼睛…（推薦）」、未選帶小窗的 C → 09-18 15:34 **A**（後者生效，只限色光頁） | **A**「請幫我多做一個小窗，呈現眼睛看到的顏色」 |

| — | 白光選項 | 色光膠囊加「白光」，與紅綠藍連動：三色全開時亮；按下＝三色全開，已全開再按＝全關〔連動規則 C〕 | **A**「多加一個白光，會和紅、綠、藍光的選項連動」 |
| — | 三原色混色示意圖 | 方格區左下「發出的光」、右下「眼睛接收的光」；深色底，三圓切成 7 塊、每塊直接塗 `COLOR_FILL` 的對應色（09-18 22:22 改；初版 `mix-blend-mode:screen`＋純紅綠藍疊出 `#FFFF00`，與光源方框、小窗的 `#F5D21C` 不同，**A**「眼睛看到的顏色、光源的顏色、三原色圈圈疊出來的顏色看起來是不一樣的」），沒有的色只畫虛線圈；播放中右圖等光走完才亮〔畫法 C〕 | **A**「在方格紙畫布的左下和右下角增加三原色三個圈圈疊加混色的示意圖，分別對應發出的光和眼睛接收到的光」 |
| — | 光源方框 | 燈罩中間方框塗發出的合成光（紅＋綠＝黃），全關為黑 | **A**「光源的灰色間可以有一個方框，會隨著射出的光線變色」 |

> 以上三項 09-18 15:51 同一則訊息提出。透明體的「光源」標字因此移到燈罩上方（左下角讓給示意圖）〔C〕；
> 示意圖框寬至少容得下標題（窄畫布上字比三個圈寬，實測 543 px 寬時被切）。

> 與 §6 的關係：§6 ① 推翻的是**成像四頁**的「觀察者眼睛」功能；本頁的眼睛是第 3、4 題 **B** 新定的，只適用色光頁，不回頭復活其他頁的眼睛。
> 第 4 題回覆 A，沒有選 C 的「看到的樣子」小窗，§6 ② 的否決仍然有效。

### 14.2 未回覆（第 10 題，原句重述）

> 「10. 明確不做（我預設都不做，要做請說）：稜鏡分光（白光→彩虹）、顏料減色混合、螢光燈／日光等不同光源光譜」

使用者未回覆 → 暫以 **C** 處理：三項都不做。使用者表態後改標 A／B。

### 14.3 模型判斷（C，使用者未表態）

1. 新鈕 `data-screen="5"`，DOM 排在眼睛之後；既有 0–4 不重新編號（沿用 §13.4 第 1 點的理由）
2. 版面：光源在左、色紙／玻璃紙在中、眼睛在右，眼睛固定、不可拖。紅、綠、藍三道光**分開畫成三條平行光線**，才看得出哪一條被吸收、哪一條通過；三條同時開時合成白光
3. 玻璃紙：3 個槽位，每槽可選「不放／紅／綠／藍」。修訂鏈：原寫「無或 8 色」→ 09-18 15:20 C「不放／紅／綠／藍／無色／黑」→
   09-18 15:27 **A**「玻璃紙刪除黑、白」（後者生效；色紙仍是五色）。每經過一片就只剩兩者共有的成分，停下的光線畫在那一片的入光面上
4. 物體色塊的「看起來的顏色」＝照射光 ∩ 物體成分；全被吸收就畫成黑色。透明體沒有物體可著色，改由射入眼睛的光線顏色呈現
5. 文字結論寫在畫布角落，不標數值（守 §2.2）
6. 不透明體的反射光線畫成射向眼睛的單一方向（課本示意圖的畫法），不做漫射扇形
7. 導覽列增為 6 顆，768 與 640 寬可能溢出 → 實作時先量測，溢出再提方案給使用者決定

### 14.4 階段切分（C，待使用者核可）

- [x]（2026-09-18 23:21 **A**「實測都通過」）階段 1：頁籤＋左欄控制（透明體／不透明體 `.seg`、RGB 三開關、顏色選擇）＋**不透明體**靜態光路、物體著色、文字結論
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 導覽列第 6 顆「色光」（`data-screen="5"`）；左欄：物體 `.seg`（透明體鈕**先停用**，階段 2 接上）、色光三顆膠囊可複選、色紙 5 色單選
      - 場面：光源在左上（出光口三顆小圓顯示各色開關）、色紙平放中下（斜看的平行四邊形，塗「看起來的顏色」）、眼睛在右上（FA `fa-eye`，不可拖）〔C〕
      - 光源 `y=−2.0`、色紙 `y=2.2`（格，相對主軸）：原取 ±2.6，燈罩頂端貼到畫布上緣
      - 圖例卡三列一律用色塊（照射光／色紙本色／看起來的顏色），不用 `.ln` 線段——`.ln` 預設是橘色，會被讀成「橘光」
      - 驗證（1366×768）：8 組光×色紙的結論文字與色紙塗色全對（例：綠光照紅紙 → `#1C1C1C`、1 道光、「看起來是黑色：沒有光進入眼睛」；紅＋綠照白紙 → 黃）；
        動畫播到 40% 光線照截；重置留在本頁且膠囊回預設；切回透鏡／視深／針孔頁，圖例、「顯示」組、眼睛圖示皆正確；
        導覽列 768 與 640 寬不溢出；640 寬結論卡不超出畫布；console 無錯誤
      - 本階段刻意沒做：色紙在播放時「光到了才變色」（屬階段 3 動畫）
- [x] 階段 1 已 commit（`ae672e4`，2026-09-18 15:20 使用者指示「commit，接著做階段 2」；使用者實測狀態未表態）
- [x]（2026-09-18 23:21 **A**「實測都通過」）階段 2：**透明體**：1–3 片玻璃紙疊加、逐片吸收、眼睛透過看
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 場面〔C〕：光源（主軸上、中央左 8 格）→ 三個槽位 `SHEET_X = [-4, 0, 4]` → 眼睛（右 8.6 格），三色光水平平行；
        玻璃紙畫成直立薄片（高 3.6、厚 0.4 格，厚度誇大才看得到顏色），半透明塗自己的顏色，下方標名稱
      - 左欄：「物體」切到透明體時「色紙的顏色」整組換成「第 1／2／3 片玻璃紙」三組膠囊（JS 依 `SHEET_CHOICES` 產生）；預設第 1 片紅、其餘不放〔C〕
      - 結論卡逐片寫「照到它的光」讓誰通過、吸收誰（多片時加「第 N 片」），最後一列「眼睛看到 X 色」
      - 驗證（1366×768）：無色＋紅＋綠 → 紅光停在第 3 片入光面（668 px ＝ 677 − 半厚）、綠藍停在第 2 片（488 px）、看到黑；
        三片都不放 → 白；黑 → 三道都停在第 1 片；關藍光後無色＋藍 → 黑；動畫 50% 照截；重置回不透明體與預設玻璃紙；
        切到透鏡頁再回來保留透明體；`?u=1.15` 左欄透明體不捲動（透鏡頁 33 px 為既有）；console 無錯誤
      - 順手清掉階段 1 不透明體光路裡沒用到的 `ends` 陣列
- [x] 階段 2 已 commit（`9a9d3e4`，含 09-18 15:27 **A**「玻璃紙刪除黑、白」）
- [x]（2026-09-18 23:21 **A**「實測都通過」，含小窗、白光、示意圖、統一色值）階段 3：播放動畫（抽屜）、重置、另外五頁回歸、導覽列寬度
      —— **2026-09-18 實作與瀏覽器驗證完成，待使用者實測後打勾**
      - 光線本身在階段 1、2 已經走 `clipByAnim()`，播放鈕、速度、改參數即回到完整顯示都是共用機制；重置與導覽列寬度在階段 1 已驗
      - 本階段補「結果出現的時機」〔C〕：~~不透明體的色紙在第一道光抵達前畫淺灰~~（09-18 22:22 色紙改塗本來的顏色後作廢；
        `COLOR_UNLIT = #DDE1E7` 仍用於小窗與結論卡的「光還在路上」，不用黑：黑是「照到了但全被吸收」）；兩種物體的結論卡最後一列（看起來是／眼睛看到）等光全部走完才出現，之前顯示「光還在路上……」
      - 說明彈窗「四個頁面都可以播」→「每個頁面都可以播」；導覽列註解「五頁」→「六頁」
      - 驗證（逐點設 `state.anim`）：不透明體 0／30% 淺灰、60% 起紅、99% 仍「光還在路上」、100% 與 null 顯示「看起來是紅色」；透明體 50%／100% 同理。
        **實際按播放沒能在 Browser pane 跑完**：pane 在背景 `document.hidden === true`、1 秒 0 個 rAF 畫格（pitfalls-services #3 已載），待使用者實測
      - 五頁回歸：各頁圖例首列、左欄組別、播放鈕可按、眼睛圖示只在色光頁；重置後凸透鏡 v=7、凹透鏡 v=−1.909、平面鏡「5.3 格」×2；console 無錯誤
      - 旁註（既有行為，本次未改）：從眼睛頁切回透鏡頁時物距沿用被眼睛頁夾過的 5.5 格（v=6.6），重置才回 5.25

## 15. 2026-09-20 第七批：眼睛模型重整、面鏡方案再裁決、說明重寫（6 項）

使用者 2026-09-20 11:07 單則訊息含 6 個編號需求。同日模型盤點後以 AskUserQuestion 提 4 題，
使用者全數選定（**B**）。**本節只記裁示與範圍，實作進度見 §15.7。**

### 15.1 已定案

| 項 | 項目 | 裁決 | 來源 |
|---|---|---|---|
| 1 | 近視／遠視的成因 | 改成**眼軸過長／過短**；調節能力夠 → 像落在視網膜上，不夠 → 落在視網膜前或後。**推翻** 2026-09-08 的「只做水晶體太厚／太薄，不做眼軸過長」。修訂鏈：09-08 **A** → 09-20 **A**（後者生效） | **A**「近視眼成因改成眼軸過長，遠視眼原因改成眼軸過短」 |
| 1a | 眼軸長短的畫法 | **只把視網膜後壁往後／往前推**，眼球前半部（角膜、水晶體）位置不動 | **B** 選項原文「只把視網膜後壁往後／往前推」 |
| 1b | 三種眼睛的調節範圍 | **完全相同**（同一組最厚／最薄焦距），差別純粹來自眼軸長 | **B** 選項原文「三種眼睛完全相同」 |
| 2 | 面鏡方案 | **維持 A 案（現行做法）定案**，2026-09-20 使用者看過對照檔後裁示。§10 末掛了 10 天的「仍等使用者裁示」到此結案，不再改反射點畫法 | **A**「維持A案」（對照檔見 §15.3；提問原話「我還是不確定要現在的方案還是球面鏡＋近軸近似＋光線真的碰到鏡面再反射＋給法線，你幫我再生另一個html檔讓我看看兩者差異再決定」） |
| 2a | 對照檔涵蓋範圍 | **只做凹面鏡，物距可拖** | **B** 選項原文「只做凹面鏡，物距可拖」 |
| 3 | 色紙的塗色 | 改回**塗「看起來的顏色」**，並同時標「實際 ○ 色／看起來 ○ 色」。**推翻** §14.1 第 4 題。修訂鏈：09-18 14:42 **B**「物體畫成它看起來的顏色」→ 09-18 22:22 **A**「畫本來的顏色」→ 09-20 **A**（最後者生效，等於回到 09-18 14:42 的 B 再加雙標籤） | **A**「色光頁的色紙我希望改回呈現看起來的顏色，並且標示實際xx色，看起來xx色」 |
| 4 | 操作說明改寫 | 拆成**「基本操作」＋「本頁原理」**兩段。基本操作含拖曳、設定、播放、重置、格線；本頁原理以國中生為主，**並加上國中生可理解的限制** | **A**「最上方的操作說明我覺得太雜，要是寫給學生看的，我希望調整成基本操作、本頁原理」 |
| 4a | 說明的呈現方式 | **隨頁切換，只顯示當前頁** | **B** 選項原文「隨頁切換，只顯示當前頁」 |
| 5 | 「完整光束」改名 | 全部改成**「其他光線」** | **A**「完整光束的名稱我想要都改成其他光線」 |
| 6 | 白光膠囊 | 脫離紅綠藍同一列，**另起一行**寫成「**全部開啟（白光）**」 | **A**「白光我不想讓他和紅綠藍三色同一階層，我想換行寫成全部開啟（白光）」 |

### 15.2 第 1 項：眼睛模型重整的範圍

現行模型（09-08 起）：`EYE_L = 4.647 格` 是常數，三種眼睛靠各自的調節範圍 `EYE_ACC` 區分。
新模型把兩者對調 —— **眼軸長變成三種眼睛的差異來源，`EYE_ACC` 三種相同**。

要動的識別符（`index.html`）：`EYE.retinaX`、`EYE_L`、`EYE_ACC`、`EYE_GLASS`、`fNeed()`、
眼球繪製（視網膜後壁的 path 端點）、視網膜小窗 `EYE_WIN_GRID`、圖例卡文字、說明彈窗。

**要重新裁示的連帶問題**〔C，模型判斷，待實作時驗證〕：

1. 眼軸一旦可變，`EYE_GLASS`（矯正度數三檔）的反解基準跟著變 —— 現行是「把物距搬到調節極限」，
   新模型要改成「把像搬回視網膜」。兩者算出的度數不同，三檔的強／正確／弱必須重算。
2. 現行「物距小於眼軸長 4.647 格 → 變成倒立放大實像」的下限（§2.3 眼睛頁右移量）綁死在常數 `EYE_L` 上。
   近視眼的眼軸變長之後這個下限跟著變大，**蠟燭的可拖範圍要重算**，否則近視眼拖到最近會出現眼睛不會發生的成像。
3. 眼球右移 4 格的餘裕（現行上限是「水晶體右側 6.194 格眼球 ＋ 遠視未矯正時像落在視網膜後 1.5 格」）
   要重新確認 —— 近視眼的視網膜往後推之後，眼球後緣會更靠近畫布右邊界。

### 15.3 第 2 項：面鏡對照檔

**§10 已經有的結論，本次不重做**（對照檔直接引用）：

- 「換成拋物面鏡」在階段 2 就做完了，鏡面弧本身已是拋物線，**誤差不來自鏡面形狀**。
- 2026-09-10 fresh-context opus 審查獨立確認：**「反射點在曲面上」＋「①精確過焦點」＋「u=f 出射平行」三者在數學上不可兼得**
  （①出射斜率恆為 `h/(f−a)`、②恆為 `h/u`，相等 ⟺ 矢高 `a=0`）。與程式怎麼寫無關。
- 既有實測（f=3、u=6、物高 2 格）：近軸公式像距 6.000 格／放大率 −1.000／①偏離焦點 0.118 格；
  拋物面真實反射像距 5.400 格／放大率 −0.900／①精確過焦點。

**所以對照檔的重點是讓使用者看見 B 案的代價**：光線真的打在鏡面上之後，
三條特徵光線**不會**精確交在 `1/u+1/v=1/f` 算出的像點上，且「物在 2F → 像在 2F、倒立同大」
這個國中課本的核心結論會變成約 0.90 倍。對照檔要把這兩個數字即時量出來顯示，不能只是畫兩張圖。

落點：`optics-sim/tmp/面鏡方案對照.html`（`tmp/` 進 `.gitignore`），裁決後刪除。

### 15.4 第 3 項：色紙的連帶處理

- 標籤從單行「紅色色紙」改成兩行「實際：紅色／看起來：黑色」（現行 `index.html` 的 `label.textContent`）。
- **播放中的中間態**：光還沒照到色紙時塗 `COLOR_UNLIT = #DDE1E7` 淺灰，且「看起來」那一行不顯示
  〔**A** 使用者 2026-09-20「復用淺灰」〕。這正是 09-18 22:22 之前的用途，等於把那個被作廢的做法接回來
  （但該做法從未進過 commit，git 歷史裡沒有原文可搬，是重新實作的）。
- **判斷門檻不是 `animTotal`**〔C〕：色紙畫在 `drawColorRays()` 之前，讀 `animTotal` 會拿到上一幀的值。
  改用各道光**自己的入射段長**（與光路迴圈同一條式子），取已開啟那幾道的最大值。
  初版誤用中心線距離 `len`，三道光沿 `n` 錯開約 ±0.46 格，會讓紅光那道在還沒碰到紙時色紙就先變色
  —— 由 09-20 的 fresh-context 審查抓到並修正。
- 專案 memory `optics-color-page-decisions.md` 第 1 條要改寫成修訂鏈，不是覆蓋。

### 15.5 第 4 項：說明重寫

現行 `index.html` 的 `#helpModal` 是一整份 90 行、6 個 `h3`、全部頁面一次攤開。
改成「基本操作」（固定五項：拖曳、設定、播放、重置、格線）＋「本頁原理」（依 `state.screen` 切換）。

**「加上國中生可理解的限制」**〔A〕落在「本頁原理」段尾，每頁一到兩句，寫這個模擬**沒有**表現什麼。
**用字必須先給使用者過目再實作**〔**A** 使用者 2026-09-20「給我過目」〕。
草擬（待使用者確認用字）：光線畫的是作圖用的特徵光線不是全部的光；透鏡當成沒有厚度；
面鏡的轉折畫在垂直平面上不是鏡面弧上（若第 2 項改採 B 案，這句要跟著改）；
眼睛頁只畫一個變數在動；色光頁的顏色是三原色的理想模型，真實顏料還有別的成分。

### 15.6 第 5、6 項：改名與白光

**「完整光束」三路併掃**（`index.html`，2026-09-20）：

```
grep -n '>完整光束\|完整光束<' index.html     → UI 標籤 1 處
grep -c "完整光束" index.html                  → 全文 26 行命中
grep -n "chkBeam\|state\.beam" index.html      → 識別符 10 行
```

聯集 26 行 ＝ **1 個 UI 標籤（`chkBeam` 的 label）＋ 2 段操作說明內文（778、782 行）＋ 23 行程式碼註解**。

- 變數名 `state.beam`／`chkBeam` **不改**〔C〕：英文識別符與中文顯示名本來就沒綁。
- **2 行註解不改**〔C〕：2099、2101 行引的是使用者 2026-09-10 的原話「可以勾選完整光束…」，
  改掉等於竄改裁示原文。保留原話，旁邊加註「（現名：其他光線）」。
- 778、782 行併入第 4 項一起重寫，不單獨改。

第 6 項純版面：三色一列、白光另起一列，`aria-pressed` 由三色反推的邏輯不變。

### 15.7 階段切分與進度

- [x] 階段 1：第 3、5、6 項（色紙雙標籤、改名、白光換行）—— 2026-09-20 實作與瀏覽器驗證完成，**待使用者實測後打勾**
      - 色紙：`fill` 改 `COLOR_FILL[info.seen]`；標籤拆兩行（`實際：○色` 恆顯示、`看起來：○色` 等光抵達）；門檻 `litAt` 見 §15.4
      - 改名：三路併掃 26 行命中 → 21 行改為「其他光線」，5 行引用使用者原話的註解保留舊名並加註「現名」；
        變數名 `state.beam`／`chkBeam` 不動。說明彈窗 784、788 行同步改名（結構重寫留在階段 3）
      - 白光：`.chips-br`（`flex-basis:100%;height:0`）斷行 ＋ 文字「全部開啟（白光）」；事件委派與 `syncLightChips()` 不受影響
      - **fresh-context opus 審查（dispatch §7，本專案目視推翻累計 2 輪）**：PASS with issues 4 條，
        3 條已修（說明彈窗漏改「按「白光」」、`len` 門檻誤差、註解描述與實作不符），
        1 條低嚴重度保留（`.chips-br` 讓該處列距 12px 而非 6px——與「不要同一階層」的意圖同向，刻意保留）
      - 驗證（1366×768，Browser pane）：白光＋紅紙 → `#E0342B`／綠光＋紅紙 → `#1C1C1C`「看起來：黑色」／
        無光＋紅紙 → 黑／綠光＋白紙 → `#1E9E46`／白光＋黑紙 → 黑；三種單色光按播放的起點都是 `#DDE1E7` 且第二行消失；
        播放跑完回復 `#E0342B`（按鈕變「重新播放」）；六頁籤全部渲染、`chkBeam` 標籤皆「其他光線」；
        白光膠囊三段序列 `只開藍 → 全開 → 全關` 正確；使用者可見文字「完整光束」0 殘留（script 內 7 次全是原話註解）；console 無錯誤
      - **待使用者實測**：iPad 觸控、教室大屏的淺灰 `#DDE1E7` 對比與兩行標籤可讀性（見 memory `sim-test-devices`）
- [x] 階段 2：第 2 項面鏡對照檔 —— 2026-09-20 完成，**等使用者看過後裁決 A/B 案**
      - 落點 `tmp/面鏡方案對照.html`（`tmp/` 已加進 `.gitignore`），`index.html` 零改動
      - A 案逐條對應現行實作：`hitX` 恆回 cx（index.html:1813）、① 連焦點（:1837）、
        像點由 `imaging()` 給（:1219）、鏡面矢高（:862）；B 案是直線與鏡面求交 ＋ 反射律 ＋ 法線
      - 兩案共用同一組參數與同一個鏡面形狀（拋物面／球面可切），差別只有「光在哪裡轉向、依什麼規則轉向」
      - **像距不給單一數字**：B 案三條出射光不交於一點，表格逐對列出 ①②／①③／②③ 的交點，
        另給平均值——只給平均會把「根本沒交在一起」藏起來
      - **交叉驗證**：f=3、u=6、拋物面時本檔算出 ①② 交點 ＝ **5.400 格**，與 §10 於 2026-09-10
        獨立記載的「拋物面真實反射像距 5.400 格」完全一致；A 案 v=6.000、m=−1.000 亦與課本一致
      - 實測（1366×768）：f2 球面 2F → A 案 4.000／B 案 2.377～2.964，散布 0.657 格、像點相距 44.9 px（肉眼可辨）；
        f3 u6 拋物面 → 散布 0.053 格、相距 18.2 px（圖上不易分辨，故加「一鍵跳到差異最明顯」按鈕）；
        u=f → A 案不成像、B 案只剩兩條光線；u<f 虛像 → 三條反向延長線散開 3.571 格（f=3、u=1.5）。
        虛線一律沿各自出射線畫（實測偏離 0.000000 px），不連到平均像點。console 無錯誤
      - **裁決後連同 `tmp/` 一起刪**
- [ ] 階段 3：第 4 項說明重寫（依賴第 2 項的裁決結果：面鏡那句限制要看選哪案）
- [x] 階段 4：第 1 項眼睛模型重整 —— 2026-09-20 完成並通過瀏覽器驗證，**待使用者實測後打勾**
      **採用 Codex 方案**〔**A** 使用者 2026-09-20「照codex」〕，取代本專案 09-20 稍早那一版「由三等分分界反解眼軸」的做法。
      - **調節範圍三種共用、且必須是字面常數** `EYE_ACC = [2.45, 3.62]`。
        **不可改回由 `fNeedOf` 反解**——反解等於「正常眼剛好對得到」、兩端沒有餘裕，
        而放寬出來的餘裕正是近視與遠視的症狀空間（正常眼實際只用到 2.519～3.525，兩端各剩 0.069／0.095）
      - **眼軸是字面常數** `EYE_AXIS = { normal: EYE_AXIS_N（素材量出 4.6472）, myopia: 5.247, hyper: 4.047 }`，各 ±0.6 格
      - **新增遠點／近點為單一真相來源**：`eyeFarPoint(L)`、`eyeNearPoint(L)`，由眼軸與調節範圍反解
      - **`EYE_U_PRESET.near` 7.5 → 5.5**；**刪除強制對焦**；**水晶體厚薄改讀實際 `f`**；**`EYE_POWER_MUL` 整組重掃**

      **本階段推翻或取代的既有裁示（修訂鏈）**：

      | 原裁示 | 原內容 | 現況 |
      |---|---|---|
      | 09-08 **A** | 「戴上正確校正的眼鏡後，水晶體的厚薄變化如戴上去前」 | **被推翻**。厚薄改讀實際 `f`，戴鏡後會跟著變。鏈：09-07 **A**「未矯正一律畫成最凸／最薄」→ 09-08 **A** → 09-20 **A**「照codex」 |
      | 09-10 **A** | 「遠視眼看近物的切換膠囊…點選後還是在螢幕範圍內」 | **前提消失**，不是被無視。該裁示成立於 09-08 模型（遠視最厚 3.316 格 → u=5.5 像距 8.35 格、落在第 23.35 格而畫布只有 22 格）；現在三種眼睛共用 `EYE_ACC`，遠視最厚只有 2.45 格。實測 u=5.5 的像落在第 19.42／20.25／19.65 格，最緊的近視眼仍有 1.75 格餘裕 |
      | **B**（09-07 前後） | 「保留度數，只有一檔全對焦」「接受度數的物理意義變成近似開關式」 | **連帶失效**。強制對焦刪掉後三檔都是真的在算兩階段成像；戴正確遠視鏡看遠時 `uEye = +33.98`（是實物不是虛物）、`need = 3.616` 落在範圍內，物理自然成立 |
      | 09-20 稍早 | 眼軸由三等分分界反解（6.006／3.220） | **被取代**：反解版眼軸差到 ±1.36 格，近視眼球畫出來超出畫布約 1 格 |

      **眼球畫法**〔**A** 使用者 2026-09-20 看過 `tmp/眼球畫法對照.html` 後選「a案+等比縮放」〕：
      沿用 `clipPath` 以水晶體那條垂直線切兩段、後段沿 x 縮放 `eyeAxisRatio()`（**B** 09-20「只把視網膜後壁往後／往前推」仍然有效）。
      實測縮放比 1.12906、眼球後緣落在畫布第 **10.993** 格（右緣 11 格），**餘裕 0.0066 格**。
      餘裕看似極小但安全：眼球素材三條 path **純填色、完全沒有 `stroke`**（已 grep 確認 stroke 計數為 0），
      只會被抗鋸齒削掉不到半個像素。對照檔的另一個選項（後段平移，餘裕 0.206 格）未採用。

      **`EYE_POWER_MUL` 重掃**〔**C** 模型判斷，使用者未就倍率表態〕：舊值 `[0.55,1,3.2]`／`[0.55,1,1.7]` 在新模型下失效——
      調節範圍放寬後眼睛的補償能力變強，近視「強」檔 3.2× 實測變成**全程清楚、完全沒有症狀**。
      新值 `myopia:[0.3, 1, 6]`、`hyper:[0.3, 1, 2]`。
      **行為差異**：舊版強檔宣稱「全程糊」，新版強檔只在這隻眼睛原本就吃力的那一端糊
      （近視強檔看遠仍清楚、遠視強檔看近仍清楚），因為調節補得回來；要做到全程糊得拉到 12× 以上。

      **實測（瀏覽器，數字＝像離視網膜的格數，正＝視網膜後／負＝視網膜前）**：

      | | 裸眼 5.5 | 裸眼 14.6 | 鏡弱 | 鏡正確 | 鏡強 |
      |---|---|---|---|---|---|
      | 正常 | 0 | 0 | — | — | — |
      | 近視 | 0 | **−0.434** | 看遠 −0.308 | **兩端 0** | 看近 +0.401 |
      | 遠視 | **+0.371** | 0 | 看近 +0.258 | **兩端 0** | 看遠 −0.642 |

      裸眼清晰度轉折實測：近視 11.68（理論 11.674）、遠視 6.21（理論 6.209）、正常眼無轉折。
      矯正度數實測 `fg` ＝ 近視 **−41.6646**、遠視 **+20.7874**，與 Codex 建議的 −41.66／+20.79 相符，
      但**刻意用 `eyeCorrectPower()` 反解到機器精度**：直接寫死 2 位小數會讓遠視在 5.5 格的 `need`
      比調節下限少 1.7e-5，被 clamp 之後 `sharp` 翻成 false、畫面誤報「糊」（像其實只偏 0.000046 格）。
      六頁切換回歸正常、console 無錯誤。

      **本階段刻意沒做**：`eyeZoneEdge()` 的三等分分界（8.533／11.567）與新的清晰度分界（11.674／6.209）
      已經脫鉤，圖例的「看遠／中間／看近」從此只描述蠟燭在哪一區、不再兼任清晰度分界。
      後果是學生可能拖進「看遠」區卻發現還是清楚。Codex 方案沒提到這點、使用者也未表態，**未擅自更動**。
      解除條件：把區名改成讀 `eyeFarPoint()`／`eyeNearPoint()`，或接受兩者本來就是不同概念。

---

## 16. 2026-09-20 第八批：左欄分兩張無標題卡、刪除矯正度數、兩種重設（跨模擬第二輪，階段 3）

四支模擬同批改動的第三支，也是**改動最大的一支**（六個頁籤）。範本是
`../motion-graphs-sim/PROJECT_PLAN.md` §9。規範來源：`../_shared/design/DESIGN_SPEC.md`
第 1 節「左欄的兩張卡片」「兩種重設」、第 4 節 `.adv` 與「說明彈窗」；跨模擬的逐項去向與
A／B／C 來源標籤見 `../PROJECT_PLAN.md` §7（§7.3 有本模擬的完整控制項對照表）。commit `413a136`。

**使用者原話**〔**A**〕：「optics-sim 針孔頁的孔徑是進階，刪除眼鏡頁的矯正度數功能。
叫做放入遮板/放入紙屏，其他依照你說的做」。

| 項目 | 去向 | 來源 |
|---|---|---|
| 眼睛頁「**矯正度數**」 | **整組刪除**：`#powerGroup`、`powerSlider`、`POWER_NAME`、`syncPower()`、`state.power`、`EYE_POWER_MUL`／`EYE_POWER`／`EYE_POWER_OK`、指紋欄位、鏡片厚度的檔位插值全部移除，**不留死碼**。戴上眼鏡一律走 `eyeCorrectPower()`，鏡片厚度固定 `t = 0.5` | **A**「刪除眼鏡頁的矯正度數功能」 |
| 五個頁專屬主 `.seg`（鏡種／情境／透鏡種類／眼睛狀態／物體） | 卡 1，**移除 `.grouplabel` 可見標題**，改掛 `aria-label` | **A**「.seg 都不要有標題」 |
| 物距滑桿、焦距、色光四顆、色紙／玻璃紙 | 卡 1 基本 | **A** |
| 針孔頁 **孔徑**、孔的形狀、孔的數量、物體造型（`#shapeGroup`） | 卡 1 的「進階」摺疊區 `.adv` | **A**（孔徑由基本改判進階） |
| 遮板、紙屏 | **留在卡 1**，改名「**放入遮板**」「**放入紙屏**」 | **A** 逐字指定名稱；卡別 **B**「特徵光線→卡2，遮板紙屏→卡1」 |
| ①②③ 特徵光線（`#rayGroup`）、眼睛頁光線（`#eyeRayGroup`，由 pagectl-3 移出） | 卡 2，分組名「顯示特徵光線」 | **B** |
| `其他光線`（`#showGroup`） | 卡 2，改名「**顯示其他光線**」 | **A**「一律稱為顯示xx」 |
| 左欄最底下 | 新增「重設全部」 | **A** |
| 播放列標籤 | 「動畫速度」→「**播放速度**」 | **B**「確認改回「播放速度」」 |
| 說明彈窗 | 4590 → 829 字元，六個 `<h3>` 小節（作圖法原理、三條特徵光線怎麼走、凹透鏡為何只成虛像、色紙呈色原理）**整段刪除**，只留操作 | **A**「只講最簡單的基本操作」＋**B**「直接刪掉」 |

**修掉的既有缺陷**：舊版 ↺ 做的是「重置這一頁」＝連設定一起還原，那是「重設全部」的行為。
現在 `resetRun()` 只做 `state.playing=false; state.anim=null; animLast=0`＋重繪，
**九項頁面設定全留**；舊的 handler 改名 `resetAll()`。兩個函式不是一個函式加旗標
（`../_shared/DEV_SPEC.md` 第 1 節）。

**版面代價**（C）：加了第二張卡與「重設全部」之後透鏡頁溢出 9px，
`.panel{gap:8→6}`、`.setcard{padding:12→10px 12px}` 收緊後六頁皆零溢出（透鏡頁剩 3px 餘裕）。

**空群組自動隱藏**（C）：卡 2 在某些頁只有一組控制項，改成讀子元素的 `.hidden` 決定整張卡顯不顯示，
不再各頁重算一次條件——條件重算過去就是漏改的來源。

**驗證（本機 1366×768）**：六頁全部零溢出、無水平捲軸；兩種重設各跑一輪語意正確；
眼睛頁物理回歸——正常眼兩端清楚，近視裸眼看遠糊（`need` 3.86 > 上限 3.62）、
遠視裸眼看近糊（2.331 < 下限 2.45），戴上眼鏡兩端都落回 `EYE_ACC` [2.45, 3.62]，
**刪掉矯正度數沒有破壞成像**。console 無錯誤。
**〔真〕iPad 與教室大屏未實測**（見專案 memory「三種驗收裝置」）。

**本輪刻意沒做**：
- 「重設全部」只作用在**當前頁／當前這組共用設定**，不掃其他五頁（C，`../PROJECT_PLAN.md` §7.6 C5）。
  解除條件：使用者要求一顆鈕清六頁。
- 視深頁仍會出現「顯示其他光線」——既有行為，不在本輪範圍。
  解除條件：使用者說那一頁不該有這個開關。
- 左欄卡片 class 用 `.setcard`，**與 `_shared/template/index.html` 的 `.card` 不同名**（C）。
  起因是 dissolution-sim 的 `.card` 早就被右欄數據卡佔用，只好另取名字，本檔與
  Density_experiment 跟著它走。現況：範本與 motion-graphs-sim 用 `.card`，其餘三支用 `.setcard`。
  依跨模擬 §7.6 C1 不改名——class 學生看不到，四支同時改名有回歸風險。
  解除條件：範本要新增別的左欄卡片規則、非統一不可時，連同範本一起改成 `.setcard`。

## 17. 2026-09-21 第九批：圖例卡換成說明卡、物體上移、色光進階（10 項）

使用者單一訊息 10 個編號需求。逐項去向如下，**A** ＝使用者原話、**B** ＝ AskUserQuestion 選定、
**C** ＝模型判斷（使用者未表態）。

| # | 需求（**A** 原話節錄） | 去向 | 類別 |
|---|---|---|---|
| 1 | 「所有頁面的物體的選擇請放在基本區最上方（如果有 seg. 就放 seg. 下方），一開始就呈現」 | `#shapeGroup` 從 `.adv` 搬到卡 1，排在所有 `.pagectl` 之後、焦距之前 | **A** |
| 2 | 「左下角的圖例我覺得可以直接刪掉，標示的部分直接標相對應位置⋯直接標圈圈數字就好。如果沒有再圖上出現的就不要標」 | 刪 `.legend` 整組（DOM／CSS／渲染）；三條特徵光線改由 `drawRayBadges()` 在**入射段中點**標白底圓圈＋數字，只掃 `cut.s`（截短後真的畫出來的段） | **A** |
| 2b | 畫布上哪些要標 | 「畫布留圈圈數字，交界面、法線、水面不標，其他標」——交界面改成只在說明卡裡講名字，畫布不放標籤 | **B** |
| 3 | 「如果沒有進階的就不要硬做進階 toggle list」 | `#advBox` 只在針孔頁、色光頁（不透明體）顯示，其餘四頁整塊 hidden | **A** |
| 4 | 「色光的部分我希望可以增加進階，紙張顏色新增黃、洋紅、青」 | 新增 `#paperColorAdvGroup`（黃 6／洋紅 5／青 3）放進 `.adv`；與基本區五色同一個單選群，統一走 `syncPaperColorChips()` | **A**＋**B**（「放進新的『進階』摺疊區」） |
| 4b | 「白光的膠囊我希望改成白光(全部開啟)」 | 文案「全部開啟（白光）」→「白光（全部開啟）」 | **A** |
| 5 | 「不要叫做光轉向的平面，可以叫做薄透鏡等效面這類的」 | ~~統一叫**等效作用面**，只出現在說明卡~~ → **同日推翻，整行刪除**（見下方修訂鏈） | **A**（後者生效） |
| 6 | 「平面鏡成像請改成格數不要標，我讓學生自己數」 | `drawEqualMarks()` 拿掉兩個 `N 格` 文字，量測線段與端點刻度保留 | **A** |
| 6b | 「斜射向鏡心改名為射向鏡心」 | `RAY_NAMES.plane[1]` | **A** |
| 7 | 「頁籤名稱經簡化成2字：針孔、面鏡、透鏡」 | 三個頁籤改名（視深／眼睛／色光已是兩字） | **A** |
| 8 | 「原本放圖例的位置我覺得可以放說明，用來呈現成像結果⋯這功能做在左欄顯示卡片的 checkbox，名稱為顯示說明」 | 新增 `#infocard` ＋ `renderInfo()`；卡 2 新增 `#chkInfo`（`state.info`）。成像頁寫「性質・位置」（`imageNature()`／`imagePlace()`），眼睛頁三列、針孔頁四列、視深頁兩列、色光頁沿用原結論 | **A** |
| 8b | 預設值 | **預設關**（照共用規範「教學輔助疊層一律預設關」） | **B** |
| 9 | 「在光線吸收/反射/穿透的位置標上吸/反/穿，這些單一文字外圍有圈圈⋯把光源發射點之間的距離拉大一點。這個的開關和顯示說明 checkbox 連動」 | `drawColorFate()`，圈字塗該道光自己的顏色、吃 `state.info`；`COLOR_RAY_GAP` 0.32 → 0.6，燈罩高 1.24 → 1.7 格 | **A** |
| 10 | 「鳥的標註也是視深/實深，不要用視高/實高」 | `drawRefrMark()` 兩個情境共用 `['實深','視深']` | **A** |

### 17.1 同日第二輪（使用者看過畫面後的兩項修訂）

| 項 | 裁示 | 去向 | 類別 |
|---|---|---|---|
| 交界面說明 | 「刪除等效作用面相關敘述」 | 說明卡那一行整行刪除。修訂鏈：舊圖例卡「光轉向的平面（折射／反射面）」→ 09-21 **B** 改名「等效作用面」搬進說明卡 → 09-21 **A** 整行刪除（**最後者生效**）。淺灰垂直線本身**照畫**，只是畫面與說明卡都不再提它 | **A** |
| 色紙變色時機 | 「色紙顏色動畫我希望改成碰到眼睛才顯示顏色」 | `renderColor()` 的光路計算移到色紙之前，門檻由「入射段長」改成 `reach` ＝各道光「入射段＋等長反射段」的最大值，與說明卡、小窗的 `animTotal` 同一刻。全吸收時沒有光到得了眼睛，退化成「最後一道光碰到紙」，否則畫面會永遠停在淺灰 | **A** |

**驗證**：色光頁不透明體設 `state.anim` 掃時間軸——0.6×、0.95× 總長時色紙都是 `COLOR_UNLIT` #DDE1E7，
1.0× 才變成 `COLOR_FILL`；紅紙照白光→#E0342B、黃紙照白光→#F5D21C、黃紙只照紅光→#E0342B、
黑紙（全吸收）→#1C1C1C（退化門檻有效，不會卡在淺灰）。

### 17.2 同日第三輪：說明卡行序重訂、兩處畫布標註移除（9 項）

| # | 裁示（**A** 原話節錄） | 去向 |
|---|---|---|
| 1 | 「平面鏡等距線不標」 | `drawEqualMarks()` 連同呼叫整支刪除。修訂鏈：09-07 **A**「標格數」（§2.2 不標數值的唯一豁免）→ 09-21 **A** 只留線段 → 同日 **A** 線段也拿掉。物距＝像距改由背景格線自己數 |
| 2 | 「眼睛的成像結果不要標在眼球下方，標在說明卡」 | `drawEyeLabel()` 整支刪除；同一句話改由說明卡第 1 行負責，畫布不再重複 |
| 3 | 「色紙的底下 2 行改成實際上 x 色，看起來 x 色，要對齊一下⋯字可以大一點」＋「淺灰色我覺得上面可以打一個問號」 | 文案改「實際上X色」「看起來X色」；字級 13 → **17px**；`text-anchor` middle → **start**（起點 `G.cx − 1.05 格`，兩行左緣對齊）；未上色時色紙中央畫 `?`（0.55 格、#5F6B7E） |
| 4 | 「平行光說明太長，縮短一點」 | `出射光通過焦點 F` ／ `出射光發散，延長線交在虛焦點 F` |
| 5 | 「大原則是第一行寫成像位置，第二行寫成像性質，第三行寫其他性質」 | **六頁共用的行序**（色光頁除外，它沒有成像）。成像頁第 3 行只有面鏡有：`像與物體左右相反` |
| 6 | 「不要寫通過 n 道光，直接寫清晰但暗/模糊但亮，m 個完整的像都不要寫」 | 針孔頁三行：`像成在紙屏上`／`倒立實像`／`清晰但暗`｜`模糊但亮` |
| 7 | 「眼睛頁一樣也是第一行寫成像位置(視網膜前/上/後)，第二行寫性質」 | 四行：位置（清晰與否黏在同一行）／`倒立縮小實像`（放大率要乘 `mg`）／眼睛狀態・調節區／矯正或水晶體 |
| 8 | 「視深頁第一行寫視深大/小於實深，第二行寫成像性質」 | 三行：`視深小於實深`｜`視深大於實深`／`正立虛像`（折射虛像**不談放大率**）／`X 以為光是沿直線射來的，看到的是虛像` |
| 9 | 「色光頁列出四行，分別是紅光、綠光、藍光，標示被反射/吸收/穿透，第四行看起來 x 色」 | 固定四行，每行行首是該道光的色塊。不透明體＝反射／吸收，透明體＝穿透／吸收（多片時寫「被第 N 片吸收」），沒開的光寫「沒有照射」並降一階字重 |
| 10 | 「圖例的樣式去做說明卡字太小了，請重新設計，多設計幾個方案⋯做成一個 html 檔讓我決定」 | `tmp/說明卡樣式方案.html`（6 個方案 × 各頁真實內容），**待裁決**。tmp/ 已在 .gitignore |

### 17.3 說明卡樣式定案：款式 8（左欄卡片語彙）

**經過**：第一輪我自己想了六個樣式，使用者「這些樣式都不滿意，你可參考其他共用規範或是其他頁面的模擬」
（本專案跨 session 第 1 次目視推翻）。改成**只用既有語彙**重提三款、再追加五款，共八款，
使用者選定**第 8 款**〔**B**，選項原文「左欄卡片語彙（.setcard ＋ .grouplabel）」〕。

**定案規格**（`index.html` 的 `.infocard`）：
| 部位 | 規格 | 出處 |
|---|---|---|
| 容器 | 白底 `--c-surface`、1px `--c-bd`、圓角 10、`padding:10px 12px`、`gap:6px` | 本檔 `.setcard` |
| 小標題 `.k` | 12px/700、`--c-text-3` | 本檔 `.grouplabel` |
| 成像位置 `.pos` | 17px/700、`--c-text` | `DESIGN_SPEC` §5 標題列級距 |
| 成像性質 `.nat` | 20px/700、`--c-text` | `dissolution-sim` 的 `.card.state .val` |
| 其他 `.etc` | 13px/400、`--c-text-2`（`.off` 再降一階到 `--c-text-3`） | `DESIGN_SPEC` §5 輔助級距 |

`renderInfo()` 改成 `group(小標題, 字級類別, [行…])` 的輸出，六頁的小標題：
成像位置／成像性質／另外｜眼睛・水晶體・矯正（眼睛頁）｜視深與實深（視深頁）｜
三原色照到色紙・三原色穿過玻璃紙・結果（色光頁）｜成像・出射光（不成像與平行光的單段情形）。

**順手修掉的缺陷**（C，實作預覽時才發現）：透明體的「被第 N 片吸收」原本取**插槽索引** `st.k + 1`，
而插槽有三格、中間可以空著——「藍—空—紅」時紅紙的 `st.k` 是 2，說明寫「第 3 片」，
學生在畫面上只數得到兩片。改成取 `steps` 陣列裡的位置。

**驗證**：`tmp/說明卡_所有情境.html`——**從實際跑出來的 `#infocard` DOM 擷取**的 68 個情境
（透鏡 13、面鏡 13、針孔 3、眼睛 15、視深 2、色光不透明 13、色光透明 9），不是手打的樣本。
擷取時踩到 pitfalls-browser-pane #28（pane 隱藏 → `stageW/stageH` 停在 0 → `render()` 提早 return），
每次操作後補 `relayout()` 解決。

### 17.4 同日第四輪：說明卡文字精簡（9 項）

| # | 裁示（**A** 原話節錄） | 去向 |
|---|---|---|
| 1 | 「僅保留 f~2f 間/2f 外/2f 上/f 內」＋「凸透鏡 f 內⋯與物體同側，凹面鏡 f 內⋯鏡後」 | `imagePlace()` 只回段名；兩個例外寫死（凸透鏡虛像＝與物體同側、凹面鏡虛像＝鏡後）。凹透鏡與凸面鏡恆在 f 內，仍用段名 |
| 2 | 「出射光一律稱為光線」＋兩種講法 | 小標題「出射光」→「光線」；內容改 `會聚，通過焦點 F`／`發散，延伸線通過虛焦點 F`。連帶把「出射光互相平行，不成像」改成「光線互相平行，不成像」（說明卡與畫布標註兩處同改） |
| 3 | 「凹面鏡和凸面鏡都不要提到左右相反」 | 左右相反只剩平面鏡，且**併進成像性質那一段**當補充行（`.nat.sub`，13px/400） |
| 3b | 「一律都不要有『另外』字樣」 | 三處小標題改名：面鏡→併段、針孔→「孔徑」、視深→整段刪除 |
| 4 | 「平面鏡的成像位置寫物距=像距」 | `物距 ＝ 像距` |
| 5 | 「針孔的成像位置寫『像在紙屏上，物距>/=/<像距』，成像性質寫倒立放大/等大/縮小實像」 | 由 `pinU()`／`pinS()` 的比值算，±2% 容差判等大；性質餵進同一支 `imageNature()` |
| 6 | 「眼睛⋯寫『像在視網膜上/前/後』，眼睛和水晶體都不用寫」 | 位置不再附「看得清楚／看起來是糊的」；刪掉「眼睛」「水晶體」兩段，戴眼鏡時保留「矯正」 |
| 7 | 「視深的部分寫視深>/<實深，另外不要寫」 | `視深 ＜ 實深`／`視深 ＞ 實深`；第三段刪除 |
| 8 | 「三原色照到色紙/穿過玻璃紙統一寫三原色⋯字級和粗體和『看起來 x 色』一樣。結果字樣改成『看起來』，『看起來 x 色』寫成『x 色』⋯有混色的標一下混色的內容」 | 小標題統一「三原色」與「看起來」；三行改用 `.nat`（20px/700）；新增 `colorMix()` 產生「（紅＋綠＋藍）」，單色與黑色不加 |
| 9 | 「請將上述做成 html 檔讓我確認」 | `tmp/說明卡_所有情境.html` 重新生成，70 個情境 |

**驗證**：重新遍歷全部情境並逐筆核對——透鏡五檔（f ~ 2f 間／2f 上／2f 外／不成像／與物體同側）、
凹透鏡與凸面鏡五檔恆「f 內」、凹面鏡 f 內「鏡後」、平面鏡「物距 ＝ 像距」＋補充行、
四種平行光兩種講法、針孔三種距離關係（含孔擺在 `(paperV−u)/2` 時的「物距 ＝ 像距・倒立等大實像」）、
眼睛 15 種（上／前／後）、視深兩種、色光 22 種（含四種混色標記與播放中）。
卡片內文字已無「另外」「出射光」字樣（`textContent` 全掃驗證）。

### 17.5 同日第五輪：說明卡字級與寬度（4 項）

流程與前四輪不同：使用者中途改口「幫我改在說明卡_所有情境html」，**先只改預覽檔**，
看過之後「你這樣改是對的，幫我回流修改 index.html」〔**A**〕才回流。

| # | 裁示（**A** 原話節錄） | 去向 |
|---|---|---|
| 1 | 「三原色像是『紅光：反射』之類的字體大小和成像位置像是『f ~ 2f 間』大小一樣」 | 新增 `.infocard .col`（17px/700，flex ＋ gap 8px 放色塊），色光三行由 `.nat` 改用它；`.col.off` 承接「沒有照射」的灰字 |
| 2 | 「針孔的說明刪除『孔徑』標題」 | `group()` 的 `k` 傳空字串就不畫 `.k`；針孔第三段變成無標題的「清晰但暗／模糊但亮」 |
| 3 | 「戴上眼鏡後，刪除矯正的相關文字」 | 刪掉 `group('矯正', …)` 整段；連帶刪除因此只剩零個呼叫點的 `powerLabel()`（§8 不留死碼） |
| 4 | 「我希望這些說明卡有統一的寬度」 | `.infocard{width:calc(264px * var(--u))}`。264 ＝ 70 個情境裡最長的「發散，延伸線通過虛焦點 F」（20px 粗體）實測 262px ＋ 2px |

**一處 C 模型判斷，使用者未表態**：第 1 項把三原色降到 17px 之後，「看起來」那一行**維持 20px**，
與 §17.4 第 8 項〔**A**「字級和粗體和『看起來 x 色』一樣」〕相衝。取這次點名的一邊，
在預覽檔頁首寫明並請使用者裁示，使用者回「你這樣改是對的」——**歸 B（看過預覽檔後認可）**。

**順手補的一致性缺口**（C）：`.nat` 原本沒有 `display:flex`，色光結論行的色塊緊貼文字，
而預覽檔的 `.nat` 有 flex ＋ gap 8px。使用者確認的是預覽檔的樣子，故把模擬這邊補成一樣。

**驗證**（重新 `navigate` 後在模擬本體實測，不是看預覽檔）：
六頁卡片寬度全部回 264、`scrollWidth − clientWidth` 全為 0（含最長的平行光句與
「藍光：被第 2 片吸收」）；針孔第三段無 `.k`、眼睛戴鏡兩段無「矯正」、
色光三行 computed `font-size` 17px 而結論行 20px、`gap` 8px。
`node --check` 通過，`powerLabel`／`group('矯正'`／`group('孔徑'` 全庫零命中。

### 17.6 同日第六輪：播放中的圈圈數字、箭頭畫法（2 項）

| # | 裁示（**A** 原話） | 去向 |
|---|---|---|
| 1 | 「圈圈數字在動畫播放時會顯示成 nan，而且有多個圈圈數字時只會顯示 1 個」 | `clipByAnim()` 的 `cut()` 原本只 push 四個座標，截出來的段**不帶 `ray`／`beam`**。於是播放中 `drawRayBadges()` 讀到 `s.ray === undefined`：`String(undefined + 1)` 就是「NaN」，而 `seen.has(undefined)` 在第一段之後恆為真 → 三個編號只畫得出一個。改成 `Object.assign({}, s, {…})` 沿用原段全部欄位 |
| 2 | 「箭頭樣式好醜，好像一個方形突出三角形的尖端。你可以看看我之前溶解度的箭頭怎麼改的」 | 比照共用規範「桿止於三角形底邊」：marker 的 `refX` 由 8.5 改成 0（三角形底邊對齊路徑終點），畫線那端新增 `segDTip()` 往回退 `3.96 × 線寬`（＝三角形長度），總長不變 |

**第 2 項的來源**（§4 回流規定）：規範 `_shared/design/components-spec.html` 的向量箭頭一節
（「桿的終點要設在三角形的底邊」「**改 lineCap 沒有用**：round／square 只會讓尖端變成圓頭或更長的方頭」），
既有 canvas 實作 `dissolution-sim/index.html:2513-2534` 的 `arrow()`。
本檔是 SVG marker、不是 canvas，**這是把同一條規則換成 SVG 的寫法，不是逐字搬**：
canvas 版縮的是 `lineTo` 的終點，這裡縮的是 path 的 `d` 並把 marker 的錨點移到底邊。

**原本為什麼會突出**：線用 `stroke-linecap:'round'`，而 `refX=8.5` 讓線的端點落在三角形尖端
（x=9）後方僅 0.5 個 viewBox 單位處；圓線帽再往前伸半個線寬，於是**比尖端還前面**，
白邊那一層（線寬 ＋3.4）更明顯。

**枚舉範圍**（§1）：`grep -n "marker-end" index.html` 命中 4 處實線群 ——
透鏡與面鏡 `drawRays`、眼睛 `drawEyeRays`、視深頁、色光頁，四處連同各自的白邊層全部改；
針孔頁（無箭頭）、虛線層與平行光束層（無 marker）不動。

**順手清掉的繞路**（C）：色光頁的 `colorOf()`——用起點座標反查原線段拿回 `ray`——
存在的唯一理由就是「`clipByAnim` 不帶 `ray`」。第 1 項修好之後直接讀 `s.ray`，該函式刪除。
這是同一個根因的第二個受害點。

**驗證**：透鏡頁播放 5 個時刻（0.1／0.3／0.5／0.75／0.99 × animTotal）＋靜止，
編號一律是「①②」（第三條特徵光線預設關閉，`state.rays = [true,true,false]`），無 NaN；
六頁各跑一次靜止與播放中的 `render()` 無例外；色光頁三段箭頭顏色仍是紅綠藍三色。
箭頭幾何：`refX` 讀回 0，線寬 2.02 → 三角形長 8.0px，**相鄰兩段的空隙量到 8.0／8.5px**
（＝讓給三角形的長度），折點位置不變。

**編號位置的處置**（C，使用者未表態）：截短的段中點會隨光前緣移動，數字會在畫面上滑一整段。
改成釘在**原始入射段的中點**（`cut` 另存 `ox2`／`oy2`／`cutK`），光走過中點（`cutK ≥ 0.5`）才出現。

**第二意見**（dispatch §7，本專案跨 session 第 2 輪目視推翻後觸發）：派了 fresh-context 審查 agent，
只給驗收條件與檔案路徑、不給診斷。結論 PASS with issues，三條 finding 的處置：

| 嚴重度 | finding | 處置 |
|---|---|---|
| 高 | 色光頁不透明模式：`reach` 用解析式 `2 * t`，`animTotal` 是 clipByAnim 逐段 `Math.hypot` 加總，兩者差在浮點末位。播到底時 `state.anim` 被夾在 `animTotal`，`state.anim >= reach` 永遠不成立 → 色紙停在淺灰加問號，而說明卡與小窗已給出結論 | **已修**：比較改成 `>= reach - 1e-6`。自行復現確認：色紙 4–7（三道光全反射時 `reach` 恰等於 `animTotal`）播完仍是問號，0–3（有光被吸收，差一整個反射段）正常 |
| 低 | 色光頁 `colorOf()` 用起點座標回查顏色，`find` 落空會 TypeError | **已不存在**：審查用的是 14:10:57 的版本，該函式在同一輪的箭頭修改裡已一併刪除（現況 `grep -c colorOf` ＝ 0） |
| 低 | `segDTip()` 在 `len ≤ 1` 時退回 `segD`，那一瞬間圓線帽又會從尖端冒出；`1 < len < 3.96×線寬` 時三角形會超出該段終點 | **不修**（C）：只發生在播放中每段剛長出來的頭幾幀（8px，約 0.02 秒），靜止時最短的段也有 140px。要修得替那些段單獨關掉 marker，為一兩幀的瑕疵把 marker 從 `<g>` 繼承拆成逐段屬性，不划算 |

**同輪追加：箭頭加白框**〔**A** 使用者 2026-09-21，三則指示逐步收斂〕

| 版本 | 使用者原話／附圖 | 做法 | 為什麼不行 |
|---|---|---|---|
| v1 | 「尖端三角形也要有白框，但⋯不要三角形的白框疊到原本的線上」 | marker 內加一個外擴的白三角形，尖端也外推 | 白框從尖端伸出去，蓋住從折點出發的**下一段**光線，裂出約 5px 白縫 |
| v2 | — | 尖端收回、與實色尖端重合；肩膀不補 | 使用者附圖「左邊是你做的、右邊是我要的」：**肩膀沒有白框** |
| v3 | 「你的箭頭尖端形狀很奇怪」＋附圖「紅色這樣才是我要的」 | 白框**完整包住**三角形（含尖端），實色是乾淨的尖三角形 | 定案 |

**v3 的關鍵是換掉 `<marker>`**：marker 是附屬於某一段 path 的，實測它會疊在**後面那一段**
光線的起頭上（v1 的白縫就是這樣來的），而且 marker 內部沒辦法讓白框只露在外圈、
又不蓋住自己那條線——v2 的「尖端不外推」正是為了繞開這個限制，結果尖端形狀就歪了。

改成自己畫兩個三角形（`arrowHead(s, sw, grow)`），靠**圖層順序**解決：
- **白框**（`grow = 1.7`，與線的白邊單邊等寬）畫在**線的白邊那一層**，最先畫
- **實色三角形**（`grow = 0`）跟線同一層，後畫

於是白框不管外擴多少都不可能蓋到任何一條線的顏色，尖端就能放心往前推。
外擴後仍是相似三角形：尖端前推 `grow / sin θ`（θ ＝ 半頂角，`tan θ = 1.496/3.96 = 0.378`），
底邊後退 `grow`，半底邊按 `hh/h` 等比放大。

**順手刪掉的死碼**（§8）：四個 `<marker>` 定義與 `LIGHT_MARK`，全庫零引用。

**驗證**：
- `tmp/箭頭白框預覽.html`——**`arrowHead()`／`segDTip()` 用 regex 從 `index.html` 抽出來直接執行**，
  不是另外寫一份。深色底那組看得最清楚：白框沿整個輪廓連成一圈、把尖端也包住。
- 模擬本體：五個有光線的頁各有 8 個三角形（4 段 × 白框＋實色），色光頁的實色是紅綠藍三色；
  **白框的 DOM 位置全部早於實色三角形與所有彩線**（逐頁比對 index）；
  六頁 × 五個播放時刻掃 `d` 屬性，無 NaN／undefined。

**針孔頁補箭頭**〔**A** 使用者 2026-09-21「針孔成像的主要光線也要有箭頭」〕：
通過孔的那幾道光（`cut.s`）套同一支 `arrowHead()`，線寬用這一頁的 `2.6 × G.sc`。
**只畫在每道光的前端**，不是每段末端都畫（C）：這一頁的孔不是轉折點，光是直線穿過去的，
在孔上插箭頭只會在那個小缺口堆一團、還被擋板蓋掉一半。判準是「同一個 ray 最後出現的那一段」——
靜止時就是打到紙屏那一段，播放中就是正在生長那一段，兩種情形都對。
被擋住的光（`cut.bs`）不標：它們的重點是過不去，加箭頭反而像走得通。

驗證：孔小／中／大 ＝ 3／9／15 道，箭頭數與道數一一對應（白框與實色各一個），
白框的 DOM 位置全部早於實色；勾「其他光線」後箭頭數不變（被擋的沒有被誤標）；
四個播放時刻每道都只有一個箭頭。

**已知的外觀取捨**（C）：折點處，前一段箭頭的白框會外擴到後一段線的兩側，
看起來像箭頭長了一圈白邊。這是圖層順序的必然結果，也是**不切斷線**所要付的代價
（v1 那種「線被切斷」的相反面）。


**修正後回歸**：八種色紙 × 半程與播完 ＝ 16 組全通過（半程仍是問號，容差沒有把時機放寬）；
六頁 × 五個播放時刻 ＝ 30 組掃畫布文字，無 NaN／undefined。

**順帶清掉的死碼**（C）：`lightList()`（把位元列成「紅光、綠光」）在色光頁改成三原色各一行之後沒有呼叫端，移除。

**驗證（Claude 內建瀏覽器）**：重設全部後逐頁讀 `#infocard`——
透鏡五檔（2f 外→像距 f~2f 間／2f 上→2f 上／f~2f→2f 外／f 上→不成像／f 內→正立放大虛像）、
凹透鏡、平面鏡（`像在鏡後，像距等於物距`＋`正立等大虛像`＋`像與物體左右相反`）、凹面鏡、平行光、
針孔小孔（清晰但暗）／大孔（模糊但亮）、正常眼（`像落在視網膜上，看得清楚`＋`倒立縮小實像`）、
遠視眼看近（`像落在視網膜後，看起來是糊的`＋`水晶體已經調到最厚，還是不夠`）、近視戴眼鏡（`凹透鏡矯正・度數剛好`）、
鳥看魚（`視深小於實深`）／魚看鳥（`視深大於實深`）、色光白光紅紙（反射／吸收／吸收／看起來紅色）、
關掉綠光（`綠光：沒有照射`）、黑紙（三行吸收＋看起來黑色）、透明體（`紅光：穿透`）。
色紙問號實測存在（x 496.58、y 293.67、字級 25px＝0.55 格，落在色紙 y 269～301 之內）。

**踩到的既有坑**：Browser pane 隱藏時 `stageW/stageH` 停在 0，`render()` 開頭就 return，
說明卡讀出來全是空陣列——症狀與 pitfalls-browser-pane #28 同族，解法是先在頁面呼叫 `relayout()`。

**順帶清掉的死碼**（C）：`syncOverlays(im, P, eye)` 的 `P` 只給舊圖例卡判斷第③條有沒有被遮板擋住，
圖例卡移除後六個呼叫端一併改成 `syncOverlays(im, eye)`；卡 2 的「三組全藏就收整張卡」也失效
（「顯示說明」六頁都有），整段移除。

**一個實作陷阱**（C）：平行光模式的五道光借用 `ray` 0～4 當索引，第一版會被 `drawRayBadges()`
標成①②③。`drawRays()` 末尾改成 `if (!im.parallel)` 才呼叫。

**驗證（Claude 內建瀏覽器，1366×768 等比）**：六頁 × 成像情境逐項讀 `#infocard` 的文字——
凸透鏡 2f 上「倒立等大實像・像在透鏡另一側，像距 2f 上」、f 上「此位置不成像」、f 內「正立放大虛像」、
凹透鏡「正立縮小虛像・像與物體同側」、平面鏡「正立等大虛像・像在鏡後，像距等於物距」、
凹面鏡「倒立放大實像・像在鏡前」、凸面鏡「正立縮小虛像・像在鏡後」、遮板全遮「透鏡被完全遮住」、
針孔（大孔三孔）四列、眼睛（近視看近）三列、視深兩個情境各兩列且量測線都標「實深／視深」、
色光不透明體「反／吸／吸」、透明體「穿／吸／吸」。重設全部後 `chkInfo` 回到未勾、說明卡收起。
console 無錯誤。圈字間距實測：透明體是下限，0.6 格間距下圓心距 27px 對直徑 20px。

**版面代價**：透鏡頁左欄在 1366×768 需捲動約 74px（物體組從摺疊區展開 ＋ 新增「顯示說明」一列），
其餘五頁零溢出。**未自行刪減任何控制項**——要不要收，是使用者的決定。

**本輪刻意沒做**：
- 針孔頁的「其他光線」與視深頁的反向延長線**沒有就地標名**（C）：第 2 項只點名了光線的圈圈數字，
  這兩者沒有編號可標，硬加文字標籤會把畫面塞滿。它們的說明留在說明卡。
  解除條件：使用者說畫布上也要標它們。
- **〔真〕iPad 與教室大屏未實測**（見專案 memory「三種驗收裝置」）：圈字直徑 0.44 格、
  說明卡字級 12px×U，在大屏上讀不讀得清楚只能現場看。
